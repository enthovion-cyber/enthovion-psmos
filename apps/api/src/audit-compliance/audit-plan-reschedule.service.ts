import { Injectable } from '@nestjs/common'; import { RequestUser } from '../common/decorators/current-user.decorator'; import { AuditPlanService } from './audit-plan.service';
@Injectable() export class AuditPlanRescheduleService { constructor(private readonly plans:AuditPlanService){} reschedule(user:RequestUser,id:string,dto:Record<string,any>){return this.plans.transition(user,id,'reschedule',dto);} }
