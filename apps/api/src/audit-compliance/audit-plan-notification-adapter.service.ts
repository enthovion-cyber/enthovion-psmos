import { Injectable } from '@nestjs/common'; import { NotificationsService } from '../notifications/notifications.service';
@Injectable() export class AuditPlanNotificationAdapterService { constructor(private readonly notifications:NotificationsService){} async planEvent(input:{tenantId:string;siteId?:string|null;planId:string;planCode:string;planTitle:string;eventType:string;message:string;recipientIds:Array<string|null|undefined>;criticality?:string}){const ids=[...new Set(input.recipientIds.filter((id):id is string=>Boolean(id)))];return Promise.allSettled(ids.map(userId=>this.notifications.notifyUser({tenantId:input.tenantId,userId,siteId:input.siteId,type:`audit.plan.${input.eventType}`,module:'audit',title:`${input.planCode} - ${input.planTitle}`,message:input.message,relatedRecordId:input.planId,relatedRecordType:'Audit Plan',relatedUrl:`/audit-compliance/plans/${input.planId}`,priority:['Safety-Critical','Regulatory-Critical','PSM-Critical'].includes(input.criticality??'')?'Safety-Critical':'Normal'})));} }
