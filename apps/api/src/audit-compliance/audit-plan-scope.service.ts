import { Injectable } from '@nestjs/common'; import { RequestUser } from '../common/decorators/current-user.decorator'; import { AuditPlanService } from './audit-plan.service';
@Injectable() export class AuditPlanScopeService { constructor(private readonly plans:AuditPlanService){} list(user:RequestUser,id:string){return this.plans.childRows(user,id,'scope');} save(user:RequestUser,id:string,dto:Record<string,any>,rowId?:string){return this.plans.saveChild(user,id,'scope',dto,rowId);} remove(user:RequestUser,id:string,rowId:string,reason:string){return this.plans.deleteChild(user,id,'scope',rowId,{reason});} }
