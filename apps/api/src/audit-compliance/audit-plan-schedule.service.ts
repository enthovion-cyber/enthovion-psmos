import { Injectable } from '@nestjs/common'; import { RequestUser } from '../common/decorators/current-user.decorator'; import { AuditPlanService } from './audit-plan.service';
@Injectable() export class AuditPlanScheduleService { constructor(private readonly plans:AuditPlanService){} schedule(user:RequestUser,id:string){return this.plans.transition(user,id,'schedule');} postpone(user:RequestUser,id:string,dto:Record<string,any>){return this.plans.transition(user,id,'postpone',dto);} cancel(user:RequestUser,id:string,dto:Record<string,any>){return this.plans.transition(user,id,'cancel',dto);} }
