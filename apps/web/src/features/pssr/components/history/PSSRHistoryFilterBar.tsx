'use client';
import { inputClass } from '../pssr-ui';
export function PSSRHistoryFilterBar({ filters, onChange }: { filters: Record<string, any>; onChange: (filters: Record<string, any>) => void }) {
  return <div className="grid gap-2 rounded-xl border border-cyan-300/10 bg-[#07182a]/95 p-3 md:grid-cols-4"><input className={inputClass} value={filters.search ?? ''} placeholder="Search history..." onChange={(e) => onChange({ ...filters, search: e.target.value })} /><select className={inputClass} value={filters.category ?? ''} onChange={(e) => onChange({ ...filters, category: e.target.value || undefined })}><option value="">All categories</option>{['CREATE', 'UPDATE', 'LIFECYCLE', 'READINESS', 'CHECKLIST', 'FIELD', 'DOCUMENTS', 'TRAINING', 'TESTING', 'PUNCH', 'AUTHORIZATION', 'ATTACHMENTS'].map((item) => <option key={item}>{item}</option>)}</select><input className={inputClass} type="date" value={filters.date_from ?? ''} onChange={(e) => onChange({ ...filters, date_from: e.target.value || undefined })} /><input className={inputClass} type="date" value={filters.date_to ?? ''} onChange={(e) => onChange({ ...filters, date_to: e.target.value || undefined })} /></div>;
}
