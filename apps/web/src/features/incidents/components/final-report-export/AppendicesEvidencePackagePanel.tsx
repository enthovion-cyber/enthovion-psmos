import { Badge } from '../shared/IncidentStatusBadge';
import { TabPanel } from '../shared/IncidentTabPrimitives';
export function AppendicesEvidencePackagePanel({ rows, onGeneratePackage }: any) { return <TabPanel title="Appendices / Evidence Package Panel"><div className="grid gap-2">{(rows ?? []).map((row: any) => <div key={row.appendix} className="rounded-lg border border-slate-200 p-3 text-xs dark:border-cyan-300/10"><div className="flex justify-between gap-2"><b>{row.appendix}</b><Badge value={row.exportStatus} /></div><div className="mt-1 text-slate-500">Files {row.fileCount ?? 0} · Source {row.sourceStatus} · Restricted {row.restrictedContent ? 'Yes' : 'No'}</div></div>)}</div><button className="mt-3 rounded-lg border border-slate-200 px-3 py-2 text-xs font-black dark:border-cyan-300/10" onClick={onGeneratePackage}>Generate Evidence Package</button></TabPanel>; }
