import { InfoRows, TabPanel } from '../shared/IncidentTabPrimitives';
export function RedactionPermissionPreviewPanel({ data }: { data: any }) { return <TabPanel title="Redaction / Permission Preview Panel"><InfoRows rows={[['Export audience', data?.exportAudience], ['Redaction profile', data?.redactionProfile], ['Medical fields', data?.medicalFields], ['Confidential fields', data?.confidentialFields], ['Restricted evidence', data?.restrictedEvidence], ['Personal data redaction', data?.personalDataRedactionStatus], ['Legal/regulatory redaction', data?.legalRegulatoryRedactionStatus]]} />{(data?.warnings ?? []).map((warning: string) => <div key={warning} className="mt-2 rounded-lg border border-amber-400/25 bg-amber-500/10 p-2 text-xs text-amber-700 dark:text-amber-200">{warning}</div>)}</TabPanel>; }
