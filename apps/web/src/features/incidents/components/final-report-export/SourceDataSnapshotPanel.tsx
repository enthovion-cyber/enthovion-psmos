import { InfoRows, TabPanel, formatDate } from '../shared/IncidentTabPrimitives';
export function SourceDataSnapshotPanel({ snapshot, onCreate }: any) { return <TabPanel title="Source Data Snapshot Panel"><InfoRows rows={[['Snapshot number', snapshot?.snapshot_number ?? snapshot?.snapshotNumber], ['Created by', snapshot?.created_by], ['Created at', formatDate(snapshot?.created_at)], ['Approval state', snapshot?.approvalState ?? snapshot?.approval_state_json?.status], ['Redaction profile', snapshot?.redaction_profile ?? snapshot?.redactionProfile], ['Snapshot status', snapshot?.snapshot_status ?? snapshot?.snapshotStatus], ['Notes', snapshot?.notes]]} /><button className="mt-3 rounded-lg border border-slate-200 px-3 py-2 text-xs font-black dark:border-cyan-300/10" onClick={onCreate}>Create Snapshot</button></TabPanel>; }
