import { Badge } from '../shared/IncidentStatusBadge';
import { TabPanel } from '../shared/IncidentTabPrimitives';
export function ExportOptionsPanel({ options, onExport }: any) { return <TabPanel title="Export Options Panel"><div className="grid gap-2 sm:grid-cols-2 xl:grid-cols-3">{(options ?? []).map((row: any) => <button key={row.format} onClick={() => onExport(row)} disabled={!row.enabled} title={row.disabledReason} className="rounded-lg border border-slate-200 p-3 text-left text-xs disabled:cursor-not-allowed disabled:opacity-50 dark:border-cyan-300/10"><div className="flex justify-between"><b>{row.format}</b><Badge value={row.enabled ? 'Available' : 'Blocked'} /></div><p className="mt-1 text-slate-500">{row.disabledReason ?? 'Backend export will apply redaction and audit/history.'}</p></button>)}</div></TabPanel>; }
