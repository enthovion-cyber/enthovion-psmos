import { ExportStatusBadge } from '../shared/ExportStatusBadge';
import { TabPanel, formatDate } from '../shared/IncidentTabPrimitives';
export function DistributionDownloadLogPanel({ rows }: { rows: any[] }) { return <TabPanel title="Distribution / Download Log Panel"><div className="grid gap-2">{(rows ?? []).map((row: any) => <div key={row.id} className="rounded-lg border border-slate-200 p-2 text-xs dark:border-cyan-300/10"><div className="flex justify-between"><b>{row.export_type ?? row.action_type}</b><ExportStatusBadge value={row.status} /></div><div className="text-slate-500">{row.requested_by ?? row.recipient_email ?? row.recipient_user_id ?? '-'} · {formatDate(row.completed_at ?? row.requested_at ?? row.created_at)} · {row.purpose_reason ?? '-'}</div></div>)}{!(rows ?? []).length ? <p className="text-xs text-slate-500">No download, export, distribution, or share log records yet.</p> : null}</div></TabPanel>; }
