import { Badge } from '../shared/IncidentStatusBadge';
import { TabPanel } from '../shared/IncidentTabPrimitives';
export function ReportSectionBuilderPanel({ rows, onToggle }: any) {
  return <TabPanel title="Report Section Builder Panel"><div className="overflow-x-auto"><table className="min-w-full text-xs"><thead className="text-left text-slate-500"><tr><th className="py-2">Order</th><th>Section</th><th>Required</th><th>Included</th><th>Source status</th><th>Redaction</th><th>Missing data / notes</th></tr></thead><tbody>{(rows ?? []).map((row: any) => <tr key={row.sectionKey} className="border-t border-slate-200 dark:border-cyan-300/10"><td className="py-2">{row.displayOrder}</td><td className="font-bold">{row.sectionTitle}</td><td><Badge value={row.required ? 'Required' : 'Optional'} /></td><td><button className="font-bold text-blue-600 disabled:text-slate-400" disabled={row.required} title={row.required ? 'Required sections cannot be excluded without permission and reason.' : 'Toggle section inclusion'} onClick={() => onToggle(row)}>{row.included ? 'Included' : 'Excluded'}</button></td><td><Badge value={row.sourceStatus} /></td><td><Badge value={row.redactionRequired ? 'Required' : 'No'} /></td><td>{row.missingData ?? row.notes ?? '-'}</td></tr>)}</tbody></table></div></TabPanel>;
}
