import { InfoRows, TabPanel, formatDate } from '../shared/IncidentTabPrimitives';
import { ReviewStatusBadge } from '../shared/ReviewStatusBadge';
export function FinalReportReviewPanel({ review, onRequest, onApprove, onReject }: any) { return <TabPanel title="Final Report Review Panel"><div className="mb-3"><ReviewStatusBadge value={review?.reviewStatus} /></div><InfoRows rows={[['Review required', review?.reviewRequired ? 'Yes' : 'No'], ['Reviewer', review?.reviewer], ['Due date', review?.dueDate], ['Review comments', review?.reviewComments], ['Approved by', review?.approvedBy], ['Approved at', formatDate(review?.approvedAt)], ['Rejection reason', review?.rejectionReason], ['Rework required', review?.reworkRequired ? 'Yes' : 'No']]} /><div className="mt-3 flex flex-wrap gap-2"><button onClick={onRequest} className="rounded-lg border px-3 py-2 text-xs font-black">Request Review</button><button onClick={onApprove} className="rounded-lg border px-3 py-2 text-xs font-black">Approve</button><button onClick={onReject} className="rounded-lg border px-3 py-2 text-xs font-black">Reject</button></div></TabPanel>; }
