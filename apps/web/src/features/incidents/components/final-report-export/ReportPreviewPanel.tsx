import { Badge } from '../shared/IncidentStatusBadge';
import { TabPanel } from '../shared/IncidentTabPrimitives';
export function ReportPreviewPanel({ preview }: { preview: any }) { return <TabPanel title="Report Preview Panel"><div className="grid gap-3 text-xs"><div className="flex items-center justify-between"><b>{preview?.reportNumber ?? 'Current source preview'}</b><Badge value={preview?.status ?? 'Not available'} /></div>{preview?.message ? <p className="text-slate-500">{preview.message}</p> : null}<div className="rounded-lg border border-slate-200 bg-slate-50 p-3 dark:border-cyan-300/10 dark:bg-[#03111f]"><b>Included sections</b><div className="mt-2 flex flex-wrap gap-2">{(preview?.includedSections ?? []).map((item: string) => <Badge key={item} value={item} />)}{!(preview?.includedSections ?? []).length ? <span className="text-slate-500">No included sections returned.</span> : null}</div></div><div><b>Warnings</b><ul className="mt-1 list-disc pl-4 text-slate-500">{(preview?.warnings ?? []).map((item: string) => <li key={item}>{item}</li>)}{!(preview?.warnings ?? []).length ? <li>No preview warnings.</li> : null}</ul></div><div>Approval/signature block: <b>{preview?.signatureBlock ?? '-'}</b></div></div></TabPanel>; }
