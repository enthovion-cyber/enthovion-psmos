import { TimelinePhaseBadge } from '../shared/TimelinePhaseBadge';
import { formatDate, TabPanel } from '../shared/IncidentTabPrimitives';
export function VisualChronologicalTimeline({ rows }: any) { return <TabPanel title="Visual Chronological Timeline">{!rows?.length ? <p className="text-xs text-slate-500">No timeline events have been added.</p> : <div className="grid gap-3">{rows.map((row: any) => <div key={row.id} className="border-l-2 border-blue-500 pl-3 text-xs"><div className="flex flex-wrap items-center gap-2"><b>{row.title}</b><TimelinePhaseBadge value={row.phase} /></div><div className="text-slate-500">{formatDate(row.event_time)} · {row.source_reliability ?? 'Reliability not set'}</div><p className="text-slate-500">{row.description}</p></div>)}</div>}</TabPanel>; }
