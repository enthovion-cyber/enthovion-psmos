import { InfoRows, TabPanel, formatDate } from '../shared/IncidentTabPrimitives';
export function TimelineReviewPanel({ review, onApprove, onReject }: any) { return <TabPanel title="Timeline Review Panel"><InfoRows rows={[['Status', review?.status], ['Requested at', formatDate(review?.requestedAt)], ['Decision', review?.decision], ['Decided at', formatDate(review?.decidedAt)], ['Reason', review?.reason]]} /><div className="mt-3 flex gap-2"><button onClick={onApprove} className="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-black text-white">Approve</button><button onClick={onReject} className="rounded-lg border border-red-300 px-3 py-2 text-xs font-black text-red-600">Reject</button></div></TabPanel>; }
