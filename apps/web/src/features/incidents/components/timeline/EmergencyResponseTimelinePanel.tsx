import { InfoPanel } from './TimelinePrimitives';
export function EmergencyResponseTimelinePanel({ data }: any) { return <InfoPanel title="Emergency / Immediate Response Timeline Panel" data={data} empty="No emergency response timeline records have been captured." />; }
