import { InfoPanel } from './TimelinePrimitives';
export function PostEventStabilizationPanel({ data }: any) { return <InfoPanel title="Post-Event Stabilization Panel" data={data} empty="No post-event stabilization records have been captured." />; }
