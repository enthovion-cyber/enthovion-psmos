import { InfoRows, TabPanel } from '../shared/IncidentTabPrimitives';
import { ReportRequiredBadge } from '../shared/ReportRequiredBadge';
export function ReportingDeterminationPanel({ data }: { data: any }) { return <TabPanel title="Reporting Determination Panel"><div className="mb-3 flex flex-wrap gap-2"><ReportRequiredBadge value={data?.reporting_required} /></div><InfoRows rows={[['Determination status', data?.determination_status], ['Applicable jurisdiction', data?.jurisdiction], ['Applicable rule/config', data?.applicable_rule_key ?? 'Missing configuration'], ['Trigger reason', (data?.trigger_reasons_json ?? []).join(', ') || '-'], ['Confidence', data?.confidence], ['Reviewer', data?.reviewer_id], ['Review status', data?.review_status], ['Notes', data?.notes]]} /></TabPanel>; }
