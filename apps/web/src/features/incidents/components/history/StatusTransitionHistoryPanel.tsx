import { HistoryEventTypeBadge } from '../shared/HistoryEventTypeBadge';
import { formatDate, TabPanel } from '../shared/IncidentTabPrimitives';
export function StatusTransitionHistoryPanel({ rows }: { rows: any[] }) { return <TabPanel title="Status Transition History"><div className="grid gap-2">{(rows ?? []).map((row) => <div key={row.id} className="rounded-lg border border-slate-200 p-3 text-xs dark:border-cyan-300/10"><div className="flex justify-between"><b>{row.event_title}</b><HistoryEventTypeBadge value={row.event_type} /></div><div className="mt-1 text-slate-500">{row.event_description}</div><div className="mt-1 text-slate-400">{row.actor_user_id ?? '-'} · {formatDate(row.created_at)}</div></div>)}{!(rows ?? []).length ? <p className="text-xs text-slate-500">No status transition events were returned.</p> : null}</div></TabPanel>; }
