import { buttonSecondary, formatDate } from '../shared/IncidentTabPrimitives';
export function HistoryHeader({ data, exporting, message, onRefresh, onExport }: any) {
  const header = data?.header ?? {};
  return <section className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm dark:border-cyan-300/10 dark:bg-[#071525]"><div className="flex flex-col gap-4 xl:flex-row xl:items-start xl:justify-between"><div><h2 className="text-lg font-black">History</h2><p className="mt-1 text-sm text-slate-500">{header.incidentNumber} · {header.title}</p><div className="mt-3 grid gap-2 text-xs sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-8"><Fact label="Status" value={header.status} /><Fact label="Events" value={header.totalHistoryEvents} /><Fact label="Last activity" value={formatDate(header.lastActivity)} /><Fact label="Last changed by" value={header.lastChangedBy} /><Fact label="Status changes" value={header.statusChangesCount} /><Fact label="Approvals" value={header.approvalEventsCount} /><Fact label="Restricted hidden" value={header.restrictedEventsHidden} /><Fact label="Audit completeness" value={header.auditCompletenessStatus} /></div></div><div className="flex flex-wrap gap-2"><button className={buttonSecondary} onClick={onRefresh}>Refresh History</button><button className={buttonSecondary} disabled={exporting} onClick={onExport}>Export History Log</button></div></div>{message ? <div className="mt-3 rounded-lg border border-blue-400/25 bg-blue-500/10 p-2 text-xs text-blue-700 dark:text-blue-200">{message}</div> : null}</section>;
}
function Fact({ label, value }: { label: string; value: any }) { return <div><div className="text-[10px] uppercase tracking-wide text-slate-400">{label}</div><div className="font-semibold">{value ?? '-'}</div></div>; }
