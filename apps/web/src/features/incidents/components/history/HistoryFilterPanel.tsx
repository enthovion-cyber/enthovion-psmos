import { Field, SelectField, TabPanel } from '../shared/IncidentTabPrimitives';
export function HistoryFilterPanel({ filters, setFilter, context }: any) {
  return <TabPanel title="Event Type Filter Panel"><div className="grid gap-3 md:grid-cols-3 xl:grid-cols-6"><Field label="Search" value={filters.search} onChange={(v) => setFilter('search', v)} /><Field label="Date from" type="date" value={filters.dateFrom} onChange={(v) => setFilter('dateFrom', v)} /><Field label="Date to" type="date" value={filters.dateTo} onChange={(v) => setFilter('dateTo', v)} /><SelectField label="User" value={filters.userId} options={context?.users ?? []} onChange={(v) => setFilter('userId', v)} /><SelectField label="Module/tab" value={filters.module} options={context?.modules ?? []} onChange={(v) => setFilter('module', v)} /><SelectField label="Event type" value={filters.eventType} options={context?.eventTypes ?? []} onChange={(v) => setFilter('eventType', v)} /></div><p className="mt-2 text-xs text-slate-500">Filters are sent to the backend history API and exports respect the same filtered/redacted data.</p></TabPanel>;
}
