import { Panel } from './PeoplePanelPrimitives';
export function PpeControlsPanel({ data }: any) { return <Panel title="PPE / Controls Panel" data={data} empty="No PPE/control records were returned by the backend." />; }
