import { Panel } from './PeoplePanelPrimitives';
export function TreatmentMedicalOutcomePanel({ data }: any) { return <Panel title="Treatment / Medical Outcome Panel" data={data} empty="No treatment/medical outcome records were returned by the backend." />; }
