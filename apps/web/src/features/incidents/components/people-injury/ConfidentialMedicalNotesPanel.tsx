import { TabPanel } from '../shared/IncidentTabPrimitives';
export function ConfidentialMedicalNotesPanel({ data }: any) { return <TabPanel title="Confidential Medical Notes Panel">{data?.canViewMedical ? (data.rows?.length ? <div className="grid gap-2 text-xs">{data.rows.map((row: any) => <div key={row.id} className="rounded-lg border border-slate-200 p-2 dark:border-cyan-300/10"><b>{row.personName ?? 'Person'}</b><p className="mt-1 text-slate-500">{row.notes ?? 'No confidential notes.'}</p></div>)}</div> : <p className="text-xs text-slate-500">No confidential medical notes were returned.</p>) : <div className="rounded-lg border border-purple-400/25 bg-purple-500/10 p-3 text-xs text-purple-700 dark:text-purple-200">{data?.hiddenCount ?? 0} medical note(s) hidden. Requires incidents.medical_fields.view.</div>}</TabPanel>; }
