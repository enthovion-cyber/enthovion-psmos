import { Panel } from './PeoplePanelPrimitives';
export function InjuryDetailsPanel({ data }: any) { return <Panel title="Injury Details Panel" data={data} empty="No injury details were returned by the backend." />; }
