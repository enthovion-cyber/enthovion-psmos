import { Panel } from './PeoplePanelPrimitives';
export function ExposureDetailsPanel({ data }: any) { return <Panel title="Exposure Details Panel" data={data} empty="No exposure details were returned by the backend." />; }
