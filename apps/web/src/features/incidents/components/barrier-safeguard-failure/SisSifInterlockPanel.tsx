import { SimpleBarrierPanel } from './BarrierPanelPrimitives';
export function SisSifInterlockPanel({ data }: { data: any }) { return <SimpleBarrierPanel title="SIS / SIF / Interlock" data={data} />; }
