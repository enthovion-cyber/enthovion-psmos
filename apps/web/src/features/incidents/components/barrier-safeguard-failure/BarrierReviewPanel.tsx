import { Badge } from '../shared/IncidentStatusBadge';
import { TabPanel, buttonSecondary } from '../shared/IncidentTabPrimitives';
export function BarrierReviewPanel({ review, onRequest, onApprove, onReject }: any) { return <TabPanel title="Barrier Review"><div className="grid gap-3 text-xs"><div className="flex justify-between"><span>Status</span><Badge value={review?.status ?? 'Not Requested'} /></div><p className="text-slate-500">{review?.comments ?? review?.rejection_reason ?? 'No review comments yet.'}</p><div className="flex flex-wrap gap-2"><button className={buttonSecondary} onClick={onRequest}>Request</button><button className={buttonSecondary} onClick={onApprove}>Approve</button><button className={buttonSecondary} onClick={onReject}>Reject</button></div></div></TabPanel>; }
