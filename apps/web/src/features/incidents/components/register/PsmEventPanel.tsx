import { Panel } from './Panel';
export function PsmEventPanel({ data }: { data:any }) { const s=data?.summary??{}; return <Panel title="Process Safety / PSM Event Panel" subtitle="PSM/PSE, LOPC, API RP 754 tier, threshold and review status."><div className="grid grid-cols-2 gap-2 text-xs md:grid-cols-4">{Object.entries(s).map(([k,v])=><div key={k} className="rounded border border-slate-200 p-2 dark:border-cyan-300/10"><b className="text-lg text-slate-950 dark:text-white">{String(v)}</b><div className="capitalize text-slate-500">{k.replace(/[A-Z]/g,' $&')}</div></div>)}</div></Panel>; }
