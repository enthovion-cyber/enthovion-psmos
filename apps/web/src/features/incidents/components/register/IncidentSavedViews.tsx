import { Panel } from './Panel';
export function IncidentSavedViews({ views, onApply, onSave, canSave }: { views:any[]; onApply:(f:any)=>void; onSave:()=>void; canSave?:boolean }) { return <Panel title="Saved Views" subtitle="Private/team/site/company views respecting access scope." action={<button disabled={!canSave} title={canSave?'Save current filters':'Missing incidents.saved_views.create'} onClick={onSave} className="lopa-button-secondary disabled:opacity-50">Save view</button>}><div className="flex flex-wrap gap-2">{(views??[]).map(v=><button key={v.id} onClick={()=>onApply(v.filters_json??{})} className="rounded-lg border border-slate-200 px-3 py-2 text-xs dark:border-cyan-300/10"><b>{v.view_name}</b><span className="ml-2 text-slate-500">{v.visibility}</span></button>)}</div></Panel>; }
