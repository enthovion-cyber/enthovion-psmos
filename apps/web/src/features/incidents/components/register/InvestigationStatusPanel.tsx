import { Panel } from './Panel';
export function InvestigationStatusPanel({ data, onFilter }: { data:any; onFilter:(f:any)=>void }) { const s=data?.summary??{}; return <Panel title="Investigation Status" subtitle="Workflow health, due dates, RCA and review queue."><div className="grid grid-cols-2 gap-2 text-xs md:grid-cols-4">{Object.entries(s).map(([k,v])=><div key={k} className="rounded border border-slate-200 p-2 dark:border-cyan-300/10"><b className="text-slate-950 dark:text-white">{String(v ?? '-')}</b><div className="capitalize text-slate-500">{k.replace(/[A-Z]/g,' $&')}</div></div>)}</div><div className="mt-3 flex flex-wrap gap-2">{(data?.quickFilters??[]).map((x:string)=><button key={x} onClick={()=>onFilter({ quick:x })} className="rounded border border-slate-200 px-2 py-1 text-xs dark:border-cyan-300/10">{x}</button>)}</div></Panel>; }
