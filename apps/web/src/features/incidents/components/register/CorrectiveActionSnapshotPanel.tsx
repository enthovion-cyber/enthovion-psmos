import { Panel, Empty } from './Panel';
export function CorrectiveActionSnapshotPanel({ data }: { data:any }) { const s=data?.summary??{}; const items=data?.items??[]; return <Panel title="Corrective Action / CAPA Snapshot" subtitle="Uses Universal Action Engine records where linked."><div className="grid grid-cols-2 gap-2 text-xs md:grid-cols-5">{Object.entries(s).map(([k,v])=><div key={k}><b>{String(v)}</b><div className="capitalize text-slate-500">{k.replace(/[A-Z]/g,' $&')}</div></div>)}</div><div className="mt-3 space-y-2">{items.length?items.map((a:any)=><div key={`${a.actionNumber}-${a.title}`} className="rounded border border-slate-200 p-2 text-xs dark:border-cyan-300/10"><b>{a.actionNumber ?? 'Action'}</b> {a.title}<div className="text-slate-500">{a.owner ?? '-'} · {a.dueDate ?? '-'} · {a.status}</div></div>):<Empty text="No linked open Universal Actions found."/>}</div></Panel>; }
