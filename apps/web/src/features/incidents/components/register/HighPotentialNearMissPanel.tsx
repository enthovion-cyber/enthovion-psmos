import { Panel, Empty } from './Panel';
import { ActualSeverityBadge } from '../shared/ActualSeverityBadge';
import { PotentialSeverityBadge } from '../shared/PotentialSeverityBadge';
export function HighPotentialNearMissPanel({ data }: { data:any }) { const s=data?.summary??{}; const rows=data?.items??[]; return <Panel title="Potential Severity / High-Potential Near Miss" subtitle="Actual severity and potential severity are displayed separately."><div className="grid grid-cols-2 gap-2 text-xs md:grid-cols-3">{Object.entries(s).map(([k,v])=><div key={k}><b>{String(v)}</b><span className="ml-1 capitalize text-slate-500">{k.replace(/[A-Z]/g,' $&')}</span></div>)}</div><div className="mt-3 space-y-2">{rows.length?rows.map((r:any)=><div key={r.id} className="flex items-center justify-between rounded border border-slate-200 p-2 text-xs dark:border-cyan-300/10"><span>{r.incident_number} · {r.title}</span><span className="flex gap-1"><ActualSeverityBadge value={r.actual_severity}/><PotentialSeverityBadge value={r.potential_severity}/></span></div>):<Empty/>}</div></Panel>; }
