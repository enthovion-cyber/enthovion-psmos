import { Badge } from '../shared/IncidentStatusBadge';
import { TabPanel } from '../shared/IncidentTabPrimitives';
export function TrainingProcedureDocumentUpdatePanel({ data }: { data: any }) { return <TabPanel title="Training / Procedure / Document Update"><div className="grid gap-2 text-xs"><div className="flex justify-between"><span>Training required</span><Badge value={!!data?.trainingRequired} /></div><div className="flex justify-between"><span>Procedure/document update required</span><Badge value={!!data?.procedureUpdateRequired} /></div><div className="flex justify-between"><span>Linked actions</span><b>{data?.linkedActions?.length ?? 0}</b></div><p className="text-slate-500">Document Control and Universal Action Engine links are represented by backend-linked actions where available.</p></div></TabPanel>; }
