import { buttonPrimary, buttonSecondary, Field, SelectField, TextArea } from '../shared/IncidentTabPrimitives';
const TYPES = ['Process safety lesson','Occupational safety lesson','Environmental lesson','Equipment/MI lesson','Chemical/SDS lesson','Procedure/PTW lesson','MOC/PSSR lesson','HAZOP/LOPA lesson','Emergency response lesson','Training/competency lesson','Management-system lesson','Contractor management lesson','Other'];
export function AddEditLessonDrawer({ open, form, set, saving, onClose, onSave }: any) {
  if (!open) return null;
  return <div className="fixed inset-0 z-50 bg-slate-950/40"><aside className="ml-auto h-full w-full max-w-2xl overflow-y-auto bg-white p-4 shadow-xl dark:bg-[#071525]"><div className="mb-4 flex items-center justify-between"><h3 className="text-lg font-black">{form.id ? 'Edit Lesson' : 'Add Lesson'}</h3><button className={buttonSecondary} onClick={onClose}>Close</button></div><div className="grid gap-3 md:grid-cols-2"><Field label="Lesson title" value={form.title} onChange={(v) => set('title', v)} /><SelectField label="Lesson type" value={form.lessonType} options={TYPES} onChange={(v) => set('lessonType', v)} /><Field label="Source type" value={form.sourceType} onChange={(v) => set('sourceType', v)} /><Field label="Owner ID" value={form.ownerId} onChange={(v) => set('ownerId', v)} /><Field label="Applicability scope" value={form.applicabilityScope} onChange={(v) => set('applicabilityScope', v)} /><Field label="Due date" type="date" value={form.dueDate} onChange={(v) => set('dueDate', v)} /><label className="flex items-center gap-2 rounded-lg border border-slate-200 p-2 text-xs dark:border-cyan-300/10"><input type="checkbox" checked={!!form.trainingRequired} onChange={(e) => set('trainingRequired', e.target.checked)} /> Training required</label><label className="flex items-center gap-2 rounded-lg border border-slate-200 p-2 text-xs dark:border-cyan-300/10"><input type="checkbox" checked={!!form.procedureUpdateRequired} onChange={(e) => set('procedureUpdateRequired', e.target.checked)} /> Procedure/document update required</label><label className="flex items-center gap-2 rounded-lg border border-slate-200 p-2 text-xs dark:border-cyan-300/10"><input type="checkbox" checked={!!form.communicationRequired} onChange={(e) => set('communicationRequired', e.target.checked)} /> Communication required</label><TextArea label="Lesson statement" value={form.lessonStatement} onChange={(v) => set('lessonStatement', v)} className="md:col-span-2" /><TextArea label="Target audience JSON/notes" value={form.targetAudienceText} onChange={(v) => set('targetAudienceText', v)} className="md:col-span-2" /><TextArea label="Recommended action / notes" value={form.notes} onChange={(v) => set('notes', v)} className="md:col-span-2" /><TextArea label="Change reason" value={form.reason} onChange={(v) => set('reason', v)} className="md:col-span-2" /></div><div className="mt-4 flex justify-end gap-2"><button className={buttonSecondary} onClick={onClose}>Cancel</button><button className={buttonPrimary} disabled={saving} onClick={onSave}>Save Lesson</button></div></aside></div>;
}
