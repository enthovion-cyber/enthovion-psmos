import { buttonSecondary, InfoRows, TabPanel } from '../shared/IncidentTabPrimitives';
import { LessonStatusBadge } from '../shared/LessonStatusBadge';
export function LessonsReviewPanel({ review, onApprove, onReject, onReopen, onCreateAction }: any) { return <TabPanel title="Lessons Review"><div className="mb-3 flex items-center justify-between"><span className="text-xs font-bold">Review status</span><LessonStatusBadge value={review?.status} /></div><InfoRows rows={[['Review required', review?.reviewRequired ? 'Yes' : 'No'], ['Reviewer', review?.latest?.reviewer_id], ['Due date', review?.latest?.due_date], ['Comments', review?.latest?.comments], ['Rework required', review?.latest?.rework_required ? 'Yes' : 'No']]} /><div className="mt-3 flex flex-wrap gap-2"><button className={buttonSecondary} onClick={onApprove}>Approve</button><button className={buttonSecondary} onClick={onReject}>Reject</button><button className={buttonSecondary} onClick={onReopen}>Reopen</button><button className={buttonSecondary} onClick={onCreateAction}>Create action</button></div></TabPanel>; }
