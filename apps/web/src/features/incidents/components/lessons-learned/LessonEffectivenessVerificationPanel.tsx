import { LessonStatusBadge } from '../shared/LessonStatusBadge';
import { formatDate, TabPanel } from '../shared/IncidentTabPrimitives';
export function LessonEffectivenessVerificationPanel({ data }: { data: any }) { const rows = data?.rows ?? []; return <TabPanel title="Effectiveness / Verification"><div className="grid gap-2">{rows.map((row: any) => <div key={row.lessonId} className="rounded-lg border border-slate-200 p-3 text-xs dark:border-cyan-300/10"><div className="flex justify-between"><b>{row.lessonNumber} · {row.title}</b><LessonStatusBadge value={row.verificationStatus} /></div><div className="mt-1 text-slate-500">Owner: {row.ownerId ?? '-'} · Due: {formatDate(row.dueDate)}</div></div>)}{!rows.length ? <p className="text-xs text-slate-500">No verification rows returned.</p> : null}</div></TabPanel>; }
