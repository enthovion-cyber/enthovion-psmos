import { AcknowledgementStatusBadge } from '../shared/AcknowledgementStatusBadge';
import { formatDate, TabPanel } from '../shared/IncidentTabPrimitives';
export function LessonAcknowledgementPanel({ rows }: { rows: any[] }) { return <TabPanel title="Lesson Acknowledgement"><div className="grid gap-2">{(rows ?? []).map((row) => <div key={row.id} className="rounded-lg border border-slate-200 p-3 text-xs dark:border-cyan-300/10"><div className="flex justify-between"><b>{row.recipient_user_id ?? 'Recipient'}</b><AcknowledgementStatusBadge value={row.acknowledgement_status} /></div><div className="mt-1 text-slate-500">Acknowledged: {formatDate(row.acknowledged_at)} · Reminder: {formatDate(row.reminder_sent_at)}</div></div>)}{!(rows ?? []).length ? <p className="text-xs text-slate-500">No acknowledgement recipients returned.</p> : null}</div></TabPanel>; }
