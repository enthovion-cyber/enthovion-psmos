import { DistributionStatusBadge } from '../shared/DistributionStatusBadge';
import { formatDate, TabPanel } from '../shared/IncidentTabPrimitives';
export function CommunicationDistributionPanel({ data }: { data: any }) { const rows = data?.rows ?? []; return <TabPanel title="Communication & Distribution"><div className="grid gap-2">{rows.map((row: any) => <div key={row.id} className="rounded-lg border border-slate-200 p-3 text-xs dark:border-cyan-300/10"><div className="flex justify-between"><b>{row.channel ?? 'Notification Center'}</b><DistributionStatusBadge value={row.distribution_status} /></div><p className="mt-1 text-slate-500">{row.message_snapshot}</p><div className="mt-1 text-slate-400">Distributed by {row.distributed_by ?? '-'} · {formatDate(row.distributed_at)}</div></div>)}{!rows.length ? <p className="text-xs text-slate-500">No lesson distributions have been sent.</p> : null}</div></TabPanel>; }
