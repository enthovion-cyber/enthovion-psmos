import { Badge } from '../shared/IncidentStatusBadge';
import { TabPanel } from '../shared/IncidentTabPrimitives';
export function LessonSourceMappingPanel({ data }: { data: any }) { const rows = data?.rows ?? []; return <TabPanel title="Lesson Source Mapping"><div className="grid gap-2">{rows.map((row: any) => <div key={`${row.sourceType}-${row.sourceId}`} className="rounded-lg border border-slate-200 p-3 text-xs dark:border-cyan-300/10"><div className="flex justify-between gap-2"><b>{row.sourceItem}</b><Badge value={row.coverageStatus} /></div><div className="mt-1 text-slate-500">{row.sourceType} · {row.action}</div></div>)}{!rows.length ? <p className="text-xs text-slate-500">No RCA, barrier, CAPA, or evidence source mapping rows were returned.</p> : null}</div></TabPanel>; }
