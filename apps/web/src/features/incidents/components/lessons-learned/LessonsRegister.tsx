import { AcknowledgementStatusBadge } from '../shared/AcknowledgementStatusBadge';
import { DistributionStatusBadge } from '../shared/DistributionStatusBadge';
import { buttonSecondary, formatDate, TabPanel } from '../shared/IncidentTabPrimitives';
import { LessonStatusBadge } from '../shared/LessonStatusBadge';
import { LessonTypeBadge } from '../shared/LessonTypeBadge';
export function LessonsRegister({ rows, onView, onEdit, onLinkSource, onCreateAction, onDistribute, onVerify, onArchive }: any) {
  return <TabPanel title="Lessons Learned Register"><div className="overflow-x-auto"><table className="min-w-[1250px] w-full text-left text-xs"><thead className="text-slate-500"><tr>{['Lesson #','Title','Type','Source','Applicability','Training','Procedure','Owner','Due','Review','Distribution','Acknowledgement','Verification','Actions'].map((h) => <th key={h} className="border-b border-slate-200 px-2 py-2 dark:border-cyan-300/10">{h}</th>)}</tr></thead><tbody>{(rows ?? []).map((row: any) => <tr key={row.id} className="align-top"><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10">{row.lesson_number}</td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10"><b>{row.title}</b><div className="text-slate-500">{row.lesson_statement}</div></td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10"><LessonTypeBadge value={row.lesson_type} /></td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10">{row.source_type}</td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10">{row.applicability_scope}</td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10">{row.training_required ? 'Yes' : 'No'}</td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10">{row.procedure_update_required ? 'Yes' : 'No'}</td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10">{row.owner_id ?? '-'}</td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10">{formatDate(row.due_date)}</td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10"><LessonStatusBadge value={row.review_status} /></td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10"><DistributionStatusBadge value={row.distribution_status} /></td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10"><AcknowledgementStatusBadge value={row.acknowledgement_status} /></td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10"><LessonStatusBadge value={row.verification_status} /></td><td className="border-b border-slate-100 px-2 py-2 dark:border-cyan-300/10"><div className="flex flex-wrap gap-1"><button className={buttonSecondary} onClick={() => onView(row)}>View</button><button className={buttonSecondary} onClick={() => onEdit(row)}>Edit</button><button className={buttonSecondary} onClick={() => onLinkSource(row)}>Link source</button><button className={buttonSecondary} onClick={() => onCreateAction(row)}>Create action</button><button className={buttonSecondary} onClick={() => onDistribute(row)}>Distribute</button><button className={buttonSecondary} onClick={() => onVerify(row)}>Verify</button><button className={buttonSecondary} onClick={() => onArchive(row)}>Archive</button></div></td></tr>)}</tbody></table>{!(rows ?? []).length ? <p className="py-3 text-xs text-slate-500">No lessons learned have been created.</p> : null}</div></TabPanel>;
}
