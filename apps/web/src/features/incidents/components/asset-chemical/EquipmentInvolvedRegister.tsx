import { EquipmentStatusBadge } from '../shared/EquipmentStatusBadge';
import { TabPanel } from '../shared/IncidentTabPrimitives';
export function EquipmentInvolvedRegister({ rows, onEdit, onDelete, canDelete }: any) { return <TabPanel title="Equipment Involved Register">{!rows?.length ? <p className="text-xs text-slate-500">No equipment records have been added.</p> : <div className="overflow-auto"><table className="w-full min-w-[900px] text-left text-xs"><thead className="text-slate-500"><tr><th className="p-2">Tag</th><th>Name</th><th>Type</th><th>Status</th><th>Condition</th><th>Safeguard failed</th><th>MI concern</th><th>Actions</th></tr></thead><tbody>{rows.map((row: any) => <tr key={row.id} className="border-t border-slate-200 dark:border-cyan-300/10"><td className="p-2 font-bold">{row.equipment_tag_snapshot ?? '-'}</td><td>{row.equipment_name_snapshot ?? '-'}</td><td>{row.equipment_type_snapshot ?? '-'}</td><td><EquipmentStatusBadge value={row.equipment_status} /></td><td>{row.condition_at_event ?? '-'}</td><td>{row.safeguard_failed ? 'Yes' : 'No'}</td><td>{row.maintenance_overdue_suspected ? 'Yes' : 'No'}</td><td><button onClick={() => onEdit(row)} className="text-blue-600">Edit</button>{canDelete ? <button onClick={() => onDelete(row.id)} className="ml-3 text-red-600">Delete</button> : null}</td></tr>)}</tbody></table></div>}</TabPanel>; }
