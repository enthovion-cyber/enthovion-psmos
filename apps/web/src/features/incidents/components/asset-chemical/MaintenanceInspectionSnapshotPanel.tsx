import { Panel } from './AssetPanelPrimitives';
export function MaintenanceInspectionSnapshotPanel({ data }: any) { return <Panel title="Maintenance / Inspection / MI Snapshot Panel" data={data} empty="No maintenance or inspection data was returned." />; }
