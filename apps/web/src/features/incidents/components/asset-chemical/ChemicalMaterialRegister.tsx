import { ChemicalHazardBadge } from '../shared/ChemicalHazardBadge';
import { TabPanel } from '../shared/IncidentTabPrimitives';
export function ChemicalMaterialRegister({ rows, onEdit, onDelete, canDelete }: any) { return <TabPanel title="Chemical / Material Register">{!rows?.length ? <p className="text-xs text-slate-500">No chemical/material records have been added.</p> : <div className="overflow-auto"><table className="w-full min-w-[900px] text-left text-xs"><thead className="text-slate-500"><tr><th className="p-2">Material</th><th>CAS</th><th>SDS</th><th>Hazard</th><th>Quantity</th><th>Released</th><th>Unit</th><th>Actions</th></tr></thead><tbody>{rows.map((row: any) => <tr key={row.id} className="border-t border-slate-200 dark:border-cyan-300/10"><td className="p-2 font-bold">{row.chemical_name_snapshot ?? '-'}</td><td>{row.cas_number_snapshot ?? '-'}</td><td>{row.sds_id ?? row.sds_link ?? 'Missing'}</td><td><ChemicalHazardBadge value={row.hazard_classification} /></td><td>{row.estimated_quantity_involved ?? '-'}</td><td>{row.released_quantity ?? '-'}</td><td>{row.release_unit ?? '-'}</td><td><button onClick={() => onEdit(row)} className="text-blue-600">Edit</button>{canDelete ? <button onClick={() => onDelete(row.id)} className="ml-3 text-red-600">Delete</button> : null}</td></tr>)}</tbody></table></div>}</TabPanel>; }
