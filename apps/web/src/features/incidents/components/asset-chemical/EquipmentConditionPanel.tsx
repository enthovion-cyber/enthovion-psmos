import { Panel } from './AssetPanelPrimitives';
export function EquipmentConditionPanel({ data }: any) { return <Panel title="Equipment Condition / Operating Status Panel" data={data} empty="No equipment condition records were returned." />; }
