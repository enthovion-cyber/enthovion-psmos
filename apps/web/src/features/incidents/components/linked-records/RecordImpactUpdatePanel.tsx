import { LinkedRecordSimplePanel } from './LinkedRecordsPrimitives';
export function RecordImpactUpdatePanel({ data }: { data: any }) { return <LinkedRecordSimplePanel title="Record Impact / Update Required Panel" data={data} empty="No record impacts require updates." />; }
