'use client';
import { StateBanner } from './FormBits';
export function PotentialSeverityMatrix({ matrix, value, likelihood, score, onSeverity, onLikelihood }: { matrix?: any; value?: string; likelihood?: string; score?: any; onSeverity: (v: string) => void; onLikelihood: (v: string) => void }) {
  const severities = matrix?.severities ?? [];
  const likelihoods = matrix?.likelihoods ?? [];
  const missingConfig = matrix?.configured === false || score?.riskMatrixConfigMissing;
  return <div className="space-y-3">{missingConfig ? <StateBanner tone="warn"><b>Missing risk matrix config.</b> Continue is allowed. Backend score is <b>Not Determined</b>, priority is <b>Pending Review</b>, and severity review is required.<div className="mt-2 flex flex-wrap gap-2 text-xs"><span className="rounded-full border border-amber-400/40 px-2 py-1">Request Severity Review</span><span className="rounded-full border border-amber-400/40 px-2 py-1">Configure company/site incident risk matrix</span></div></StateBanner> : null}<div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{severities.map((s: string) => <button key={s} type="button" onClick={() => onSeverity(s)} className={`rounded-lg border p-3 text-left text-sm font-bold ${value === s ? 'border-red-500 bg-red-500/10 text-red-700 dark:text-red-100' : 'border-slate-200 dark:border-cyan-300/10'}`}>{s}</button>)}</div><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-5">{likelihoods.map((l: string) => <button key={l} type="button" onClick={() => onLikelihood(l)} className={`rounded-lg border p-2 text-xs font-semibold ${likelihood === l ? 'border-amber-500 bg-amber-500/10 text-amber-700 dark:text-amber-100' : 'border-slate-200 dark:border-cyan-300/10'}`}>{l}</button>)}</div><div className="rounded-lg border border-slate-200 p-3 text-sm dark:border-cyan-300/10">Backend score: <b>{score?.potentialRiskScore ?? 'Not Determined'}</b> - Priority: <b>{score?.investigationPriority ?? (missingConfig ? 'Pending Review' : 'Pending')}</b><div className="text-xs text-slate-500 dark:text-slate-400">{score?.reason ?? matrix?.missingReason}</div></div></div>;
}
