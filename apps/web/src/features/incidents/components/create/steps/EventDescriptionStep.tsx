'use client';
import { Card, TextArea, Toggle } from '../FormBits';
export function EventDescriptionStep({ values, update }: any) {
  return <Card title="3. Event Description"><div className="grid gap-3"><TextArea label="Detailed description: What happened?" required value={values.detailedDescription} onChange={(v) => update({ detailedDescription: v })} placeholder="Describe facts, not blame." /><TextArea label="What was being done at the time?" value={values.activityAtTime} onChange={(v) => update({ activityAtTime: v })} /><TextArea label="Abnormal condition observed" value={values.abnormalCondition} onChange={(v) => update({ abnormalCondition: v })} /><TextArea label="Immediate consequence" value={values.immediateConsequence} onChange={(v) => update({ immediateConsequence: v })} /><TextArea label="Potential consequence" value={values.potentialConsequence} onChange={(v) => update({ potentialConsequence: v })} /><TextArea label="Initial suspected cause" value={values.suspectedInitialCause} onChange={(v) => update({ suspectedInitialCause: v })} /></div><div className="mt-3 grid gap-2 md:grid-cols-3"><Toggle label="Witnesses known" checked={!!values.witnessesKnown} onChange={(v) => update({ witnessesKnown: v })} /><Toggle label="Emergency response activated" checked={!!values.emergencyResponseActivated} onChange={(v) => update({ emergencyResponseActivated: v })} /><Toggle label="Operation stopped" checked={!!values.operationStopped} onChange={(v) => update({ operationStopped: v })} /><Toggle label="Equipment isolated" checked={!!values.equipmentIsolated} onChange={(v) => update({ equipmentIsolated: v })} /><Toggle label="Area barricaded" checked={!!values.areaBarricaded} onChange={(v) => update({ areaBarricaded: v })} /><Toggle label="Release/fire/explosion occurred" checked={!!values.fireExplosionOccurred} onChange={(v) => update({ fireExplosionOccurred: v })} /><Toggle label="Environmental impact suspected" checked={!!values.environmentalImpact} onChange={(v) => update({ environmentalImpact: v })} /><Toggle label="Community impact suspected" checked={!!values.communityImpact} onChange={(v) => update({ communityImpact: v })} /></div></Card>;
}
