'use client';
export function IncidentWizardFooter({ step, busy, canSaveDraft, canSubmit, onBack, onNext, onSaveDraft, onSubmit, disabledReason }: { step: number; busy?: boolean; canSaveDraft?: boolean | undefined; canSubmit?: boolean | undefined; onBack: () => void; onNext: () => void; onSaveDraft: () => void; onSubmit: () => void; disabledReason?: string }) {
  return <footer className="sticky bottom-3 z-10 rounded-xl border border-slate-200 bg-white/95 p-3 shadow-lg backdrop-blur dark:border-cyan-300/10 dark:bg-[#071525]/95"><div className="flex flex-wrap items-center justify-between gap-2"><button type="button" className="lopa-button-secondary" onClick={onBack} disabled={step === 1 || busy}>Back</button><div className="flex flex-wrap gap-2"><button type="button" className="lopa-button-secondary" onClick={onSaveDraft} disabled={!canSaveDraft || busy} title={!canSaveDraft ? 'Missing incidents.draft.create/edit permission' : undefined}>Save Draft</button>{step < 12 ? <button type="button" className="lopa-button-primary" onClick={onNext} disabled={busy}>Next</button> : <button type="button" className="lopa-button-primary" onClick={onSubmit} disabled={!canSubmit || busy} title={!canSubmit ? disabledReason ?? 'Missing incidents.submit permission' : undefined}>{busy ? 'Submitting...' : 'Submit Incident'}</button>}</div></div></footer>;
}
