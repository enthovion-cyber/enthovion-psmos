'use client';
export function IncidentWizardHeader({ dirty, context }: { dirty: boolean; context?: any }) {
  return <header className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm dark:border-cyan-300/10 dark:bg-[#071525]"><div className="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div><p className="text-xs font-semibold uppercase tracking-wide text-blue-600 dark:text-cyan-300">Incident / Near Miss / Accident Investigation</p><h1 className="text-2xl font-bold text-slate-950 dark:text-white">Create Incident Wizard</h1><p className="text-sm text-slate-500 dark:text-slate-400">Report incidents with structured severity, PSM/PSE classification, evidence, and backend-generated follow-up.</p></div><div className="flex flex-wrap gap-2 text-xs"><span className="rounded-lg border border-slate-200 px-3 py-2 dark:border-cyan-300/10">{context?.riskMatrix?.configured ? 'Risk matrix configured' : 'Risk matrix missing'}</span><span className="rounded-lg border border-slate-200 px-3 py-2 dark:border-cyan-300/10">{context?.pseThresholdConfig?.configured ? 'PSE thresholds configured' : 'PSE thresholds missing'}</span>{dirty ? <span className="rounded-lg border border-amber-300 bg-amber-500/10 px-3 py-2 text-amber-700 dark:text-amber-100">Unsaved changes</span> : null}</div></div></header>;
}
