import { CapaSimplePanel } from './CapaPanelPrimitives';
export function OwnerDueDateAssignmentPanel({ data }: { data: any }) { return <CapaSimplePanel title="Owner / Due Date / Assignment Panel" data={data} empty="No owner or due date assignments are available." />; }
