import { CapaSimplePanel } from './CapaPanelPrimitives';
export function EvidenceOfCompletionPanel({ data }: { data: any }) { return <CapaSimplePanel title="Evidence of Completion Panel" data={data} empty="No completion evidence is linked." />; }
