import { api } from '@/services/api';
import type { IncidentDetailData, IncidentOverviewData } from '../types/incident-detail.types';

const unwrap = <T,>(response: { data: { data: T } }) => response.data.data;

export const incidentDetailService = {
  detail: (id: string) => api.get(`/incidents/${id}`).then(unwrap<IncidentDetailData>),
  header: (id: string) => api.get(`/incidents/${id}/header`).then(unwrap<any>),
  overview: (id: string) => api.get(`/incidents/${id}/overview`).then(unwrap<IncidentOverviewData>),
  eventDetails: (id: string) => api.get(`/incidents/${id}/event-details`).then(unwrap<any>),
  updateEventDetails: (id: string, values: any) => api.patch(`/incidents/${id}/event-details`, values).then(unwrap<any>),
  requestClassificationReview: (id: string, values: any) => api.post(`/incidents/${id}/event-details/request-classification-review`, values).then(unwrap<any>),
  approveClassification: (id: string, values: any) => api.post(`/incidents/${id}/event-details/approve-classification`, values).then(unwrap<any>),
  rejectClassification: (id: string, values: any) => api.post(`/incidents/${id}/event-details/reject-classification`, values).then(unwrap<any>),
  severityRisk: (id: string) => api.get(`/incidents/${id}/severity-risk`).then(unwrap<any>),
  updateSeverityRisk: (id: string, values: any) => api.patch(`/incidents/${id}/severity-risk`, values).then(unwrap<any>),
  recalculateSeverityRisk: (id: string) => api.post(`/incidents/${id}/severity-risk/recalculate`, {}).then(unwrap<any>),
  requestSeverityReview: (id: string, values: any) => api.post(`/incidents/${id}/severity-risk/request-review`, values).then(unwrap<any>),
  approveSeverityReview: (id: string, values: any) => api.post(`/incidents/${id}/severity-risk/approve-review`, values).then(unwrap<any>),
  rejectSeverityReview: (id: string, values: any) => api.post(`/incidents/${id}/severity-risk/reject-review`, values).then(unwrap<any>),
  peopleInjury: (id: string) => api.get(`/incidents/${id}/people-injury`).then(unwrap<any>),
  createPerson: (id: string, values: any) => api.post(`/incidents/${id}/people`, values).then(unwrap<any>),
  updatePerson: (id: string, personId: string, values: any) => api.patch(`/incidents/${id}/people/${personId}`, values).then(unwrap<any>),
  deletePerson: (id: string, personId: string) => api.delete(`/incidents/${id}/people/${personId}`).then(unwrap<any>),
  requestPeopleReview: (id: string, values: any) => api.post(`/incidents/${id}/people/review-request`, values).then(unwrap<any>),
  approvePeopleReview: (id: string, values: any) => api.post(`/incidents/${id}/people/review-approve`, values).then(unwrap<any>),
  rejectPeopleReview: (id: string, values: any) => api.post(`/incidents/${id}/people/review-reject`, values).then(unwrap<any>),
  assetChemical: (id: string) => api.get(`/incidents/${id}/asset-chemical`).then(unwrap<any>),
  createEquipment: (id: string, values: any) => api.post(`/incidents/${id}/equipment`, values).then(unwrap<any>),
  updateEquipment: (id: string, rowId: string, values: any) => api.patch(`/incidents/${id}/equipment/${rowId}`, values).then(unwrap<any>),
  deleteEquipment: (id: string, rowId: string) => api.delete(`/incidents/${id}/equipment/${rowId}`).then(unwrap<any>),
  createChemical: (id: string, values: any) => api.post(`/incidents/${id}/chemicals`, values).then(unwrap<any>),
  updateChemical: (id: string, rowId: string, values: any) => api.patch(`/incidents/${id}/chemicals/${rowId}`, values).then(unwrap<any>),
  deleteChemical: (id: string, rowId: string) => api.delete(`/incidents/${id}/chemicals/${rowId}`).then(unwrap<any>),
  requestAssetReview: (id: string, values: any) => api.post(`/incidents/${id}/asset-chemical/review-request`, values).then(unwrap<any>),
  approveAssetReview: (id: string, values: any) => api.post(`/incidents/${id}/asset-chemical/review-approve`, values).then(unwrap<any>),
  rejectAssetReview: (id: string, values: any) => api.post(`/incidents/${id}/asset-chemical/review-reject`, values).then(unwrap<any>),
  timeline: (id: string) => api.get(`/incidents/${id}/timeline`).then(unwrap<any>),
  createTimelineEvent: (id: string, values: any) => api.post(`/incidents/${id}/timeline/events`, values).then(unwrap<any>),
  updateTimelineEvent: (id: string, eventId: string, values: any) => api.patch(`/incidents/${id}/timeline/events/${eventId}`, values).then(unwrap<any>),
  deleteTimelineEvent: (id: string, eventId: string) => api.delete(`/incidents/${id}/timeline/events/${eventId}`).then(unwrap<any>),
  requestTimelineReview: (id: string, values: any) => api.post(`/incidents/${id}/timeline/review-request`, values).then(unwrap<any>),
  approveTimelineReview: (id: string, values: any) => api.post(`/incidents/${id}/timeline/review-approve`, values).then(unwrap<any>),
  rejectTimelineReview: (id: string, values: any) => api.post(`/incidents/${id}/timeline/review-reject`, values).then(unwrap<any>),
  evidenceAttachments: (id: string) => api.get(`/incidents/${id}/evidence-attachments`).then(unwrap<any>),
  createEvidence: (id: string, values: any) => api.post(`/incidents/${id}/evidence`, values).then(unwrap<any>),
  updateEvidence: (id: string, evidenceId: string, values: any) => api.patch(`/incidents/${id}/evidence/${evidenceId}`, values).then(unwrap<any>),
  deleteIncidentEvidence: (id: string, evidenceId: string) => api.delete(`/incidents/${id}/evidence/${evidenceId}`).then(unwrap<any>),
  archiveEvidence: (id: string, evidenceId: string, values: any) => api.post(`/incidents/${id}/evidence/${evidenceId}/archive`, values).then(unwrap<any>),
  createEvidenceVersion: (id: string, evidenceId: string, values: any) => api.post(`/incidents/${id}/evidence/${evidenceId}/version`, values).then(unwrap<any>),
  createEvidenceMapping: (id: string, evidenceId: string, values: any) => api.post(`/incidents/${id}/evidence/${evidenceId}/mappings`, values).then(unwrap<any>),
  createEvidenceCustody: (id: string, evidenceId: string, values: any) => api.post(`/incidents/${id}/evidence/${evidenceId}/custody`, values).then(unwrap<any>),
  previewEvidence: (id: string, evidenceId: string) => api.get(`/incidents/${id}/evidence/${evidenceId}/preview`).then(unwrap<any>),
  downloadEvidence: (id: string, evidenceId: string) => api.get(`/incidents/${id}/evidence/${evidenceId}/download`).then(unwrap<any>),
  requestEvidenceReview: (id: string, values: any) => api.post(`/incidents/${id}/evidence/review-request`, values).then(unwrap<any>),
  approveEvidenceReview: (id: string, values: any) => api.post(`/incidents/${id}/evidence/review-approve`, values).then(unwrap<any>),
  rejectEvidenceReview: (id: string, values: any) => api.post(`/incidents/${id}/evidence/review-reject`, values).then(unwrap<any>),
  exportEvidenceIndex: (id: string) => api.get(`/incidents/${id}/evidence/export-index`).then(unwrap<any>),
  immediateActions: (id: string) => api.get(`/incidents/${id}/immediate-actions`).then(unwrap<any>),
  updateImmediateSiteSafety: (id: string, values: any) => api.patch(`/incidents/${id}/immediate-actions/site-safety`, values).then(unwrap<any>),
  updateImmediateRestartControl: (id: string, values: any) => api.patch(`/incidents/${id}/immediate-actions/restart-control`, values).then(unwrap<any>),
  createImmediateAction: (id: string, values: any) => api.post(`/incidents/${id}/immediate-actions`, values).then(unwrap<any>),
  updateImmediateAction: (id: string, actionId: string, values: any) => api.patch(`/incidents/${id}/immediate-actions/${actionId}`, values).then(unwrap<any>),
  deleteImmediateAction: (id: string, actionId: string) => api.delete(`/incidents/${id}/immediate-actions/${actionId}`).then(unwrap<any>),
  convertImmediateActionToCapa: (id: string, actionId: string, values: any) => api.post(`/incidents/${id}/immediate-actions/${actionId}/convert-capa`, values).then(unwrap<any>),
  completeImmediateAction: (id: string, actionId: string, values: any) => api.post(`/incidents/${id}/immediate-actions/${actionId}/complete`, values).then(unwrap<any>),
  verifyImmediateAction: (id: string, actionId: string, values: any) => api.post(`/incidents/${id}/immediate-actions/${actionId}/verify`, values).then(unwrap<any>),
  rejectImmediateActionVerification: (id: string, actionId: string, values: any) => api.post(`/incidents/${id}/immediate-actions/${actionId}/reject-verification`, values).then(unwrap<any>),
  linkImmediateActionEvidence: (id: string, actionId: string, values: any) => api.post(`/incidents/${id}/immediate-actions/${actionId}/link-evidence`, values).then(unwrap<any>),
  linkImmediateActionCapa: (id: string, actionId: string, values: any) => api.post(`/incidents/${id}/immediate-actions/${actionId}/link-capa`, values).then(unwrap<any>),
  cancelImmediateAction: (id: string, actionId: string, values: any) => api.post(`/incidents/${id}/immediate-actions/${actionId}/cancel`, values).then(unwrap<any>),
  createImmediateActionFollowup: (id: string, actionId: string, values: any) => api.post(`/incidents/${id}/immediate-actions/${actionId}/create-followup-action`, values).then(unwrap<any>),
  requestImmediateActionsReview: (id: string, values: any) => api.post(`/incidents/${id}/immediate-actions/review-request`, values).then(unwrap<any>),
  approveImmediateActionsReview: (id: string, values: any) => api.post(`/incidents/${id}/immediate-actions/review-approve`, values).then(unwrap<any>),
  rejectImmediateActionsReview: (id: string, values: any) => api.post(`/incidents/${id}/immediate-actions/review-reject`, values).then(unwrap<any>),
  investigationTeam: (id: string) => api.get(`/incidents/${id}/investigation-team`).then(unwrap<any>),
  createTeamMember: (id: string, values: any) => api.post(`/incidents/${id}/team/members`, values).then(unwrap<any>),
  updateTeamMember: (id: string, memberId: string, values: any) => api.patch(`/incidents/${id}/investigation-team/members/${memberId}`, values).then(unwrap<any>),
  deleteTeamMember: (id: string, memberId: string, values?: any) => api.delete(`/incidents/${id}/team/members/${memberId}`, { data: values ?? {} }).then(unwrap<any>),
  updateTeamOwnerLead: (id: string, values: any) => api.patch(`/incidents/${id}/team/owner-lead`, values).then(unwrap<any>),
  acceptTeamMember: (id: string, memberId: string, values: any) => api.post(`/incidents/${id}/team/members/${memberId}/accept`, values).then(unwrap<any>),
  declineTeamMember: (id: string, memberId: string, values: any) => api.post(`/incidents/${id}/team/members/${memberId}/decline`, values).then(unwrap<any>),
  replaceTeamMember: (id: string, memberId: string, values: any) => api.post(`/incidents/${id}/team/members/${memberId}/replace`, values).then(unwrap<any>),
  generateRequiredTeamRoles: (id: string) => api.post(`/incidents/${id}/investigation-team/generate-required-roles`, {}).then(unwrap<any>),
  sendTeamNotification: (id: string, values: any) => api.post(`/incidents/${id}/investigation-team/send-notification`, values).then(unwrap<any>),
  sendTeamReminders: (id: string, values: any) => api.post(`/incidents/${id}/team/send-reminders`, values).then(unwrap<any>),
  updateTeamRaci: (id: string, values: any) => api.patch(`/incidents/${id}/team/raci`, values).then(unwrap<any>),
  updateTeamChecks: (id: string, values: any) => api.patch(`/incidents/${id}/team/competency-independence`, values).then(unwrap<any>),
  searchTeamUsers: (id: string, values: any) => api.post(`/incidents/${id}/team/user-search`, values).then(unwrap<any>),
  requestTeamReview: (id: string, values: any) => api.post(`/incidents/${id}/investigation-team/review-request`, values).then(unwrap<any>),
  approveTeamReview: (id: string, values: any) => api.post(`/incidents/${id}/investigation-team/review-approve`, values).then(unwrap<any>),
  rejectTeamReview: (id: string, values: any) => api.post(`/incidents/${id}/investigation-team/review-reject`, values).then(unwrap<any>),
  rca: (id: string) => api.get(`/incidents/${id}/rca`).then(unwrap<any>),
  updateRcaMethod: (id: string, values: any) => api.patch(`/incidents/${id}/rca/method`, values).then(unwrap<any>),
  createRcaCausalFactor: (id: string, values: any) => api.post(`/incidents/${id}/rca/causal-factors`, values).then(unwrap<any>),
  updateRcaCausalFactor: (id: string, factorId: string, values: any) => api.patch(`/incidents/${id}/rca/causal-factors/${factorId}`, values).then(unwrap<any>),
  deleteRcaCausalFactor: (id: string, factorId: string, values: any) => api.delete(`/incidents/${id}/rca/causal-factors/${factorId}`, { data: values }).then(unwrap<any>),
  confirmRcaCausalFactor: (id: string, factorId: string, values: any) => api.post(`/incidents/${id}/rca/causal-factors/${factorId}/confirm`, values).then(unwrap<any>),
  rejectRcaCausalFactor: (id: string, factorId: string, values: any) => api.post(`/incidents/${id}/rca/causal-factors/${factorId}/reject`, values).then(unwrap<any>),
  linkRcaCausalFactorEvidence: (id: string, factorId: string, values: any) => api.post(`/incidents/${id}/rca/causal-factors/${factorId}/link-evidence`, values).then(unwrap<any>),
  convertRcaFactorToRootCause: (id: string, factorId: string, values: any) => api.post(`/incidents/${id}/rca/causal-factors/${factorId}/convert-root-cause`, values).then(unwrap<any>),
  createRcaRootCause: (id: string, values: any) => api.post(`/incidents/${id}/rca/root-causes`, values).then(unwrap<any>),
  updateRcaRootCause: (id: string, rootCauseId: string, values: any) => api.patch(`/incidents/${id}/rca/root-causes/${rootCauseId}`, values).then(unwrap<any>),
  deleteRcaRootCause: (id: string, rootCauseId: string, values: any) => api.delete(`/incidents/${id}/rca/root-causes/${rootCauseId}`, { data: values }).then(unwrap<any>),
  createRcaCapa: (id: string, rootCauseId: string, values: any) => api.post(`/incidents/${id}/rca/root-causes/${rootCauseId}/create-capa`, values).then(unwrap<any>),
  upsertRcaFiveWhyChain: (id: string, values: any) => api.post(`/incidents/${id}/rca/five-why/chains`, values).then(unwrap<any>),
  upsertRcaFiveWhyStep: (id: string, values: any) => api.post(`/incidents/${id}/rca/five-why/steps`, values).then(unwrap<any>),
  upsertRcaFishboneItem: (id: string, values: any) => api.post(`/incidents/${id}/rca/fishbone/items`, values).then(unwrap<any>),
  upsertRcaCauseTreeNode: (id: string, values: any) => api.post(`/incidents/${id}/rca/cause-tree/nodes`, values).then(unwrap<any>),
  upsertRcaCauseTreeEdge: (id: string, values: any) => api.post(`/incidents/${id}/rca/cause-tree/edges`, values).then(unwrap<any>),
  upsertRcaSystemicWeakness: (id: string, values: any) => api.post(`/incidents/${id}/rca/systemic-weaknesses`, values).then(unwrap<any>),
  upsertRcaHypothesis: (id: string, values: any) => api.post(`/incidents/${id}/rca/hypotheses`, values).then(unwrap<any>),
  requestRcaReview: (id: string, values: any) => api.post(`/incidents/${id}/rca/review-request`, values).then(unwrap<any>),
  approveRcaReview: (id: string, values: any) => api.post(`/incidents/${id}/rca/review-approve`, values).then(unwrap<any>),
  rejectRcaReview: (id: string, values: any) => api.post(`/incidents/${id}/rca/review-reject`, values).then(unwrap<any>),
  completeRca: (id: string, values: any) => api.post(`/incidents/${id}/rca/complete`, values).then(unwrap<any>),
  reopenRca: (id: string, values: any) => api.post(`/incidents/${id}/rca/reopen`, values).then(unwrap<any>),
  barriers: (id: string) => api.get(`/incidents/${id}/barriers`).then(unwrap<any>),
  createBarrier: (id: string, values: any) => api.post(`/incidents/${id}/barriers`, values).then(unwrap<any>),
  updateBarrier: (id: string, barrierId: string, values: any) => api.patch(`/incidents/${id}/barriers/${barrierId}`, values).then(unwrap<any>),
  deleteBarrier: (id: string, barrierId: string, values: any) => api.delete(`/incidents/${id}/barriers/${barrierId}`, { data: values }).then(unwrap<any>),
  importBarriersFromHazop: (id: string) => api.post(`/incidents/${id}/barriers/import-from-hazop`, {}).then(unwrap<any>),
  importBarriersFromLopa: (id: string) => api.post(`/incidents/${id}/barriers/import-from-lopa`, {}).then(unwrap<any>),
  linkBarrierEvidence: (id: string, barrierId: string, values: any) => api.post(`/incidents/${id}/barriers/${barrierId}/link-evidence`, values).then(unwrap<any>),
  linkBarrierRca: (id: string, barrierId: string, values: any) => api.post(`/incidents/${id}/barriers/${barrierId}/link-rca`, values).then(unwrap<any>),
  createBarrierFollowupAction: (id: string, values: any) => api.post(`/incidents/${id}/barriers/followups/create-action`, values).then(unwrap<any>),
  requestBarrierReview: (id: string, values: any) => api.post(`/incidents/${id}/barriers/review-request`, values).then(unwrap<any>),
  approveBarrierReview: (id: string, values: any) => api.post(`/incidents/${id}/barriers/review-approve`, values).then(unwrap<any>),
  rejectBarrierReview: (id: string, values: any) => api.post(`/incidents/${id}/barriers/review-reject`, values).then(unwrap<any>),
  capa: (id: string) => api.get(`/incidents/${id}/capa`).then(unwrap<any>),
  createCapa: (id: string, values: any) => api.post(`/incidents/${id}/capa`, values).then(unwrap<any>),
  updateCapa: (id: string, capaId: string, values: any) => api.patch(`/incidents/${id}/capa/${capaId}`, values).then(unwrap<any>),
  deleteCapa: (id: string, capaId: string, values: any) => api.delete(`/incidents/${id}/capa/${capaId}`, { data: values }).then(unwrap<any>),
  generateCapaFromRca: (id: string, values: any) => api.post(`/incidents/${id}/capa/generate-from-rca`, values).then(unwrap<any>),
  generateCapaFromBarriers: (id: string, values: any) => api.post(`/incidents/${id}/capa/generate-from-barriers`, values).then(unwrap<any>),
  linkCapaSource: (id: string, capaId: string, values: any) => api.post(`/incidents/${id}/capa/${capaId}/link-source`, values).then(unwrap<any>),
  linkCapaEvidence: (id: string, capaId: string, values: any) => api.post(`/incidents/${id}/capa/${capaId}/link-evidence`, values).then(unwrap<any>),
  submitCapaCompletion: (id: string, capaId: string, values: any) => api.post(`/incidents/${id}/capa/${capaId}/submit-completion`, values).then(unwrap<any>),
  acceptCapaEvidence: (id: string, capaId: string, values: any) => api.post(`/incidents/${id}/capa/${capaId}/accept-evidence`, values).then(unwrap<any>),
  rejectCapaEvidence: (id: string, capaId: string, values: any) => api.post(`/incidents/${id}/capa/${capaId}/reject-evidence`, values).then(unwrap<any>),
  verifyCapaEffectiveness: (id: string, capaId: string, values: any) => api.post(`/incidents/${id}/capa/${capaId}/verify-effectiveness`, values).then(unwrap<any>),
  requestCapaRework: (id: string, capaId: string, values: any) => api.post(`/incidents/${id}/capa/${capaId}/request-rework`, values).then(unwrap<any>),
  escalateCapa: (id: string, capaId: string, values: any) => api.post(`/incidents/${id}/capa/${capaId}/escalate`, values).then(unwrap<any>),
  linkExistingCapaAction: (id: string, capaId: string, values: any) => api.post(`/incidents/${id}/capa/${capaId}/link-existing-action`, values).then(unwrap<any>),
  requestCapaReview: (id: string, values: any) => api.post(`/incidents/${id}/capa/request-review`, values).then(unwrap<any>),
  approveCapaReview: (id: string, values: any) => api.post(`/incidents/${id}/capa/approve-review`, values).then(unwrap<any>),
  rejectCapaReview: (id: string, values: any) => api.post(`/incidents/${id}/capa/reject-review`, values).then(unwrap<any>),
  exportCapa: (id: string) => api.get(`/incidents/${id}/capa/export`).then(unwrap<any>),
  linkedRecords: (id: string) => api.get(`/incidents/${id}/linked-records`).then(unwrap<any>),
  createLinkedRecord: (id: string, values: any) => api.post(`/incidents/${id}/linked-records`, values).then(unwrap<any>),
  updateLinkedRecord: (id: string, linkId: string, values: any) => api.patch(`/incidents/${id}/linked-records/${linkId}`, values).then(unwrap<any>),
  deleteLinkedRecord: (id: string, linkId: string, values: any) => api.delete(`/incidents/${id}/linked-records/${linkId}`, { data: values }).then(unwrap<any>),
  autoDetectLinkedRecords: (id: string, values: any) => api.post(`/incidents/${id}/linked-records/auto-detect`, values).then(unwrap<any>),
  refreshLinkedRecord: (id: string, linkId: string) => api.post(`/incidents/${id}/linked-records/${linkId}/refresh-status`, {}).then(unwrap<any>),
  refreshAllLinkedRecords: (id: string) => api.post(`/incidents/${id}/linked-records/refresh-all-statuses`, {}).then(unwrap<any>),
  updateLinkedRecordImpact: (id: string, impactId: string, values: any) => api.patch(`/incidents/${id}/linked-records/record-impacts/${impactId}`, values).then(unwrap<any>),
  createLinkedRecordFollowup: (id: string, values: any) => api.post(`/incidents/${id}/linked-records/create-followup-action`, values).then(unwrap<any>),
  requestLinkedRecordsReview: (id: string, values: any) => api.post(`/incidents/${id}/linked-records/request-review`, values).then(unwrap<any>),
  approveLinkedRecordsReview: (id: string, values: any) => api.post(`/incidents/${id}/linked-records/approve-review`, values).then(unwrap<any>),
  rejectLinkedRecordsReview: (id: string, values: any) => api.post(`/incidents/${id}/linked-records/reject-review`, values).then(unwrap<any>),
  exportLinkedRecords: (id: string) => api.get(`/incidents/${id}/linked-records/export`).then(unwrap<any>),
  notificationsReporting: (id: string) => api.get(`/incidents/${id}/notifications-reporting`).then(unwrap<any>),
  runReportingDetermination: (id: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/run-determination`, values).then(unwrap<any>),
  updateReportingDetermination: (id: string, values: any) => api.patch(`/incidents/${id}/notifications-reporting/determination`, values).then(unwrap<any>),
  sendIncidentNotification: (id: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/notifications`, values).then(unwrap<any>),
  resendIncidentNotification: (id: string, notificationId: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/notifications/${notificationId}/resend`, values).then(unwrap<any>),
  acknowledgeIncidentNotification: (id: string, notificationId: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/notifications/${notificationId}/acknowledge`, values).then(unwrap<any>),
  createRegulatoryReport: (id: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/reports`, values).then(unwrap<any>),
  updateRegulatoryReport: (id: string, reportId: string, values: any) => api.patch(`/incidents/${id}/notifications-reporting/reports/${reportId}`, values).then(unwrap<any>),
  generateRegulatoryPackage: (id: string, reportId: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/reports/${reportId}/generate-package`, values).then(unwrap<any>),
  requestRegulatoryApproval: (id: string, reportId: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/reports/${reportId}/request-approval`, values).then(unwrap<any>),
  markRegulatorySubmitted: (id: string, reportId: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/reports/${reportId}/mark-submitted`, values).then(unwrap<any>),
  addRegulatoryAcknowledgement: (id: string, reportId: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/reports/${reportId}/add-acknowledgement`, values).then(unwrap<any>),
  markRegulatoryRejected: (id: string, reportId: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/reports/${reportId}/mark-rejected`, values).then(unwrap<any>),
  createExternalStakeholderNotification: (id: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/stakeholders`, values).then(unwrap<any>),
  createReportingFollowup: (id: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/create-followup-action`, values).then(unwrap<any>),
  requestReportingReview: (id: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/request-review`, values).then(unwrap<any>),
  approveReportingReview: (id: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/approve-review`, values).then(unwrap<any>),
  rejectReportingReview: (id: string, values: any) => api.post(`/incidents/${id}/notifications-reporting/reject-review`, values).then(unwrap<any>),
  exportReportingLog: (id: string) => api.get(`/incidents/${id}/notifications-reporting/export-log`).then(unwrap<any>),
  reviewApproval: (id: string) => api.get(`/incidents/${id}/review-approval`).then(unwrap<any>),
  runReviewReadinessCheck: (id: string, values: any) => api.post(`/incidents/${id}/review-approval/run-readiness-check`, values).then(unwrap<any>),
  startReviewWorkflow: (id: string, values: any) => api.post(`/incidents/${id}/review-approval/start-workflow`, values).then(unwrap<any>),
  createReviewReviewer: (id: string, values: any) => api.post(`/incidents/${id}/review-approval/reviewers`, values).then(unwrap<any>),
  updateReviewReviewer: (id: string, reviewerId: string, values: any) => api.patch(`/incidents/${id}/review-approval/reviewers/${reviewerId}`, values).then(unwrap<any>),
  removeReviewReviewer: (id: string, reviewerId: string, values: any) => api.delete(`/incidents/${id}/review-approval/reviewers/${reviewerId}`, { data: values }).then(unwrap<any>),
  requestReviewReviewer: (id: string, reviewerId: string, values: any) => api.post(`/incidents/${id}/review-approval/reviewers/${reviewerId}/request`, values).then(unwrap<any>),
  approveReviewReviewer: (id: string, reviewerId: string, values: any) => api.post(`/incidents/${id}/review-approval/reviewers/${reviewerId}/approve`, values).then(unwrap<any>),
  rejectReviewReviewer: (id: string, reviewerId: string, values: any) => api.post(`/incidents/${id}/review-approval/reviewers/${reviewerId}/reject`, values).then(unwrap<any>),
  requestReviewReviewerChanges: (id: string, reviewerId: string, values: any) => api.post(`/incidents/${id}/review-approval/reviewers/${reviewerId}/request-changes`, values).then(unwrap<any>),
  delegateReviewReviewer: (id: string, reviewerId: string, values: any) => api.post(`/incidents/${id}/review-approval/reviewers/${reviewerId}/delegate`, values).then(unwrap<any>),
  escalateReviewReviewer: (id: string, reviewerId: string, values: any) => api.post(`/incidents/${id}/review-approval/reviewers/${reviewerId}/escalate`, values).then(unwrap<any>),
  eSignReviewApproval: (id: string, values: any) => api.post(`/incidents/${id}/review-approval/e-sign`, values).then(unwrap<any>),
  overrideReviewBlocker: (id: string, blockerId: string, values: any) => api.post(`/incidents/${id}/review-approval/blockers/${blockerId}/override`, values).then(unwrap<any>),
  createReviewChangeRequest: (id: string, values: any) => api.post(`/incidents/${id}/review-approval/change-requests`, values).then(unwrap<any>),
  updateReviewChangeRequest: (id: string, requestId: string, values: any) => api.patch(`/incidents/${id}/review-approval/change-requests/${requestId}`, values).then(unwrap<any>),
  resolveReviewChangeRequest: (id: string, requestId: string, values: any) => api.post(`/incidents/${id}/review-approval/change-requests/${requestId}/resolve`, values).then(unwrap<any>),
  requestReviewClosure: (id: string, values: any) => api.post(`/incidents/${id}/review-approval/request-closure`, values).then(unwrap<any>),
  approveReviewClosure: (id: string, values: any) => api.post(`/incidents/${id}/review-approval/approve-closure`, values).then(unwrap<any>),
  closeFromReviewApproval: (id: string, values: any) => api.post(`/incidents/${id}/review-approval/close`, values).then(unwrap<any>),
  reopenFromReviewApproval: (id: string, values: any) => api.post(`/incidents/${id}/review-approval/reopen`, values).then(unwrap<any>),
  lessons: (id: string) => api.get(`/incidents/${id}/lessons`).then(unwrap<any>),
  createLesson: (id: string, values: any) => api.post(`/incidents/${id}/lessons`, values).then(unwrap<any>),
  updateLesson: (id: string, lessonId: string, values: any) => api.patch(`/incidents/${id}/lessons/${lessonId}`, values).then(unwrap<any>),
  deleteLesson: (id: string, lessonId: string, values: any) => api.delete(`/incidents/${id}/lessons/${lessonId}`, { data: values }).then(unwrap<any>),
  generateLessons: (id: string, values: any) => api.post(`/incidents/${id}/lessons/generate-from-rca-capa`, values).then(unwrap<any>),
  linkLessonSource: (id: string, lessonId: string, values: any) => api.post(`/incidents/${id}/lessons/${lessonId}/link-source`, values).then(unwrap<any>),
  distributeLesson: (id: string, lessonId: string, values: any) => api.post(`/incidents/${id}/lessons/${lessonId}/distribute`, values).then(unwrap<any>),
  verifyLesson: (id: string, lessonId: string, values: any) => api.post(`/incidents/${id}/lessons/${lessonId}/verify`, values).then(unwrap<any>),
  requestLessonsReview: (id: string, values: any) => api.post(`/incidents/${id}/lessons/request-review`, values).then(unwrap<any>),
  approveLessonsReview: (id: string, values: any) => api.post(`/incidents/${id}/lessons/approve-review`, values).then(unwrap<any>),
  rejectLessonsReview: (id: string, values: any) => api.post(`/incidents/${id}/lessons/reject-review`, values).then(unwrap<any>),
  history: (id: string, filters?: any) => api.get(`/incidents/${id}/history`, { params: filters ?? {} }).then(unwrap<any>),
  historyDiff: (id: string, eventId: string) => api.get(`/incidents/${id}/history/diff/${eventId}`).then(unwrap<any>),
  exportHistory: (id: string, filters?: any) => api.get(`/incidents/${id}/history/export`, { params: filters ?? {} }).then(unwrap<any>),
  finalReport: (id: string) => api.get(`/incidents/${id}/final-report`).then(unwrap<any>),
  finalReportTemplates: (id: string) => api.get(`/incidents/${id}/final-report/templates`).then(unwrap<any>),
  finalReportPreview: (id: string) => api.get(`/incidents/${id}/final-report/preview`).then(unwrap<any>),
  finalReportRedactionPreview: (id: string) => api.get(`/incidents/${id}/final-report/redaction-preview`).then(unwrap<any>),
  finalReportContext: (id: string) => api.get(`/incidents/${id}/final-report/context`).then(unwrap<any>),
  runFinalReportReadiness: (id: string, values: any) => api.post(`/incidents/${id}/final-report/run-readiness-check`, values).then(unwrap<any>),
  selectFinalReportTemplate: (id: string, values: any) => api.post(`/incidents/${id}/final-report/select-template`, values).then(unwrap<any>),
  updateFinalReportSections: (id: string, values: any) => api.patch(`/incidents/${id}/final-report/sections`, values).then(unwrap<any>),
  createFinalReportSnapshot: (id: string, values: any) => api.post(`/incidents/${id}/final-report/create-snapshot`, values).then(unwrap<any>),
  generateFinalReport: (id: string, values: any) => api.post(`/incidents/${id}/final-report/generate`, values).then(unwrap<any>),
  exportFinalReport: (id: string, exportType: string, values: any) => api.post(`/incidents/${id}/final-report/export/${exportType}`, values).then(unwrap<any>),
  downloadFinalReport: (id: string, reportId: string) => api.get(`/incidents/${id}/final-report/reports/${reportId}/download`).then(unwrap<any>),
  markFinalReportOfficial: (id: string, reportId: string, values: any) => api.post(`/incidents/${id}/final-report/reports/${reportId}/mark-official`, values).then(unwrap<any>),
  publishFinalReport: (id: string, reportId: string, values: any) => api.post(`/incidents/${id}/final-report/reports/${reportId}/publish`, values).then(unwrap<any>),
  supersedeFinalReport: (id: string, reportId: string, values: any) => api.post(`/incidents/${id}/final-report/reports/${reportId}/supersede`, values).then(unwrap<any>),
  archiveFinalReport: (id: string, reportId: string, values: any) => api.post(`/incidents/${id}/final-report/reports/${reportId}/archive`, values).then(unwrap<any>),
  requestFinalReportReview: (id: string, values: any) => api.post(`/incidents/${id}/final-report/request-review`, values).then(unwrap<any>),
  approveFinalReportReview: (id: string, values: any) => api.post(`/incidents/${id}/final-report/approve-review`, values).then(unwrap<any>),
  rejectFinalReportReview: (id: string, values: any) => api.post(`/incidents/${id}/final-report/reject-review`, values).then(unwrap<any>),
  statusBar: (id: string) => api.get(`/incidents/${id}/status-bar`).then(unwrap<any>),
  tabs: (id: string) => api.get(`/incidents/${id}/tabs`).then(unwrap<any>),
  quickActions: (id: string) => api.get(`/incidents/${id}/quick-actions`).then(unwrap<any[]>),
  assignOwner: (id: string, values: any) => api.patch(`/incidents/${id}/owner`, values).then(unwrap<any>),
  changeStatus: (id: string, values: any) => api.post(`/incidents/${id}/status`, values).then(unwrap<any>),
  close: (id: string, values: any) => api.post(`/incidents/${id}/close`, values).then(unwrap<any>),
  reopen: (id: string, values: any) => api.post(`/incidents/${id}/reopen`, values).then(unwrap<any>),
  exportSummary: (id: string) => api.get(`/incidents/${id}/export-summary`).then(unwrap<any>)
};
