import { X } from 'lucide-react';
import type { PermitConflictRecord } from '../../services/ptw-conflict.service';

export function ConflictDetailDrawer({ conflict, onClose }: { conflict?: PermitConflictRecord | null | undefined; onClose: () => void }) {
  if (!conflict) return null;
  return <div className="fixed inset-0 z-50 bg-black/50"><aside className="ml-auto h-full w-full max-w-xl overflow-y-auto border-l border-[var(--psm-line)] bg-[var(--psm-surface)] p-5 shadow-2xl"><div className="mb-4 flex items-center justify-between"><h3 className="text-lg font-semibold">Conflict Detail</h3><button className="psm-button psm-button-secondary" onClick={onClose}><X size={15} /></button></div><div className="space-y-3 text-sm">{[['Conflict type', conflict.conflict_type], ['Severity', conflict.severity], ['Status', conflict.status], ['Override status', conflict.override_status ?? 'Not Requested'], ['Conflicting permit', conflict.conflicting_permit?.permit_number ?? conflict.conflicting_permit_id ?? '-'], ['Equipment', conflict.conflicting_permit?.equipment_tag ?? conflict.conflicting_equipment_id ?? '-'], ['Area / Unit', conflict.conflicting_permit?.job_area ?? '-'], ['Distance', conflict.distance_meters ? `${conflict.distance_meters} m` : '-'], ['Why this matters', conflict.why_it_matters ?? '-'], ['Recommended action', conflict.recommended_action ?? '-']].map(([label, value]) => <div key={label} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3"><div className="text-xs uppercase text-[var(--psm-muted)]">{label}</div><div className="mt-1 font-semibold">{value}</div></div>)}<div className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3"><div className="text-xs uppercase text-[var(--psm-muted)]">Description</div><p className="mt-1">{conflict.description}</p></div><div className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3"><div className="text-xs uppercase text-[var(--psm-muted)]">Required controls</div><ul className="mt-2 list-inside list-disc">{(conflict.required_controls ?? []).map((item) => <li key={item}>{item}</li>)}</ul></div></div></aside></div>;
}
