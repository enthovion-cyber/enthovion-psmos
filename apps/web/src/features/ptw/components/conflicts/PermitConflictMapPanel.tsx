import type { ConflictMap } from '../../services/ptw-conflict.service';

export function PermitConflictMapPanel({ map }: { map?: ConflictMap | undefined }) {
  return <section className="psm-card p-5"><div className="mb-4 flex items-center justify-between"><h3 className="text-sm font-semibold uppercase tracking-wide">Permit Map Conflict Panel</h3><span className="text-xs text-[var(--psm-muted)]">Radius {map?.radiusMeters ?? 50} m</span></div><div className="relative h-56 overflow-hidden rounded-xl border border-[var(--psm-line)] bg-[linear-gradient(var(--psm-line)_1px,transparent_1px),linear-gradient(90deg,var(--psm-line)_1px,transparent_1px)] bg-[size:32px_32px]"><div className="absolute left-1/2 top-1/2 h-4 w-4 -translate-x-1/2 -translate-y-1/2 rounded-full bg-primary shadow-[0_0_0_10px_rgba(37,99,235,0.15)]" title="Current permit" />{(map?.markers ?? []).map((marker, index) => <div key={String(marker.id ?? index)} className="absolute h-4 w-4 rounded-full bg-danger shadow-[0_0_0_8px_rgba(239,68,68,0.16)]" style={{ left: `${Number(marker.x ?? 20)}%`, top: `${Number(marker.y ?? 30)}%` }} title={String(marker.label ?? marker.type)} />)}</div><div className="mt-3 flex flex-wrap gap-2 text-xs text-[var(--psm-muted)]">{(map?.areas ?? []).map((area) => <span key={area} className="rounded-full border border-[var(--psm-line)] px-2 py-1">{area}</span>)}{!map?.areas?.length ? 'No mapped conflict areas.' : null}</div></section>;
}
