import type { SimopsReview } from '../../services/ptw-conflict.service';

export function SIMOPSReviewPanel({ review, onSave, onApprove, onReject, onAcknowledge }: { review?: SimopsReview | null | undefined; onSave: () => void; onApprove: () => void; onReject: () => void; onAcknowledge: () => void }) {
  return <section className="psm-card p-5"><div className="mb-4 flex items-center justify-between"><h3 className="text-sm font-semibold uppercase tracking-wide">SIMOPS Review</h3><span className="rounded-full bg-primary/10 px-2 py-1 text-xs text-primary">{review?.status ?? 'Not Created'}</span></div><div className="grid gap-3 md:grid-cols-2"><Info label="SIMOPS Required" value={review?.simops_required ? 'Yes' : 'No'} /><Info label="Coordinator" value={review?.coordinator_name ?? '-'} /><Info label="Concurrent Work" value={review?.concurrent_work_description ?? '-'} /><Info label="Interaction Hazards" value={review?.interaction_hazards ?? '-'} /><Info label="Required Controls" value={review?.required_controls ?? '-'} /><Info label="Control Room" value={review?.control_room_acknowledged ? 'Acknowledged' : 'Pending'} /><Info label="Area Authority" value={review?.area_authority_reviewed ? 'Reviewed' : 'Pending'} /></div><div className="mt-4 flex flex-wrap gap-2"><button className="psm-button psm-button-secondary" onClick={onSave}>{review ? 'Update SIMOPS' : 'Create SIMOPS'}</button>{review ? <button className="psm-button psm-button-secondary" onClick={onAcknowledge}>Control Room Acknowledge</button> : null}{review ? <button className="psm-button psm-button-primary" onClick={onApprove}>Approve</button> : null}{review ? <button className="psm-button psm-button-danger" onClick={onReject}>Reject</button> : null}</div></section>;
}

function Info({ label, value }: { label: string; value: string }) { return <div className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3"><div className="text-xs text-[var(--psm-muted)]">{label}</div><div className="mt-1 text-sm font-semibold">{value}</div></div>; }
