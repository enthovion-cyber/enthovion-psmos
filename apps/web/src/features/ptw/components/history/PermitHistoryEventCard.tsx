import { AlertTriangle, ArrowRight, ExternalLink } from 'lucide-react';
import type { PermitHistoryEvent } from '../../services/ptw-history.service';

export function PermitHistoryEventCard({ event, onOpen }: { event: PermitHistoryEvent; onOpen: (event: PermitHistoryEvent) => void }) {
  return <button onClick={() => onOpen(event)} className="w-full rounded-xl border border-[var(--psm-line)] bg-[var(--psm-surface)] p-4 text-left shadow-sm transition hover:border-primary/50"><div className="flex flex-col gap-3 md:flex-row md:items-start md:justify-between"><div><div className="flex flex-wrap items-center gap-2"><span className="rounded-full border border-primary/30 bg-primary/10 px-2 py-1 text-xs font-semibold text-primary">{event.event_category ?? 'System'}</span>{event.is_safety_critical ? <span className="inline-flex items-center gap-1 rounded-full border border-danger/40 bg-danger/10 px-2 py-1 text-xs font-semibold text-danger"><AlertTriangle size={12} /> Safety Critical</span> : null}</div><h4 className="mt-3 text-sm font-semibold">{event.event_title ?? event.title ?? event.event_type}</h4><p className="mt-1 text-sm text-[var(--psm-muted)]">{event.description ?? event.event_type}</p></div><div className="text-right text-xs text-[var(--psm-muted)]"><div>{new Date(event.created_at).toLocaleString()}</div><div className="mt-1">{event.user_name ?? event.user_id ?? '-'}</div></div></div><div className="mt-3 flex flex-wrap items-center gap-3 text-xs text-[var(--psm-muted)]"><span>{event.event_type}</span>{event.related_record_number ? <span className="inline-flex items-center gap-1"><ExternalLink size={12} /> {event.related_record_number}</span> : null}{event.before_value || event.before_data || event.after_value || event.after_data ? <span className="inline-flex items-center gap-1 text-primary">Before <ArrowRight size={12} /> After</span> : null}{event.ip_address ? <span>IP {event.ip_address}</span> : null}</div></button>;
}
