import { Download, Search, ShieldAlert } from 'lucide-react';
import type { PermitHistoryFilters } from '../../services/ptw-history.service';

const categories = ['All', 'Lifecycle', 'Details', 'Isolation', 'Gas Test', 'Workforce', 'Handover', 'Conflicts', 'Signatures', 'Attachments', 'Certificates', 'System', 'Safety-Critical'];

export function PermitHistoryFilterBar({ filters, onChange, onExportPdf, onExportCsv }: { filters: PermitHistoryFilters; onChange: (filters: PermitHistoryFilters) => void; onExportPdf: () => void; onExportCsv: () => void }) {
  const active = filters.safety_critical ? 'Safety-Critical' : filters.category ?? 'All';
  const setCategory = (category: string) => onChange({ ...filters, category: category === 'Safety-Critical' ? undefined : category, safety_critical: category === 'Safety-Critical' ? true : undefined });
  return <section className="psm-card p-4"><div className="flex flex-col gap-3 xl:flex-row xl:items-center xl:justify-between"><div className="flex flex-wrap gap-2">{categories.map((category) => <button key={category} onClick={() => setCategory(category)} className={`rounded-lg border px-3 py-2 text-xs font-semibold transition ${active === category ? 'border-primary bg-primary text-white' : 'border-[var(--psm-line)] bg-[var(--psm-surface-2)] text-[var(--psm-muted)] hover:text-[var(--psm-text)]'}`}>{category}</button>)}</div><div className="flex flex-wrap gap-2"><button onClick={onExportPdf} className="psm-button psm-button-secondary"><Download size={15} /> PDF</button><button onClick={onExportCsv} className="psm-button psm-button-secondary"><Download size={15} /> CSV</button></div></div><div className="mt-4 grid gap-3 md:grid-cols-5"><label className="relative md:col-span-2"><Search size={15} className="absolute left-3 top-3 text-[var(--psm-muted)]" /><input value={filters.search ?? ''} onChange={(event) => onChange({ ...filters, search: event.target.value })} placeholder="Search timeline..." className="w-full rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] py-2 pl-9 pr-3 text-sm outline-none focus:border-primary" /></label><input type="date" value={filters.date_from ?? ''} onChange={(event) => onChange({ ...filters, date_from: event.target.value })} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" /><input type="date" value={filters.date_to ?? ''} onChange={(event) => onChange({ ...filters, date_to: event.target.value })} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" /><input value={filters.event_type ?? ''} onChange={(event) => onChange({ ...filters, event_type: event.target.value })} placeholder="Event type" className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" /></div><button onClick={() => onChange({ ...filters, safety_critical: !filters.safety_critical })} className={`mt-3 inline-flex items-center gap-2 rounded-lg border px-3 py-2 text-xs font-semibold ${filters.safety_critical ? 'border-danger bg-danger/10 text-danger' : 'border-[var(--psm-line)] text-[var(--psm-muted)]'}`}><ShieldAlert size={14} /> Safety-critical only</button></section>;
}
