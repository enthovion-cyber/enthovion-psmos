'use client';

export function CompetencyFilters({ value = {}, onChange }: { value?: Record<string, any>; onChange: (value: Record<string, any>) => void }) {
  const set = (key: string, next: string) => onChange({ ...value, [key]: next || undefined });
  return <div className="grid gap-3 rounded-xl border border-[var(--psm-line)] bg-[var(--psm-surface)] p-4 md:grid-cols-4"><input className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" placeholder="Search profile, code, role" value={value.search ?? ''} onChange={(e) => set('search', e.target.value)} /><select className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" value={value.profileStatus ?? ''} onChange={(e) => set('profileStatus', e.target.value)}><option value="">All profile statuses</option>{['Draft','Active','Approved','Locked','Archived'].map((v) => <option key={v}>{v}</option>)}</select><select className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" value={value.reviewStatus ?? ''} onChange={(e) => set('reviewStatus', e.target.value)}><option value="">All review statuses</option>{['Not Submitted','Pending Review','Approved','Rejected','Review Overdue'].map((v) => <option key={v}>{v}</option>)}</select><select className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" value={value.profileType ?? ''} onChange={(e) => set('profileType', e.target.value)}><option value="">All profile types</option>{['Job Role Profile','PTW Role Profile','Safety-Critical Role Profile','Contractor Role Profile','Custom'].map((v) => <option key={v}>{v}</option>)}</select></div>;
}
