import { ProfileMatrixSyncStatusBadge } from '../../shared/ProfileMatrixSyncStatusBadge';
import { TrainingCard } from '../../shared/TrainingUi';
export function ProfileMatrixSyncTab({ profile, rows }: { profile: Record<string, any>; rows: Record<string, any>[] }) { return <TrainingCard title="Training Matrix Sync"><div className="mb-3"><ProfileMatrixSyncStatusBadge value={profile.matrix_sync_status} /></div>{rows.length ? rows.map((row) => <div key={row.id} className="mb-2 rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3 text-sm"><p className="font-semibold">{row.sync_status}</p><p className="text-[var(--psm-muted)]">Rule {row.matrix_rule_id ?? 'pending'} / {row.sync_message ?? 'No message'}</p></div>) : <p className="text-sm text-[var(--psm-muted)]">No matrix links yet. Sync creates/updates Training Matrix rules from safety-critical, PSM-critical, and PTW-critical requirements.</p>}</TrainingCard>; }
