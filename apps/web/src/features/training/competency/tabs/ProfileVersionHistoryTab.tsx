import { ProfileVersionBadge } from '../../shared/ProfileVersionBadge';
import { TrainingCard } from '../../shared/TrainingUi';
export function ProfileVersionHistoryTab({ rows }: { rows: Record<string, any>[] }) { return <TrainingCard title="Profile Version History">{rows.length ? <div className="space-y-2">{rows.map((row) => <div key={row.id} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3"><div className="flex flex-wrap gap-2"><ProfileVersionBadge value={row.version} /><span className="text-sm font-semibold">{row.version_type}</span><span className="text-sm text-[var(--psm-muted)]">{row.version_status}</span></div><p className="mt-1 text-sm text-[var(--psm-muted)]">{row.change_summary ?? 'No change summary'}</p></div>)}</div> : <p className="text-sm text-[var(--psm-muted)]">No profile versions have been recorded.</p>}</TrainingCard>; }
