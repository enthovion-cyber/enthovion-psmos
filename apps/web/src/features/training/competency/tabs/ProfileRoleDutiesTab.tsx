import { SafetyCriticalCompetencyBadge } from '../../shared/SafetyCriticalCompetencyBadge';
import { TrainingCard } from '../../shared/TrainingUi';
export function ProfileRoleDutiesTab({ rows }: { rows: Record<string, any>[] }) { return <TrainingCard title="Role Duties / Safety-Critical Tasks">{rows.length ? <div className="grid gap-3 md:grid-cols-2">{rows.map((row) => <div key={row.id} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3"><p className="font-semibold">{row.duty_title}</p><p className="text-sm text-[var(--psm-muted)]">{row.task_type} / {row.related_hazard ?? 'No hazard linked'}</p><div className="mt-2"><SafetyCriticalCompetencyBadge value={row.safety_critical} /></div></div>)}</div> : <p className="text-sm text-[var(--psm-muted)]">No role duties have been added.</p>}</TrainingCard>; }
