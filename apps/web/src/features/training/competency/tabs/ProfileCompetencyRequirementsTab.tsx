import { CompetencyLevelBadge } from '../../shared/CompetencyLevelBadge';
import { SafetyCriticalCompetencyBadge } from '../../shared/SafetyCriticalCompetencyBadge';
import { TrainingCard } from '../../shared/TrainingUi';
export function ProfileCompetencyRequirementsTab({ rows }: { rows: Record<string, any>[] }) { return <TrainingCard title="Competency Requirements">{rows.length ? <div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="text-xs uppercase text-[var(--psm-muted)]"><tr>{['Competency','Category','Level','Mandatory','Evidence','Verification','Certificate','SOP','Safety','Linked Training','PTW Role'].map((h) => <th key={h} className="px-3 py-2">{h}</th>)}</tr></thead><tbody>{rows.map((row) => <tr key={row.id} className="border-t border-[var(--psm-line)]"><td className="px-3 py-3 font-semibold">{row.competency_title}</td><td className="px-3 py-3">{row.competency_category}</td><td className="px-3 py-3"><CompetencyLevelBadge value={row.required_level} /></td><td className="px-3 py-3">{row.mandatory ? 'Yes' : 'No'}</td><td className="px-3 py-3">{row.evidence_required ? 'Required' : 'Not required'}</td><td className="px-3 py-3">{row.verification_required ? 'Required' : 'No'}</td><td className="px-3 py-3">{row.certificate_required ? 'Yes' : 'No'}</td><td className="px-3 py-3">{row.sop_acknowledgement_required ? 'Yes' : 'No'}</td><td className="px-3 py-3"><SafetyCriticalCompetencyBadge value={row.safety_critical} /></td><td className="px-3 py-3">{row.linked_training_reference_id ?? '-'}</td><td className="px-3 py-3">{row.linked_ptw_role ?? '-'}</td></tr>)}</tbody></table></div> : <p className="text-sm text-[var(--psm-muted)]">No competency requirements. Activation is blocked until at least one requirement exists.</p>}</TrainingCard>; }
