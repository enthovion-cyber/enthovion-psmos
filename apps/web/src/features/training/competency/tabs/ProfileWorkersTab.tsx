import { TrainingCard } from '../../shared/TrainingUi';
export function ProfileWorkersTab({ rows }: { rows: Record<string, any>[] }) { return <TrainingCard title="Assigned Workers">{rows.length ? <div className="grid gap-2">{rows.map((row) => <div key={row.id} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3 text-sm"><p className="font-semibold">{row.worker_id}</p><p className="text-[var(--psm-muted)]">{row.assignment_source} / {row.assignment_status} / assigned {row.assigned_at}</p></div>)}</div> : <p className="text-sm text-[var(--psm-muted)]">No workers assigned. Use assignments to apply the profile manually or via backend auto-assignment.</p>}</TrainingCard>; }
