import { CompetencyLevelBadge } from '../shared/CompetencyLevelBadge';
import { SafetyCriticalCompetencyBadge } from '../shared/SafetyCriticalCompetencyBadge';

export function CompetencyLibraryTable({ rows }: { rows: Record<string, any>[] }) {
  if (!rows.length) return <p className="text-sm text-[var(--psm-muted)]">No reusable competencies have been created.</p>;
  return <div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="text-xs uppercase text-[var(--psm-muted)]"><tr>{['Competency','Code','Category','Default level','Evidence rule','Safety','PSM','PTW','Renewal','Linked training','Linked SOP','Active'].map((h) => <th key={h} className="px-3 py-2">{h}</th>)}</tr></thead><tbody>{rows.map((row) => <tr key={row.id} className="border-t border-[var(--psm-line)]"><td className="px-3 py-3 font-semibold">{row.competency_name}</td><td className="px-3 py-3">{row.competency_code}</td><td className="px-3 py-3">{row.category}</td><td className="px-3 py-3"><CompetencyLevelBadge value={row.default_required_level} /></td><td className="px-3 py-3">{row.default_evidence_rule_json ? 'Configured' : 'Missing Evidence Rule'}</td><td className="px-3 py-3"><SafetyCriticalCompetencyBadge value={row.safety_critical_default} /></td><td className="px-3 py-3">{row.psm_critical_default ? 'Yes' : 'No'}</td><td className="px-3 py-3">{row.ptw_critical_default ? 'Yes' : 'No'}</td><td className="px-3 py-3">{row.renewal_interval_days_default ?? '-'}</td><td className="px-3 py-3">{row.linked_training_reference_id ?? '-'}</td><td className="px-3 py-3">{row.linked_sop_id ?? '-'}</td><td className="px-3 py-3">{row.active ? 'Active' : 'Inactive'}</td></tr>)}</tbody></table></div>;
}
