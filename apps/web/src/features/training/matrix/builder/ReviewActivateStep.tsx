import { TrainingCard } from '../../shared/TrainingUi';

export function ReviewActivateStep() {
  return <TrainingCard title="7. Review & Activate" subtitle="Rule summary, affected workers preview, current gaps, sites/units/areas, blocking impact, evidence requirements, and activation permissions." ><p className="text-sm text-[var(--psm-muted)]">Activation creates audit/history and triggers a backend matrix evaluation.</p></TrainingCard>;
}
