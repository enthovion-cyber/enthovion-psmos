import { TrainingCard } from '../../shared/TrainingUi';

export function BlockingSafetyImpactStep() {
  return <TrainingCard title="6. Blocking / Safety Impact" subtitle="PTW, MOC, PSSR, safety-critical work, action creation, notifications, escalation, waiver, and temporary waiver controls." ><p className="text-sm text-[var(--psm-muted)]">Blocking rules require safety-critical or PSM-critical justification.</p></TrainingCard>;
}
