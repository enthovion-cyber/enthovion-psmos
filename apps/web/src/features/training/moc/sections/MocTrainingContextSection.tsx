import { MiniField, PanelGrid } from '../MocTrainingPanelPrimitives';

export function MocTrainingContextSection({ values, onChange, readOnly = false }: { values: Record<string, any>; onChange?: (values: Record<string, any>) => void; readOnly?: boolean }) {
  return <div className="space-y-3"><h2 className="text-base font-semibold">MOC Training Context</h2><div className="grid gap-3 md:grid-cols-2"><input disabled={readOnly} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" placeholder="Linked MOC ID or number" value={values.mocId ?? values.moc_id ?? ''} onChange={(event) => onChange?.({ ...values, mocId: event.target.value })} /><input disabled={readOnly} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" placeholder="Requirement title" value={values.requirementTitle ?? values.requirement_title ?? ''} onChange={(event) => onChange?.({ ...values, requirementTitle: event.target.value })} /><select disabled={readOnly} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" value={values.requirementSource ?? values.requirement_source ?? 'MOC Impact Assessment'} onChange={(event) => onChange?.({ ...values, requirementSource: event.target.value })}><option>MOC Impact Assessment</option><option>MOC Risk Ranking</option><option>MOC Engineering Package</option><option>MOC SOP Update</option><option>MOC PSI Update</option><option>MOC PSSR Requirement</option><option>MOC Approval Condition</option><option>Training Matrix</option><option>Required Training Library</option><option>Manual</option></select><select disabled={readOnly} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" value={values.impactLevel ?? values.impact_level ?? 'Medium'} onChange={(event) => onChange?.({ ...values, impactLevel: event.target.value })}><option>Low</option><option>Medium</option><option>High</option><option>Critical</option></select></div><PanelGrid><MiniField label="Site" value={values.site_id ?? values.siteId} /><MiniField label="Unit" value={values.unit_id ?? values.unitId} /><MiniField label="Area" value={values.area_id ?? values.areaId} /></PanelGrid></div>;
}
