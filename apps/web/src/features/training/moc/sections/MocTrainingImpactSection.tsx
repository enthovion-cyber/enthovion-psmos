import { MiniField, PanelGrid } from '../MocTrainingPanelPrimitives';

export function MocTrainingImpactSection({ values, onChange, readOnly = false }: { values: Record<string, any>; onChange?: (values: Record<string, any>) => void; readOnly?: boolean }) {
  const set = (key: string, value: unknown) => onChange?.({ ...values, [key]: value });
  return <div className="space-y-3"><h2 className="text-base font-semibold">MOC Training Impact</h2><div className="grid gap-3 md:grid-cols-3"><label className="flex items-center gap-2 rounded-lg border border-[var(--psm-line)] p-3 text-sm"><input disabled={readOnly} type="checkbox" checked={Boolean(values.trainingRequired ?? values.training_required)} onChange={(event) => set('trainingRequired', event.target.checked)} /> Training required</label><label className="flex items-center gap-2 rounded-lg border border-[var(--psm-line)] p-3 text-sm"><input disabled={readOnly} type="checkbox" checked={Boolean(values.safetyCritical ?? values.safety_critical)} onChange={(event) => set('safetyCritical', event.target.checked)} /> Safety-critical</label><label className="flex items-center gap-2 rounded-lg border border-[var(--psm-line)] p-3 text-sm"><input disabled={readOnly} type="checkbox" checked={Boolean(values.reevaluationRequired ?? values.reevaluation_required)} onChange={(event) => set('reevaluationRequired', event.target.checked)} /> Re-evaluation required</label></div><textarea disabled={readOnly} className="min-h-24 w-full rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3 text-sm" placeholder="Training required reason / impact basis" value={values.trainingRequiredReason ?? values.training_required_reason ?? ''} onChange={(event) => set('trainingRequiredReason', event.target.value)} /><PanelGrid><MiniField label="SOP changed" value={values.sop_updated ?? values.sopUpdated} /><MiniField label="PSI changed" value={values.psi_changed ?? values.psiChanged} /><MiniField label="PTW role affected" value={values.ptw_role_affected ?? values.ptwRoleAffected} /></PanelGrid></div>;
}
