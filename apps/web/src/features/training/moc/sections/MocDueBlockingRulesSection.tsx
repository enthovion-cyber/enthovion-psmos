import { MiniField, PanelGrid } from '../MocTrainingPanelPrimitives';

export function MocDueBlockingRulesSection({ values, onChange, readOnly = false }: { values: Record<string, any>; onChange?: (values: Record<string, any>) => void; readOnly?: boolean }) {
  const set = (key: string, value: unknown) => onChange?.({ ...values, [key]: value });
  return <div className="space-y-3"><h2 className="text-base font-semibold">Due Dates and Blocking Rules</h2><div className="grid gap-3 md:grid-cols-2"><input disabled={readOnly} type="datetime-local" className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" value={String(values.dueDate ?? values.due_date ?? '').slice(0, 16)} onChange={(event) => set('dueDate', event.target.value)} /><input disabled={readOnly} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" placeholder="Owner user ID" value={values.ownerUserId ?? values.owner_user_id ?? ''} onChange={(event) => set('ownerUserId', event.target.value)} /></div><PanelGrid><MiniField label="Implementation blocker" value={values.implementation_blocker ?? values.implementationBlocker} /><MiniField label="Closure blocker" value={values.closure_blocker ?? values.closureBlocker} /><MiniField label="Startup blocker" value={values.startup_blocker ?? values.startupBlocker} /></PanelGrid><textarea disabled={readOnly} className="min-h-20 w-full rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3 text-sm" placeholder="Blocker reasons / startup conditions" value={values.blockerReason ?? values.blocker_reason ?? ''} onChange={(event) => set('blockerReason', event.target.value)} /></div>;
}
