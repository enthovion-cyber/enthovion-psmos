import { MocTrainingBlockerBadge } from '../../shared/MOCTrainingBlockerBadge';
import { RowsPanel, SimpleTable, valueText } from '../MocTrainingPanelPrimitives';

export function MocTrainingBlockersTab({ rows = [] }: { rows?: any[] | undefined }) {
  return <RowsPanel title="MOC Training Blockers" subtitle="Implementation, closure and PSSR startup blockers generated by backend readiness." rows={rows} emptyTitle="No blockers" emptyMessage="No blocker rows were returned for this requirement.">{(items) => <SimpleTable rows={items} columns={[{ key: 'blocker_title', label: 'Blocker', render: (row) => <div><p className="font-semibold">{valueText(row.blocker_title)}</p><p className="text-xs text-[var(--psm-muted)]">{valueText(row.blocker_description)}</p></div> }, { key: 'blocker_type', label: 'Type' }, { key: 'blocker_status', label: 'Status', render: (row) => <MocTrainingBlockerBadge status={row.blocker_status} /> }, { key: 'severity', label: 'Severity' }, { key: 'implementation_blocker', label: 'Implementation', render: (row) => valueText(row.implementation_blocker) }, { key: 'closure_blocker', label: 'Closure', render: (row) => valueText(row.closure_blocker) }, { key: 'startup_blocker', label: 'Startup', render: (row) => valueText(row.startup_blocker) }]} />}</RowsPanel>;
}
