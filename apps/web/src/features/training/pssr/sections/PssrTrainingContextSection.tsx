import { MiniField, PanelGrid } from '../PssrTrainingPanelPrimitives';

export function PssrTrainingContextSection({ values, onChange, readOnly = false }: { values: Record<string, any>; onChange?: (values: Record<string, any>) => void; readOnly?: boolean }) {
  return <div className="space-y-3"><h2 className="text-base font-semibold">PSSR Training Context</h2><div className="grid gap-3 md:grid-cols-2"><input disabled={readOnly} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" placeholder="Linked PSSR ID or number" value={values.pssrId ?? values.pssr_id ?? ''} onChange={(event) => onChange?.({ ...values, pssrId: event.target.value })} /><input disabled={readOnly} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" placeholder="Readiness title" value={values.readinessTitle ?? values.readiness_title ?? ''} onChange={(event) => onChange?.({ ...values, readinessTitle: event.target.value })} /><select disabled={readOnly} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" value={values.readinessSource ?? values.readiness_source ?? 'PSSR Impact Assessment'} onChange={(event) => onChange?.({ ...values, readinessSource: event.target.value })}><option>PSSR Impact Assessment</option><option>PSSR Risk Ranking</option><option>PSSR Engineering Package</option><option>PSSR SOP Update</option><option>PSSR PSI Update</option><option>PSSR PSSR Readiness</option><option>PSSR Approval Condition</option><option>Training Matrix</option><option>Required Training Library</option><option>Manual</option></select><select disabled={readOnly} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2 text-sm" value={values.impactLevel ?? values.impact_level ?? 'Medium'} onChange={(event) => onChange?.({ ...values, impactLevel: event.target.value })}><option>Low</option><option>Medium</option><option>High</option><option>Critical</option></select></div><PanelGrid><MiniField label="Site" value={values.site_id ?? values.siteId} /><MiniField label="Unit" value={values.unit_id ?? values.unitId} /><MiniField label="Area" value={values.area_id ?? values.areaId} /></PanelGrid></div>;
}

