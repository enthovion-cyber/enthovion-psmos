import { TrainingCard } from '../../shared/TrainingUi';
export function ApprovalRuleIdentitySection({ value, onChange }: { value: Record<string, any>; onChange: (next: Record<string, any>) => void }) {
  const set = (key: string, val: any) => onChange({ ...value, [key]: val });
  return <TrainingCard title="Rule Identity"><div className="grid gap-3 md:grid-cols-3"><Field label="Rule title" value={value.ruleTitle ?? value.rule_title} onChange={(v) => set('ruleTitle', v)} /><Field label="Rule code" value={value.ruleCode ?? value.rule_code} onChange={(v) => set('ruleCode', v)} /><Field label="Owner user ID" value={value.ownerUserId ?? value.owner_user_id} onChange={(v) => set('ownerUserId', v)} /><Field label="Scope JSON" value={JSON.stringify(value.scope ?? value.scope_json ?? {})} onChange={(v) => set('scope', safeJson(v))} /><Select label="Rule status" value={value.ruleStatus ?? value.rule_status ?? 'Draft'} values={['Draft','Active','Archived']} onChange={(v) => set('ruleStatus', v)} /></div></TrainingCard>;
}
function Field({ label, value, onChange }: { label: string; value?: any; onChange: (v: string) => void }) { return <label className="text-sm font-semibold">{label}<input className="mt-1 w-full rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2" value={value ?? ''} onChange={(e) => onChange(e.target.value)} /></label>; }
function Select({ label, value, values, onChange }: { label: string; value?: any; values: string[]; onChange: (v: string) => void }) { return <label className="text-sm font-semibold">{label}<select className="mt-1 w-full rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2" value={value ?? ''} onChange={(e) => onChange(e.target.value)}>{values.map((v) => <option key={v}>{v}</option>)}</select></label>; }
function safeJson(v: string) { try { return JSON.parse(v || '{}'); } catch { return {}; } }
