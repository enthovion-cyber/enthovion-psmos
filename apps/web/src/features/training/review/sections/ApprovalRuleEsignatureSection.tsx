import { TrainingCard } from '../../shared/TrainingUi';
export function ApprovalRuleEsignatureSection({ value, onChange }: { value: Record<string, any>; onChange: (next: Record<string, any>) => void }) {
  return <TrainingCard title="E-Signature / Locking"><div className="grid gap-3 md:grid-cols-2"><label className="flex items-center gap-2 rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3 text-sm font-semibold"><input type="checkbox" checked={Boolean(value.esignRequired ?? value.esign_required)} onChange={(e) => onChange({ ...value, esignRequired: e.target.checked })} />E-signature required</label><label className="flex items-center gap-2 rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3 text-sm font-semibold"><input type="checkbox" checked={value.lockSourceAfterApproval ?? value.lock_source_after_approval ?? true} onChange={(e) => onChange({ ...value, lockSourceAfterApproval: e.target.checked })} />Lock source after approval</label></div></TrainingCard>;
}
