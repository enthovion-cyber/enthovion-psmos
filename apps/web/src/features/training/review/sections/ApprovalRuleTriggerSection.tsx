import { TrainingCard } from '../../shared/TrainingUi';
export function ApprovalRuleTriggerSection({ value, onChange }: { value: Record<string, any>; onChange: (next: Record<string, any>) => void }) {
  const set = (key: string, val: any) => onChange({ ...value, [key]: val });
  return <TrainingCard title="Trigger & Source"><div className="grid gap-3 md:grid-cols-3"><Select label="Source module" value={value.sourceModule ?? value.source_module} values={['Training Matrix','Roles & Competency','Required Training','Training Records','Certifications','Assessments','SOP Acknowledgements','MOC Training','PSSR Training','PTW Authorization','Reports / Export']} onChange={(v) => set('sourceModule', v)} /><Select label="Source record type" value={value.sourceRecordType ?? value.source_record_type} values={['Matrix rule','Competency profile','Training library item','Safety-critical completion record','Certificate verification','Assessment result verification','SOP acknowledgement waiver','MOC training requirement','PSSR training readiness record','PTW authorization record','Restricted report export']} onChange={(v) => set('sourceRecordType', v)} /><Select label="Trigger event" value={value.triggerEvent ?? value.trigger_event ?? 'Submit'} values={['Create','Submit','Activate','Approve','Verify','Manual Override','Waiver Request','Safety-Critical Change','Major Version Change','Archive','Reopen','Restricted Export','Emergency Override']} onChange={(v) => set('triggerEvent', v)} /><label className="flex items-center gap-2 rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3 text-sm font-semibold"><input type="checkbox" checked={Boolean(value.safetyCriticalOnly ?? value.safety_critical_only)} onChange={(e) => set('safetyCriticalOnly', e.target.checked)} />Safety-critical only</label><Field label="Criticality threshold" value={value.criticalityThreshold ?? value.criticality_threshold} onChange={(v) => set('criticalityThreshold', v)} /></div></TrainingCard>;
}
function Field({ label, value, onChange }: { label: string; value?: any; onChange: (v: string) => void }) { return <label className="text-sm font-semibold">{label}<input className="mt-1 w-full rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2" value={value ?? ''} onChange={(e) => onChange(e.target.value)} /></label>; }
function Select({ label, value, values, onChange }: { label: string; value?: any; values: string[]; onChange: (v: string) => void }) { return <label className="text-sm font-semibold">{label}<select className="mt-1 w-full rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] px-3 py-2" value={value ?? ''} onChange={(e) => onChange(e.target.value)}>{values.map((v) => <option key={v}>{v}</option>)}</select></label>; }
