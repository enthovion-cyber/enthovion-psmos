import { AuditCard, AuditEmptyState } from "../../shared/AuditUi";
export function ScoreHistoryTab({ detail }: { detail: Record<string, any> }) { const rows = detail.history ?? []; return <AuditCard title="Score history events">{rows.length ? <div className="space-y-3">{rows.map((row: any) => <div key={row.id} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3"><b>{row.event_title}</b><p className="text-sm text-[var(--psm-muted)]">{row.event_description}</p><p className="text-xs text-[var(--psm-muted)]">{row.created_at ? new Date(row.created_at).toLocaleString() : ""}</p></div>)}</div> : <AuditEmptyState title="No scoring history" message="Score history events will appear after create, calculate, adjust, verify, lock, unlock, or archive actions." />}</AuditCard>; }
