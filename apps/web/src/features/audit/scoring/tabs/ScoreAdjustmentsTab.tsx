import { AuditCard, AuditEmptyState } from "../../shared/AuditUi";
import { AuditScoreAdjustmentBadge } from "../../shared/AuditScoreAdjustmentBadge";
export function ScoreAdjustmentsTab({ detail }: { detail: Record<string, any> }) { const rows = detail.adjustments ?? []; return <AuditCard title="Manual adjustments" subtitle="Adjusted score keeps original calculated score visible and requires reason/approval per settings.">{rows.length ? <div className="grid gap-3">{rows.map((row: any) => <div key={row.id} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3"><div className="flex justify-between"><b>{row.adjustment_type}</b><AuditScoreAdjustmentBadge value={row.adjustment_status} /></div><p className="text-sm text-[var(--psm-muted)]">{row.adjustment_reason}</p><p className="mt-2 text-sm">Original {row.original_value ?? "-"} / Adjusted {row.adjusted_value ?? "-"}</p></div>)}</div> : <AuditEmptyState title="No score adjustments" message="No manual adjustments have been submitted for this score run." />}</AuditCard>; }
