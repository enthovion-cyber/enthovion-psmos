import { AuditCard, AuditEmptyState } from "../shared/AuditUi";
import { AuditScoreCriticalBlockerBadge } from "../shared/AuditScoreCriticalBlockerBadge";
export function AuditScoreCriticalBlockersPanel({ blockers = [] }: { blockers?: Record<string, any>[] }) { return <AuditCard title="Critical Score Blockers" action={<AuditScoreCriticalBlockerBadge count={blockers.length} />}>{blockers.length ? <div className="space-y-3">{blockers.map((row, index) => <div key={index} className="rounded-lg border border-danger/30 bg-danger/10 p-3"><b>{row.title ?? "Blocker"}</b><p className="text-sm text-[var(--psm-muted)]">{row.reason ?? "Backend scoring blocker requires action."}</p></div>)}</div> : <AuditEmptyState title="No blockers" message="No safety-critical, regulatory-critical, or input blockers were returned by backend scoring readiness." />}</AuditCard>; }
