import { AuditCard } from "../shared/AuditUi";
export function AuditScoreBreakdownPanel({ detail }: { detail: Record<string, any> }) { const run = detail.scoreRun; return <AuditCard title="Score Breakdown" subtitle="Final score, original calculated score, penalties, caps, and backend grade."><div className="grid gap-3 sm:grid-cols-2"><Metric label="Final" value={run.final_score ?? "Not Determined"} /><Metric label="Original" value={run.original_calculated_score ?? "-"} /><Metric label="Points lost" value={run.points_lost ?? "-"} /><Metric label="Penalties" value={run.penalties_total ?? "-"} /></div></AuditCard>; }
function Metric({ label, value }: { label: string; value: unknown }) { return <div className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3"><p className="text-xs uppercase text-[var(--psm-muted)]">{label}</p><b>{String(value)}</b></div>; }
