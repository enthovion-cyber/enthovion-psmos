import { z } from 'zod'; export const auditPlanSchema=z.object({planTitle:z.string().min(1),planCode:z.string().min(1),auditType:z.string().min(1),planCategory:z.string().min(1),criticality:z.string().min(1),programId:z.string().optional(),standaloneAudit:z.boolean().default(false),standaloneReason:z.string().optional(),plannedStartAt:z.string().optional(),plannedEndAt:z.string().optional()}).superRefine((v,c)=>{if(!v.programId&&!v.standaloneAudit)c.addIssue({code:'custom',path:['programId'],message:'Select a program or choose standalone audit.'});if(v.standaloneAudit&&!v.standaloneReason)c.addIssue({code:'custom',path:['standaloneReason'],message:'Standalone reason is required.'});if(v.plannedStartAt&&v.plannedEndAt&&new Date(v.plannedEndAt)<new Date(v.plannedStartAt))c.addIssue({code:'custom',path:['plannedEndAt'],message:'End must be on or after start.'});});
