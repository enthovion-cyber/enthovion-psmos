import { AuditCard, AuditEmptyState } from "../../shared/AuditUi";
export function MappingTraceabilityTab({ detail }: { detail: Record<string, any> }) {
  const chain = detail.latestTraceability?.chain ?? [];
  return <div className="space-y-5"><AuditCard title="Clause-to-evidence traceability chain" subtitle="Standard -> clause -> source -> linked records.">{chain.length ? <div className="grid gap-3">{chain.map((item: Record<string, any>, index: number) => <div key={`${item.type}-${item.id}-${index}`} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3"><div className="text-xs uppercase tracking-[.16em] text-[var(--psm-muted)]">{index + 1}. {item.type}</div><div className="font-semibold">{item.label ?? item.id}</div><div className="text-sm text-[var(--psm-muted)]">{item.title}</div></div>)}</div> : <AuditEmptyState title="No traceability chain" message="Create links to build clause-to-evidence traceability." />}</AuditCard><AuditCard title="Latest traceability payload"><pre className="overflow-auto rounded-lg bg-[var(--psm-surface-2)] p-3 text-xs">{JSON.stringify(detail.latestTraceability ?? {}, null, 2)}</pre></AuditCard></div>;
}
