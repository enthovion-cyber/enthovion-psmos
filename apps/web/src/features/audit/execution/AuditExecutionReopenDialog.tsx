import { AuditCard } from "../shared/AuditUi";

export function AuditExecutionReopenDialog() {
  return <AuditCard title="Reopen Execution">Reopen requires permission and reason through the backend execution reopen API.</AuditCard>;
}
