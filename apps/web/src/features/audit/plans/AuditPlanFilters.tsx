import { Field, inputClass } from '../shared/AuditUi';
export function AuditPlanFilters({ value, onChange }: { value: Record<string, string>; onChange: (next: Record<string, string>) => void }) { const set = (key: string, next: string) => onChange({ ...value, [key]: next }); return <div className="grid gap-3 md:grid-cols-3 xl:grid-cols-6"><Field label="Search"><input className={inputClass()} value={value.search ?? ''} onChange={(e) => set('search', e.target.value)} placeholder="Plan code or title" /></Field><Field label="Plan status"><input className={inputClass()} value={value.planStatus ?? ''} onChange={(e) => set('planStatus', e.target.value)} placeholder="All statuses" /></Field><Field label="Schedule status"><input className={inputClass()} value={value.scheduleStatus ?? ''} onChange={(e) => set('scheduleStatus', e.target.value)} placeholder="All schedules" /></Field><Field label="Audit type"><input className={inputClass()} value={value.auditType ?? ''} onChange={(e) => set('auditType', e.target.value)} placeholder="All types" /></Field><Field label="Criticality"><input className={inputClass()} value={value.criticality ?? ''} onChange={(e) => set('criticality', e.target.value)} placeholder="All levels" /></Field><Field label="Lead auditor"><input className={inputClass()} value={value.leadAuditorUserId ?? ''} onChange={(e) => set('leadAuditorUserId', e.target.value)} placeholder="User ID" /></Field></div>; }
