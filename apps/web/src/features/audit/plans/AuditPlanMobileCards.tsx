import type { AuditPlan } from '../types/audit-plan.types'; import { AuditCard, AuditButton } from '../shared/AuditUi'; import { AuditPlanStatusBadge } from '../shared/AuditPlanStatusBadge'; import { AuditPlanReadinessBadge } from '../shared/AuditPlanReadinessBadge';
export function AuditPlanMobileCards({ rows }: { rows: AuditPlan[] }) { return <div className="space-y-3 lg:hidden">{rows.map((p) => <AuditCard key={p.id}><div className="space-y-3"><div><b>{p.plan_code}</b><p>{p.plan_title}</p></div><div className="flex flex-wrap gap-2"><AuditPlanStatusBadge value={p.plan_status} /><AuditPlanReadinessBadge value={p.readiness_health} /></div><p className="text-sm text-[var(--psm-muted)]">{p.program?.program_title ?? 'Standalone audit'} · {p.audit_type}<br />{p.planned_start_at ? new Date(p.planned_start_at).toLocaleString() : 'Dates not set'}</p><AuditButton href={`/audit-compliance/plans/${p.id}`} variant="secondary">Open plan</AuditButton></div></AuditCard>)}</div>; }
