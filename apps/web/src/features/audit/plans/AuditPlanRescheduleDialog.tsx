'use client'; import { useState } from 'react'; import { AuditCard,AuditButton,Field,inputClass } from '../shared/AuditUi'; export function AuditPlanRescheduleDialog({onSave,onClose}:{onSave:(v:Record<string,unknown>)=>void;onClose:()=>void}){const [v,setV]=useState({plannedStartAt:'',plannedEndAt:'',reason:''});return <AuditCard title="Reschedule audit"><div className="grid gap-3"><Field label="New start"><input type="datetime-local" className={inputClass()} value={v.plannedStartAt} onChange={e=>setV({...v,plannedStartAt:e.target.value})}/></Field><Field label="New end"><input type="datetime-local" className={inputClass()} value={v.plannedEndAt} onChange={e=>setV({...v,plannedEndAt:e.target.value})}/></Field><Field label="Reason *"><textarea className={inputClass()} value={v.reason} onChange={e=>setV({...v,reason:e.target.value})}/></Field><div className="flex gap-2"><AuditButton onClick={()=>onSave(v)} disabled={!v.reason||!v.plannedStartAt||!v.plannedEndAt} title="Dates and reason are required.">Reschedule</AuditButton><AuditButton onClick={onClose} variant="secondary">Cancel</AuditButton></div></div></AuditCard>}
