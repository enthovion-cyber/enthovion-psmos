import type { AuditPlanDetail } from '../../types/audit-plan.types'; import { AuditCard } from '../../shared/AuditUi'; export function PlanProgramLinkTab({detail}:{detail:AuditPlanDetail}){const p=detail.plan;return <AuditCard title="Audit Program Link"><p><b>{p.program?.program_code??'Standalone'} · {p.program?.program_title??p.standalone_reason}</b></p><pre className="mt-4 max-h-96 overflow-auto rounded-lg bg-[var(--psm-surface-2)] p-3 text-xs">{JSON.stringify((p as any).program_snapshot_json??{},null,2)}</pre></AuditCard>}
