import type { AuditPlanDetail } from '../../types/audit-plan.types'; import { PlanSectionManager } from '../PlanSectionManager'; export function PlanStandardsTab({detail}:{detail:AuditPlanDetail}){const p=detail.plan;return <PlanSectionManager planId={p.id} section="standards" title="Standards / Regulations" rows={(p.standards??[]) as any[]} readOnly={['Cancelled','Archived','Completed Foundation','Superseded'].includes(p.plan_status)} fields={[{key:'standardName',label:'Standard name',required:true},{key:'jurisdiction',label:'Jurisdiction'},{key:'clauseReference',label:'Clause reference'},{key:'requirementCategory',label:'Requirement category'},{key:'applicability',label:'Applicability',type:'textarea'},{key:'mandatory',label:'Mandatory',type:'boolean'},{key:'evidenceExpectation',label:'Evidence expectation',type:'textarea'},{key:'sourceFromProgram',label:'From program',type:'boolean'},{key:'notes',label:'Notes',type:'textarea'}]}/>}
