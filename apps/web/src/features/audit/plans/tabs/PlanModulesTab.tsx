import type { AuditPlanDetail } from '../../types/audit-plan.types'; import { PlanSectionManager } from '../PlanSectionManager'; export function PlanModulesTab({detail}:{detail:AuditPlanDetail}){const p=detail.plan;return <PlanSectionManager planId={p.id} section="modules" title="Modules Covered" rows={(p.modules??[]) as any[]} readOnly={['Cancelled','Archived','Completed Foundation','Superseded'].includes(p.plan_status)} fields={[{key:'moduleKey',label:'Module key',required:true},{key:'moduleName',label:'Module name',required:true},{key:'coverageLevel',label:'Coverage level',required:true,type:'select',options:['Full','Partial','Sampling','Evidence Review Only','Field Verification','Interview Only','Document Review','System Audit','Custom']},{key:'coverageReason',label:'Coverage reason',type:'textarea'},{key:'evidenceSource',label:'Evidence source',type:'textarea'},{key:'required',label:'Required',type:'boolean'},{key:'sourceFromProgram',label:'From program',type:'boolean'},{key:'notes',label:'Notes',type:'textarea'}]}/>}
