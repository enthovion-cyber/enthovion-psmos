import { AuditCard, AuditEmptyState } from '../../shared/AuditUi'; export function ListTab({title,rows,fields}:{title:string;rows:Record<string,any>[];fields:string[]}){return <AuditCard title={title}>{rows.length?<div className="grid gap-3 lg:grid-cols-2">{rows.map((row,index)=><div key={row.id??index} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-4">{fields.map(f=><div key={f} className="grid grid-cols-[140px_1fr] gap-2 text-sm"><span className="text-[var(--psm-muted)]">{f.replaceAll('_',' ')}</span><span>{String(row[f]??'-')}</span></div>)}</div>)}</div>:<AuditEmptyState title={`No ${title.toLowerCase()} configured`} message="Add configuration before scheduling this plan."/>}</AuditCard>}
