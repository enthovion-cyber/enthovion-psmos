import type { AuditPlanDetail } from '../../types/audit-plan.types'; import { AuditCard } from '../../shared/AuditUi'; export function PlanScheduleTab({detail}:{detail:AuditPlanDetail}){const p=detail.plan;return <AuditCard title="Schedule"><dl className="grid gap-3 md:grid-cols-2">{[['Planned start',p.planned_start_at],['Planned end',p.planned_end_at],['Timezone',p.timezone],['Location',p.audit_location],['Mode',p.audit_mode],['Duration',p.durationDays?`${p.durationDays} days`:null]].map(([l,v])=><div className="rounded-lg bg-[var(--psm-surface-2)] p-3" key={l}><dt className="text-xs uppercase text-[var(--psm-muted)]">{l}</dt><dd>{String(v??'Not set')}</dd></div>)}</dl></AuditCard>}
