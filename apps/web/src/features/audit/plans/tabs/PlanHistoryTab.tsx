import type { AuditPlanDetail } from '../../types/audit-plan.types'; import { AuditCard, AuditEmptyState } from '../../shared/AuditUi'; export function PlanHistoryTab({detail}:{detail:AuditPlanDetail}){return <AuditCard title="Immutable Plan History">{detail.history.length?<div className="space-y-3">{detail.history.map((e:any)=><div key={e.id} className="border-l-2 border-primary pl-4"><b>{e.event_title}</b><p className="text-sm text-[var(--psm-muted)]">{new Date(e.created_at).toLocaleString()} · {e.event_type}</p><p className="text-sm">{e.event_description}</p></div>)}</div>:<AuditEmptyState title="No plan history" message="Plan mutations will create immutable history and Audit Log entries."/>}</AuditCard>}
