'use client'; import { useState } from 'react'; import { AuditCard,AuditButton,Field,inputClass } from '../shared/AuditUi'; export function AuditPlanCancelDialog({onSave,onClose}:{onSave:(reason:string)=>void;onClose:()=>void}){const [reason,setReason]=useState('');return <AuditCard title="Cancel audit plan"><Field label="Cancellation reason *"><textarea className={inputClass()} value={reason} onChange={e=>setReason(e.target.value)}/></Field><div className="mt-3 flex gap-2"><AuditButton onClick={()=>onSave(reason)} disabled={!reason} title="A cancellation reason is required." variant="danger">Cancel plan</AuditButton><AuditButton onClick={onClose} variant="secondary">Keep plan</AuditButton></div></AuditCard>}
