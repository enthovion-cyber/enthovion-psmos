import type { AuditPlanCheck } from '../types/audit-plan.types'; import { AuditCard, AuditBadge, AuditEmptyState } from '../shared/AuditUi'; export function AuditPlanReadinessPanel({checks}:{checks:AuditPlanCheck[]}){return <AuditCard title="Readiness checklist">{checks.length?<div className="space-y-2">{checks.map(c=><div key={c.id??c.check_key} className="flex items-start justify-between gap-3 rounded-lg bg-[var(--psm-surface-2)] p-3"><div><b>{c.check_label}</b>{c.reason?<p className="text-sm text-[var(--psm-muted)]">{c.reason}</p>:null}</div><AuditBadge tone={c.check_status==='Complete'?'good':c.blocking?'danger':'warn'}>{c.check_status}</AuditBadge></div>)}</div>:<AuditEmptyState title="Readiness not evaluated" message="Run readiness to generate backend checks and exact blockers."/>}</AuditCard>}
