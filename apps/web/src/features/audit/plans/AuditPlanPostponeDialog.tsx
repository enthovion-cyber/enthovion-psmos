'use client'; import { useState } from 'react'; import { AuditCard,AuditButton,Field,inputClass } from '../shared/AuditUi'; export function AuditPlanPostponeDialog({onSave,onClose}:{onSave:(reason:string)=>void;onClose:()=>void}){const [reason,setReason]=useState('');return <AuditCard title="Postpone audit"><Field label="Reason *"><textarea className={inputClass()} value={reason} onChange={e=>setReason(e.target.value)}/></Field><div className="mt-3 flex gap-2"><AuditButton onClick={()=>onSave(reason)} disabled={!reason} title="A reason is required.">Postpone</AuditButton><AuditButton onClick={onClose} variant="secondary">Cancel</AuditButton></div></AuditCard>}
