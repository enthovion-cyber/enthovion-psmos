import type { AuditPlanConflict } from '../types/audit-plan.types'; import { AuditCard, AuditEmptyState } from '../shared/AuditUi'; import { AuditPlanConflictBadge } from '../shared/AuditPlanConflictBadge'; export function AuditPlanConflictPanel({rows}:{rows:AuditPlanConflict[]}){return <AuditCard title="Schedule conflicts">{rows.length?<div className="space-y-2">{rows.map(c=><div key={c.id} className="rounded-lg bg-[var(--psm-surface-2)] p-3"><div className="flex justify-between gap-3"><b>{c.title}</b><AuditPlanConflictBadge value={c.conflict_status}/></div><p className="mt-1 text-sm text-[var(--psm-muted)]">{c.description}</p></div>)}</div>:<AuditEmptyState title="No conflicts detected" message="No unresolved backend schedule conflicts exist for this plan."/>}</AuditCard>}
