import { Field, inputClass } from '../../shared/AuditUi'; export function PlanScheduleSection({ form,set }: any) { return <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3"><Field label="Planned start"><input type="datetime-local" className={inputClass()} value={form.plannedStartAt ?? ''} onChange={(e) => set('plannedStartAt',e.target.value)} /></Field><Field label="Planned end"><input type="datetime-local" className={inputClass()} value={form.plannedEndAt ?? ''} onChange={(e) => set('plannedEndAt',e.target.value)} /></Field><Field label="Timezone"><input className={inputClass()} value={form.timezone ?? 'Asia/Karachi'} onChange={(e) => set('timezone',e.target.value)} /></Field><Field label="Audit location"><input className={inputClass()} value={form.auditLocation ?? ''} onChange={(e) => set('auditLocation',e.target.value)} /></Field><Field label="Audit mode"><input className={inputClass()} value={form.auditMode ?? ''} onChange={(e) => set('auditMode',e.target.value)} /></Field><Field label="Checklist due"><input type="datetime-local" className={inputClass()} value={form.checklistDueAt ?? ''} onChange={(e) => set('checklistDueAt',e.target.value)} /></Field><Field label="Report due"><input type="datetime-local" className={inputClass()} value={form.reportDueAt ?? ''} onChange={(e) => set('reportDueAt',e.target.value)} /></Field><Field label="Reminder days"><input type="number" min="0" className={inputClass()} value={form.reminderDaysBeforeAudit ?? 7} onChange={(e) => set('reminderDaysBeforeAudit',Number(e.target.value))} /></Field><Field label="Grace period days"><input type="number" min="0" className={inputClass()} value={form.gracePeriodDays ?? 0} onChange={(e) => set('gracePeriodDays',Number(e.target.value))} /></Field></div>; }
