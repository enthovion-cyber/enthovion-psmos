import { Field, inputClass } from '../../shared/AuditUi'; export function PlanProgramLinkSection({ form,set,programs=[] }: any) { return <div className="grid gap-4 md:grid-cols-2"><Field label="Audit Program"><select className={inputClass()} value={form.programId ?? ''} onChange={(e) => set('programId',e.target.value)}><option value="">Select program</option>{programs.map((p:any) => <option key={p.id} value={p.id}>{p.program_code} · {p.program_title}</option>)}</select></Field><Field label="Standalone audit"><select className={inputClass()} value={String(form.standaloneAudit ?? false)} onChange={(e) => set('standaloneAudit',e.target.value === 'true')}><option value="false">No</option><option value="true">Yes</option></select></Field>{form.standaloneAudit ? <Field label="Standalone reason *"><textarea className={inputClass()} value={form.standaloneReason ?? ''} onChange={(e) => set('standaloneReason',e.target.value)} /></Field> : null}<label className="flex items-center gap-2"><input type="checkbox" checked={form.copyProgramConfiguration !== false} onChange={(e) => set('copyProgramConfiguration',e.target.checked)} /> Copy approved program scope, standards and modules as a snapshot</label></div>; }
