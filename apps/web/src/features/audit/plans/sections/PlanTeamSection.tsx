import { Field, inputClass } from '../../shared/AuditUi'; export function PlanTeamSection({ form,set,users=[] }: any) { const options = users.map((u:any) => <option key={u.id} value={u.id}>{u.displayName ?? u.name ?? u.email} · {u.email}</option>); return <div className="grid gap-4 md:grid-cols-3"><Field label="Lead auditor"><select className={inputClass()} value={form.leadAuditorUserId ?? ''} onChange={(e) => set('leadAuditorUserId',e.target.value)}><option value="">Select lead auditor</option>{options}</select></Field><Field label="Plan owner"><select className={inputClass()} value={form.ownerUserId ?? ''} onChange={(e) => set('ownerUserId',e.target.value)}><option value="">Select owner</option>{options}</select></Field><Field label="Reviewer / approver"><select className={inputClass()} value={form.reviewerUserId ?? ''} onChange={(e) => set('reviewerUserId',e.target.value)}><option value="">Select reviewer</option>{options}</select></Field></div>; }
