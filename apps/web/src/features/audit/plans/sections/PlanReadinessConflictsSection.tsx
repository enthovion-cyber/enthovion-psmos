import { AuditCard } from '../../shared/AuditUi'; export function PlanReadinessConflictsSection() { return <AuditCard title="Readiness / Conflicts"><p className="text-sm text-[var(--psm-muted)]">The backend checks program, scope, standards, modules, lead auditor, team, dates, approval, auditor overlap and site overlap. Hard conflicts block scheduling unless an authorized override is recorded with a reason.</p></AuditCard>; }
