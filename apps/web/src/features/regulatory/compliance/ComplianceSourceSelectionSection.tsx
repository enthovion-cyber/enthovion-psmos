import { RegulatoryField, regulatoryInputClass } from '../shared/RegulatoryUi';
import type { RegulatoryComplianceLookups } from '../types/regulatory-compliance.types';

export function ComplianceSourceSelectionSection({ form, setForm, lookups }: { form: Record<string, unknown>; setForm: (form: Record<string, unknown>) => void; lookups?: RegulatoryComplianceLookups | undefined }) {
  return <div className="grid gap-3 md:grid-cols-2"><RegulatoryField label="Source type" helper="Use a real regulatory item or obligation ID."><select className={regulatoryInputClass()} value={String(form.sourceType ?? 'Regulatory Item')} onChange={(event) => setForm({ ...form, sourceType: event.target.value })}>{lookups?.complianceSourceTypes?.map((value) => <option key={value}>{value}</option>)}</select></RegulatoryField><RegulatoryField label="Source record ID"><input className={regulatoryInputClass()} value={String(form.sourceRecordId ?? '')} onChange={(event) => setForm({ ...form, sourceRecordId: event.target.value })} placeholder="Regulatory item or obligation ID" /></RegulatoryField><RegulatoryField label="Assessment title"><input className={regulatoryInputClass()} value={String(form.assessmentTitle ?? '')} onChange={(event) => setForm({ ...form, assessmentTitle: event.target.value })} /></RegulatoryField><RegulatoryField label="Owner user ID"><input className={regulatoryInputClass()} value={String(form.ownerUserId ?? '')} onChange={(event) => setForm({ ...form, ownerUserId: event.target.value })} /></RegulatoryField></div>;
}
