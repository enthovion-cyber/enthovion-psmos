import { RegulatoryField, regulatoryInputClass } from '../shared/RegulatoryUi';
import type { RegulatoryComplianceLookups } from '../types/regulatory-compliance.types';

export function ComplianceStatusDecisionSection({ form, setForm, lookups }: { form: Record<string, unknown>; setForm: (form: Record<string, unknown>) => void; lookups?: RegulatoryComplianceLookups | undefined }) {
  return <div className="grid gap-3 md:grid-cols-2"><RegulatoryField label="Compliance status"><select className={regulatoryInputClass()} value={String(form.complianceStatus ?? 'Not Assessed')} onChange={(event) => setForm({ ...form, complianceStatus: event.target.value })}>{lookups?.complianceStatuses?.map((value) => <option key={value}>{value}</option>)}</select></RegulatoryField><RegulatoryField label="Status rationale"><textarea className={regulatoryInputClass()} value={String(form.statusRationale ?? '')} onChange={(event) => setForm({ ...form, statusRationale: event.target.value })} /></RegulatoryField><label className="flex items-center gap-2 text-sm text-[var(--psm-fg)]"><input type="checkbox" checked={Boolean(form.manualDeclaration)} onChange={(event) => setForm({ ...form, manualDeclaration: event.target.checked })} />Manual declaration</label><RegulatoryField label="Manual declaration reason"><input className={regulatoryInputClass()} value={String(form.manualDeclarationReason ?? '')} onChange={(event) => setForm({ ...form, manualDeclarationReason: event.target.value })} /></RegulatoryField></div>;
}
