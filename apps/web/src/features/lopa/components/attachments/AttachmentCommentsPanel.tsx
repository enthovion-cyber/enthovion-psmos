import { AttachmentPanel } from './AttachmentPanel'; export function AttachmentCommentsPanel({ comments }: { comments:any[] }) { return <AttachmentPanel title="Attachment Comments / Notes"><div className="space-y-2">{comments.map(row=><div className="rounded border border-slate-200 p-2 text-xs dark:border-slate-800" key={row.id}><b>{row.comment_type}</b><p className="mt-1 text-slate-600 dark:text-slate-300">{row.comment_text}</p><p className="mt-1 text-slate-500">{row.created_at}</p></div>)}{!comments.length?<p className="text-xs text-slate-500">No comments or notes.</p>:null}</div></AttachmentPanel>; }
