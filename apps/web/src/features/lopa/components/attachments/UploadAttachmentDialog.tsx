'use client'; import { useState } from 'react';
const input='w-full rounded border border-slate-300 bg-white px-3 py-2 text-sm dark:border-slate-700 dark:bg-[#0a1b2e]';
export function UploadAttachmentDialog({ context, busy, onClose, onSubmit }: any) { const [files,setFiles]=useState<File[]>([]); const [v,setV]=useState<any>({attachmentType:'Evidence File',evidenceCategory:'General',relatedTab:'General',classification:'Public/Internal',accessLevel:'Study team',requiredEvidence:false}); const set=(key:string,value:any)=>setV((old:any)=>({...old,[key]:value})); return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4"><form className="max-h-[92vh] w-full max-w-3xl overflow-auto rounded-lg bg-white p-5 dark:bg-[#071525]" onSubmit={e=>{e.preventDefault();onSubmit(files,v)}}><h3 className="font-bold text-slate-900 dark:text-white">Upload Attachment</h3><p className="mt-1 text-xs text-slate-500">Files are stored through the secure storage provider and metadata is auditable.</p><div className="mt-4 grid gap-3 md:grid-cols-2"><label className="md:col-span-2"><span className="text-xs">Files *</span><input className={input} type="file" multiple onChange={e=>setFiles(Array.from(e.target.files??[]))} required/></label>{[['attachmentType','Attachment type',context.attachmentTypes],['evidenceCategory','Evidence category',context.evidenceCategories],['relatedTab','Related section',context.relatedTabs],['classification','Classification',context.classifications],['accessLevel','Access level',context.accessLevels]].map(([key,label,options]:any)=><label key={key}><span className="text-xs">{label}</span><select className={input} value={v[key]} onChange={e=>set(key,e.target.value)}>{options?.map((x:string)=><option key={x}>{x}</option>)}</select></label>)}<label><span className="text-xs">Description</span><input className={input} value={v.description??''} onChange={e=>set('description',e.target.value)}/></label><label><span className="text-xs">Evidence purpose</span><input className={input} value={v.evidencePurpose??''} onChange={e=>set('evidencePurpose',e.target.value)}/></label><label><span className="text-xs">Source/reference</span><input className={input} value={v.sourceReference??''} onChange={e=>set('sourceReference',e.target.value)}/></label><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={v.requiredEvidence} onChange={e=>set('requiredEvidence',e.target.checked)}/> Mark as required evidence</label></div><div className="mt-5 flex justify-end gap-2"><button type="button" className="lopa-button-secondary" onClick={onClose}>Cancel</button><button className="lopa-button-primary" disabled={busy||!files.length}>{busy?'Uploading...':files.length>1?'Upload files':'Upload file'}</button></div></form></div>; }
