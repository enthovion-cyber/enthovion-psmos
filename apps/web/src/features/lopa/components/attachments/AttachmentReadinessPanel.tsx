import { AttachmentPanel } from './AttachmentPanel'; import { EvidenceStatusBadge } from '../shared/EvidenceStatusBadge';
export function AttachmentReadinessPanel({ readiness }: { readiness:any }) { return <AttachmentPanel title="Attachment Readiness" subtitle="Feeds Overview and Review & Sign-Off."><div className="mb-3"><EvidenceStatusBadge status={readiness.status}/></div><div className="space-y-2">{(readiness.checklist??[]).map((row:any)=><div className="flex items-center justify-between gap-3 text-xs" key={row.key??row.label}><span className="text-slate-700 dark:text-slate-300">{row.label}</span><EvidenceStatusBadge status={row.status}/></div>)}</div></AttachmentPanel>; }
