import { AttachmentPanel } from './AttachmentPanel'; import { EvidenceStatusBadge } from '../shared/EvidenceStatusBadge';
export function RequiredEvidencePanel({ rows, onSelect }: { rows:any[]; onSelect:(row:any)=>void }) { return <AttachmentPanel title="Required Evidence / Missing Attachments" subtitle="Backend-generated requirements that feed review readiness."><div className="space-y-2">{rows.map(row=><button className="flex w-full items-center justify-between gap-3 rounded border border-slate-200 p-2 text-left text-xs dark:border-slate-800" key={row.key??row.id} onClick={()=>onSelect(row)}><div><p className="font-semibold text-slate-800 dark:text-slate-200">{row.label}</p><p className="text-slate-500">{row.tab} · {row.linked??'No evidence linked'}</p></div><EvidenceStatusBadge status={row.status}/></button>)}{!rows.length?<p className="text-xs text-slate-500">No evidence requirements apply.</p>:null}</div></AttachmentPanel>; }
