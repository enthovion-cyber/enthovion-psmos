import { AttachmentPanel } from './AttachmentPanel'; import { AttachmentStatusBadge } from '../shared/AttachmentStatusBadge'; export function AttachmentVersionHistoryPanel({ versions }: { versions:any[] }) { return <AttachmentPanel title="Attachment Version History"><div className="space-y-2">{versions.map(row=><div key={row.id} className="flex items-center justify-between gap-2 rounded border border-slate-200 p-2 text-xs dark:border-slate-800"><div><b>{row.revision??`v${row.version}`}</b> · {row.file_name}<p className="text-slate-500">{row.change_reason??'No change reason'} · {row.uploaded_at}</p></div><AttachmentStatusBadge status={row.status}/></div>)}{!versions.length?<p className="text-xs text-slate-500">No preserved versions.</p>:null}</div></AttachmentPanel>; }
