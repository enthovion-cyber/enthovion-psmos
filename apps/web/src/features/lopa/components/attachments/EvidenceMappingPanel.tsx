import { AttachmentPanel } from './AttachmentPanel'; import { EvidenceStatusBadge } from '../shared/EvidenceStatusBadge'; export function EvidenceMappingPanel({ mappings }: { mappings:any[] }) { return <AttachmentPanel title="Evidence Mapping"><div className="space-y-2">{mappings.map(row=><div className="rounded border border-slate-200 p-2 text-xs dark:border-slate-800" key={row.id}><div className="flex justify-between gap-2"><b>{row.related_tab}</b><EvidenceStatusBadge status={row.required?'Required':'Supporting'}/></div><p className="mt-1 text-slate-500">{row.evidence_purpose} · {row.related_record_type??'Study level'}</p></div>)}{!mappings.length?<p className="text-xs text-slate-500">No evidence mappings.</p>:null}</div></AttachmentPanel>; }
