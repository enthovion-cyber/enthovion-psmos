import { AttachmentPanel } from './AttachmentPanel'; import { AttachmentStatusBadge } from '../shared/AttachmentStatusBadge'; export function ControlledDocumentLinksPanel({ links, onRefresh }: any) { return <AttachmentPanel title="Controlled Document Links" subtitle="Revision and status are stored as a snapshot at link time."><div className="space-y-2">{links.map((row:any)=><div className="flex items-center justify-between gap-2 rounded border border-slate-200 p-2 text-xs dark:border-slate-800" key={row.id}><div><b>{row.document_number_snapshot}</b> · {row.document_title_snapshot}<p className="text-slate-500">Rev {row.document_revision_snapshot??'-'} · {row.relationship??'Supporting evidence'}</p></div><div className="flex gap-2"><AttachmentStatusBadge status={row.document_status_snapshot}/><button className="text-cyan-600" onClick={()=>onRefresh(row.id)}>Refresh</button></div></div>)}{!links.length?<p className="text-xs text-slate-500">No controlled documents linked.</p>:null}</div></AttachmentPanel>; }
