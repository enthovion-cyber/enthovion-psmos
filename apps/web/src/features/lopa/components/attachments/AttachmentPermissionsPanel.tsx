import { AttachmentPanel } from './AttachmentPanel'; export function AttachmentPermissionsPanel({ attachment }: { attachment:any }) { return <AttachmentPanel title="Permissions / Classification"><dl className="grid grid-cols-2 gap-3 text-xs">{[['Classification',attachment?.classification],['Access level',attachment?.access_level],['Restricted',attachment?.restricted],['Confidential',attachment?.confidential],['External sharing',attachment?.external_sharing_allowed],['Retention category',attachment?.retention_category],['Owner',attachment?.owner_id],['Permission notes',attachment?.permission_notes]].map(([label,value])=><div key={String(label)}><dt className="text-slate-500">{label}</dt><dd className="mt-1 font-semibold text-slate-800 dark:text-slate-200">{value==null||value===''?'Not set':String(value)}</dd></div>)}</dl></AttachmentPanel>; }
