import { Search } from 'lucide-react';
export function AttachmentFilters({ filters, context, onChange }: any) { const field=(key:string,label:string,items:string[])=><select className="rounded border border-slate-300 bg-white px-2 py-2 text-xs dark:border-slate-700 dark:bg-[#071525]" value={filters[key]??''} onChange={e=>onChange({...filters,[key]:e.target.value||undefined})}><option value="">{label}</option>{items.map(item=><option key={item}>{item}</option>)}</select>; return <div className="flex flex-wrap gap-2"><div className="relative"><Search className="absolute left-2 top-2 h-4 w-4 text-slate-500"/><input className="rounded border border-slate-300 bg-white py-2 pl-8 pr-2 text-xs dark:border-slate-700 dark:bg-[#071525]" placeholder="Search file, document, tag, note..." value={filters.q??''} onChange={e=>onChange({...filters,q:e.target.value||undefined})}/></div>{field('attachmentType','All types',context.attachmentTypes??[])}{field('evidenceCategory','All evidence',context.evidenceCategories??[])}{field('relatedTab','All sections',context.relatedTabs??[])}{field('status','All statuses',context.statuses??[])}{field('classification','All classifications',context.classifications??[])}{field('accessLevel','All access levels',context.accessLevels??[])}</div>; }
