import { HistoryPanel } from './HistoryPanel'; export function WorkflowApprovalTimelinePanel({rows,onOpen}:any){return <HistoryPanel title="Workflow / Approval Timeline"><div className="space-y-2">{rows.map((row:any)=><button className="w-full rounded border border-slate-200 p-2 text-left text-xs dark:border-slate-800" key={row.id} onClick={()=>onOpen(row)}><b>{row.title}</b><p className="text-slate-500">{row.event_type} · {row.created_at}</p></button>)}{!rows.length?<p className="text-xs text-slate-500">No workflow or approval events.</p>:null}</div></HistoryPanel>;}
