import { HistoryPanel } from './HistoryPanel'; export function ModuleActivityBreakdownPanel({rows,onFilter}:any){return <HistoryPanel title="Module Activity Breakdown"><div className="space-y-2">{rows.map((row:any)=><button className="flex w-full justify-between rounded border border-slate-200 p-2 text-left text-xs dark:border-slate-800" key={row.module} onClick={()=>onFilter(row.module)}><span>{row.module}</span><span className="font-bold">{row.total} events · {row.critical} critical</span></button>)}{!rows.length?<p className="text-xs text-slate-500">No module activity.</p>:null}</div></HistoryPanel>;}
