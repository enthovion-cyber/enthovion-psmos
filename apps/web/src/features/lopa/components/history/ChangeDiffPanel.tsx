import { HistoryPanel } from './HistoryPanel'; export function ChangeDiffPanel({ diff }: { diff:any }) { return <HistoryPanel title="Change Diff / Before-After">{diff?.rows?.length?<div className="space-y-2">{diff.rows.map((row:any)=><div className="grid gap-2 rounded border border-slate-200 p-2 text-xs md:grid-cols-3 dark:border-slate-800" key={row.field}><b>{row.field}</b><span className="break-words text-red-600 dark:text-red-300">{JSON.stringify(row.previousValue)}</span><span className="break-words text-emerald-600 dark:text-emerald-300">{JSON.stringify(row.newValue)}</span></div>)}</div>:<p className="text-xs text-slate-500">No field-level diff available.</p>}</HistoryPanel>;}
