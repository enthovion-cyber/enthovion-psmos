import { Search } from 'lucide-react'; export function HistoryFilters({filters,context,onChange}:any){const select=(key:string,label:string,values:string[]) => <select className="rounded border border-slate-300 bg-white px-2 py-2 text-xs dark:border-slate-700 dark:bg-[#071525]" value={filters[key]??''} onChange={e=>onChange({...filters,[key]:e.target.value||undefined})}><option value="">{label}</option>{values.map(x=><option key={x}>{x}</option>)}</select>; return <div className="flex flex-wrap gap-2"><div className="relative"><Search className="absolute left-2 top-2 h-4 w-4 text-slate-500"/><input className="rounded border border-slate-300 bg-white py-2 pl-8 pr-2 text-xs dark:border-slate-700 dark:bg-[#071525]" placeholder="Search event, actor, record, reason..." value={filters.q??''} onChange={e=>onChange({...filters,q:e.target.value||undefined})}/></div>{select('relatedTab','All modules',context.modules??[])}{select('eventType','All event types',context.eventTypes??[])}{select('severity','All severities',context.severities??[])}</div>;}
