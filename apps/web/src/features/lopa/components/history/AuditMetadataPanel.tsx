import { HistoryPanel } from './HistoryPanel'; export function AuditMetadataPanel({ metadata }: { metadata:any }) { return <HistoryPanel title="Audit Metadata">{metadata?.restricted?<p className="text-xs text-red-600 dark:text-red-300">Audit metadata is restricted.</p>:metadata?<dl className="grid grid-cols-2 gap-3 text-xs">{Object.entries(metadata).map(([key,value])=><div key={key}><dt className="text-slate-500">{key}</dt><dd className="mt-1 break-words font-semibold text-slate-800 dark:text-slate-200">{typeof value==='object'?JSON.stringify(value):String(value??'Not set')}</dd></div>)}</dl>:<p className="text-xs text-slate-500">Select an event to load audit metadata.</p>}</HistoryPanel>;}
