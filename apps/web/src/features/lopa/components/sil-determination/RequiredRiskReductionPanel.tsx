import { SilInfoGrid, SilPanel } from './SilPanel';
export function RequiredRiskReductionPanel({ calculation, determination }: { calculation: any; determination: any }) { return <SilPanel title="Required Risk Reduction" subtitle="Backend result from the approved Risk Calculation snapshot"><SilInfoGrid items={[["Mitigated event frequency", calculation?.mitigated_event_frequency],["Tolerable frequency", calculation?.tolerable_frequency],["Risk gap factor", calculation?.risk_gap_factor],["Required additional RRF", determination?.required_rrf ?? calculation?.required_additional_rrf],["Required PFDavg", determination?.required_pfdavg],["Risk criteria met", calculation?.meets_risk_criteria]]}/></SilPanel>; }
