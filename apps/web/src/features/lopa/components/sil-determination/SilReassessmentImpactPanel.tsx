import { AlertTriangle } from 'lucide-react'; import { SilPanel, SilStatus } from './SilPanel';
export function SilReassessmentImpactPanel({ events }: { events: any[] }) { return <SilPanel title="Reassessment / Change Impact"><div className="space-y-2">{events.map(row => <div key={row.id} className="flex gap-3 rounded border border-amber-500/30 bg-amber-500/5 p-3"><AlertTriangle className="h-4 w-4 shrink-0 text-amber-500"/><div className="min-w-0 text-xs"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-slate-800 dark:text-slate-200">{row.changed_item ?? row.trigger_type}</p><SilStatus value={row.status}/></div><p className="mt-1 text-slate-500">{row.impact_summary ?? row.trigger_description}</p></div></div>)}{!events.length ? <p className="py-4 text-center text-xs text-slate-500">No pending reassessment event.</p> : null}</div></SilPanel>; }
