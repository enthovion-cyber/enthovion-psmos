import { SilInfoGrid, SilPanel, SilStatus } from './SilPanel';
export function SifArchitectureVotingPanel({ architecture }: { architecture: any }) { return <SilPanel title="SIF Architecture / Voting"><div className="mb-3"><SilStatus value={architecture?.architecture_status ?? 'Not Started'}/></div><SilInfoGrid items={[["Sensor voting", architecture?.sensor_voting],["Logic solver", architecture?.logic_solver_architecture ?? architecture?.logic_solver_voting],["Final element voting", architecture?.final_element_voting],["Overall architecture", architecture?.overall_architecture],["Redundancy", architecture?.redundancy_requirement],["Fault tolerance", architecture?.fault_tolerance],["Diagnostic coverage", architecture?.diagnostic_coverage_assumption ?? architecture?.diagnostic_coverage],["Beta factor", architecture?.beta_factor],["BPCS independence", architecture?.independence_from_bpcs],["Initiating-event independence", architecture?.independence_from_initiating_event],["Other IPL independence", architecture?.independence_from_other_ipls],["Common-cause basis", architecture?.common_cause_consideration]]}/></SilPanel>; }
