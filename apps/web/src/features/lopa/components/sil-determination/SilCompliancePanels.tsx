export function SilCompliancePanels({ gaps, links, actions, snapshots, reassessments }: { gaps: any[]; links: any[]; actions: any[]; snapshots: any[]; reassessments: any[] }) {
  return <div className="grid grid-cols-1 gap-4 xl:grid-cols-2"><Panel title="IEC 61511 Gaps" rows={gaps} empty="No IEC gaps have been generated." fields={['requirement_title', 'status', 'evidence_reference']} /><Panel title="SIF Links & Evidence" rows={links} empty="No linked records." fields={['record_type', 'record_title', 'status']} /><Panel title="SIL / SIF Actions" rows={actions} empty="No linked Universal Actions." fields={['title', 'status', 'closure_blocker']} /><Panel title="Snapshots / Reassessment" rows={[...reassessments, ...snapshots]} empty="No SIL snapshot or reassessment event." fields={['trigger_description', 'snapshot_type', 'status']} /></div>;
}
function Panel({ title, rows, empty, fields }: { title: string; rows: any[]; empty: string; fields: string[] }) { return <section className="rounded-xl border border-cyan-300/10 bg-[#071525] p-4"><h3 className="font-bold text-white">{title}</h3><div className="mt-3 space-y-2">{rows.slice(0, 6).map((row: any) => <div key={row.id} className="rounded-lg border border-slate-800 p-2 text-xs text-slate-300">{fields.map((f) => row[f] ? <span key={f} className="mr-2">{String(row[f])}</span> : null)}</div>)}{!rows.length ? <p className="text-sm text-slate-500">{empty}</p> : null}</div></section>; }
