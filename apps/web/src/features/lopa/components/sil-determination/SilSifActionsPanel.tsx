import { Plus } from 'lucide-react'; import { SilPanel, SilStatus } from './SilPanel';
export function SilSifActionsPanel({ actions, readOnly, onAdd }: { actions: any[]; readOnly: boolean; onAdd: () => void }) { return <SilPanel title="SIL / SIF Actions" subtitle="Universal Action Engine is the source of truth" action={!readOnly ? <button className="lopa-button-secondary" onClick={onAdd}><Plus size={14}/> Create Action</button> : null}><div className="space-y-2">{actions.map(row => <div key={row.id} className="flex items-center justify-between rounded border border-slate-200 p-2 text-xs dark:border-slate-800"><div><p className="font-semibold text-slate-800 dark:text-slate-200">{row.title}</p><p className="text-slate-500">{row.action?.owner?.name ?? row.action?.ownerId ?? 'Owner not assigned'} · {row.action?.dueDate ?? 'No due date'}</p></div><SilStatus value={row.status}/></div>)}{!actions.length ? <p className="py-4 text-center text-xs text-slate-500">No linked SIL/SIF actions.</p> : null}</div></SilPanel>; }
