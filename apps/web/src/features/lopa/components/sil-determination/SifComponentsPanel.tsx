import { SilPanel, SilStatus } from './SilPanel';
export function SifComponentsPanel({ components }: { components: any[] }) { const groups = ['Sensor','Logic Solver','Final Element','Auxiliary']; return <SilPanel title="Sensors / Logic Solver / Final Elements"><div className="grid gap-3 md:grid-cols-2">{groups.map(type => <div key={type} className="rounded border border-slate-200 p-3 dark:border-slate-800"><div className="mb-2 text-xs font-bold text-slate-800 dark:text-slate-200">{type}</div>{components.filter(row => row.component_type === type).map(row => <div key={row.id} className="mb-2 text-xs text-slate-600 dark:text-slate-300"><div className="flex justify-between gap-2"><span className="font-semibold">{row.component_tag ?? row.tag ?? 'Unassigned'} · {row.component_name ?? row.name ?? type}</span><SilStatus value={row.status}/></div><div className="mt-1 text-slate-500">{row.process_variable ?? row.action_on_trip ?? row.failure_data_reference ?? 'Basis not entered'}</div></div>)}{!components.some(row => row.component_type === type) ? <p className="text-xs text-slate-500">No {type.toLowerCase()} specified.</p> : null}</div>)}</div></SilPanel>; }
