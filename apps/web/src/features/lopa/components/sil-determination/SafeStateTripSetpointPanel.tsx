import { SilInfoGrid, SilPanel } from './SilPanel';
export function SafeStateTripSetpointPanel({ sif }: { sif: any }) { return <SilPanel title="Safe State / Action / Trip Setpoint"><SilInfoGrid items={[["Safe state", sif?.safe_state],["Required action", sif?.required_action ?? sif?.process_action],["Trip setpoint", [sif?.trip_setpoint,sif?.trip_setpoint_units].filter(Boolean).join(' ')],["Setpoint basis", sif?.setpoint_basis],["Response time required", sif?.response_time_required ?? sif?.response_time],["Response time available", sif?.response_time_available],["Reset requirement", sif?.reset_requirement],["Reset philosophy", sif?.reset_philosophy],["Manual reset required", sif?.manual_reset_required],["Operating mode", sif?.operating_mode],["Demand mode", sif?.demand_mode]]}/></SilPanel>; }
