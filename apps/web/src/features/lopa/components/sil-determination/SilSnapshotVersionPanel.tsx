import { GitCompareArrows } from 'lucide-react'; import { SilPanel, SilStatus } from './SilPanel';
export function SilSnapshotVersionPanel({ snapshots, onCompare }: { snapshots: any[]; onCompare: (id: string) => void }) { return <SilPanel title="SIL Determination Version / Snapshot"><div className="space-y-2">{snapshots.map(row => <div key={row.id} className="flex items-center justify-between gap-3 rounded border border-slate-200 p-2 text-xs dark:border-slate-800"><div><p className="font-semibold text-slate-800 dark:text-slate-200">Version {row.snapshot_version ?? row.version_number} · {row.snapshot_type}</p><p className="text-slate-500">{row.created_at} · {row.snapshot_hash}</p></div><div className="flex items-center gap-2"><SilStatus value={row.snapshot_status ?? row.status}/><button className="text-cyan-600" title="Compare snapshot" onClick={() => onCompare(row.id)}><GitCompareArrows size={16}/></button></div></div>)}{!snapshots.length ? <p className="py-4 text-center text-xs text-slate-500">No immutable snapshots exist.</p> : null}</div></SilPanel>; }
