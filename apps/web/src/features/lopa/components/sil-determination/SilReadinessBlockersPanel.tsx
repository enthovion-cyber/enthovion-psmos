export function SilReadinessBlockersPanel({ readiness }: { readiness: any }) {
  return <section className="rounded-xl border border-cyan-300/10 bg-[#071525] p-4"><div className="flex items-center justify-between"><h3 className="font-bold text-white">SIL Readiness / Blockers</h3><span className="text-xs text-slate-400">{readiness?.percent ?? 0}% ready</span></div><div className="mt-3 space-y-2">{(readiness?.checks ?? []).map((check: any) => <div key={check.key} className="flex items-center justify-between border-b border-slate-800 py-2 text-xs"><span className="text-slate-300">{check.label}</span><span className={check.complete ? 'text-emerald-300' : check.status === 'Blocked' ? 'text-red-300' : 'text-amber-300'}>{check.status}</span></div>)}{!(readiness?.checks ?? []).length ? <p className="text-sm text-slate-500">Readiness is generated once the SIL schema is available.</p> : null}</div>{(readiness?.blockers ?? []).length ? <div className="mt-3 rounded-lg border border-red-400/20 bg-red-500/10 p-3 text-xs text-red-100">{readiness.blockers.map((b: any) => <div key={b.id}>{b.title}</div>)}</div> : null}</section>;
}
