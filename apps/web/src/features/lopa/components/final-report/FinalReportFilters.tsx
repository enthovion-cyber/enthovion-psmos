import type { LopaFinalReportFilters } from '../../types/lopa-final-report.types';
export function FinalReportFilters({ filters, setFilters }: { filters:LopaFinalReportFilters; setFilters:(f:LopaFinalReportFilters)=>void }) { return <div className="grid gap-2 rounded-lg border border-slate-200 bg-white p-3 dark:border-cyan-300/10 dark:bg-[#071525] md:grid-cols-5"><input className="rounded border border-slate-200 bg-transparent px-2 py-2 text-xs dark:border-cyan-300/10" placeholder="Search reports..." value={filters.q??''} onChange={e=>setFilters({...filters,q:e.target.value})}/>{['status','reportType','outputFormat','classification'].map(k=><input key={k} className="rounded border border-slate-200 bg-transparent px-2 py-2 text-xs dark:border-cyan-300/10" placeholder={k} value={(filters as any)[k]??''} onChange={e=>setFilters({...filters,[k]:e.target.value})}/>)}</div>; }
