import { FinalReportPanel } from './FinalReportPanel';
export function OfficialReportPublishPanel({ reports, onOfficial, onPublish }: { reports:any[]; onOfficial:(r:any)=>void; onPublish:(r:any)=>void }) { const latest=reports.find((r:any)=>r.official)||reports[0]; return <FinalReportPanel title="Official Report / Publish to Document Control" subtitle="Official reports are immutable and publishing uses Document Control metadata.">{latest?<div className="flex flex-wrap items-center justify-between gap-3"><div className="text-sm"><b>{latest.report_number}</b><div className="text-slate-500">{latest.report_title} · {latest.status}</div></div><div className="flex gap-2"><button className="lopa-button-secondary" onClick={()=>onOfficial(latest)}>Mark Official</button><button className="lopa-button-primary" onClick={()=>onPublish(latest)}>Publish</button></div></div>:<div className="text-sm text-slate-500">Generate a report before publishing.</div>}</FinalReportPanel>; }
