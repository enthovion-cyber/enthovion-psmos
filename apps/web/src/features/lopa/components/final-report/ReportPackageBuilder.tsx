import { FinalReportPanel } from './FinalReportPanel';
export function ReportPackageBuilder({ packages, appendices, onBuild }: { packages:any[]; appendices:any[]; onBuild:()=>void }) {
  return <FinalReportPanel title="Report Package Builder" subtitle="Build complete packages with report, appendices, indexes, snapshots, and selected evidence." action={<button className="lopa-button-primary" onClick={onBuild}>Build Package</button>}><div className="grid gap-3 lg:grid-cols-2"><div className="space-y-2">{appendices.map((a:any)=><div key={a.key} className="flex justify-between rounded border border-slate-200 p-2 text-xs dark:border-cyan-300/10"><span>{a.title}</span><span>{a.status}</span></div>)}</div><div className="space-y-2">{packages.length?packages.slice(0,5).map((p:any)=><div key={p.id} className="rounded border border-slate-200 p-2 text-xs dark:border-cyan-300/10"><b>{p.package_number}</b> · {p.package_name}<div className="text-slate-500">{p.status} · {p.file_size ?? 0} bytes</div></div>):<div className="text-sm text-slate-500">No packages generated yet.</div>}</div></div></FinalReportPanel>;
}
