import { FinalReportPanel } from './FinalReportPanel';
export function ReportDistributionPanel({ rows, onShare }: { rows:any[]; onShare:()=>void }) { return <FinalReportPanel title="Report Distribution / Sharing" subtitle="Secure distribution records and share status." action={<button className="lopa-button-secondary" onClick={onShare}>Share</button>}><div className="space-y-2">{rows.length?rows.map((r:any)=><div key={r.id} className="rounded border border-slate-200 p-2 text-xs dark:border-cyan-300/10"><b>{r.recipient_email}</b><div className="text-slate-500">{r.distribution_method} · {r.status} · {r.shared_at?new Date(r.shared_at).toLocaleString():'-'}</div></div>):<div className="text-sm text-slate-500">No distribution records.</div>}</div></FinalReportPanel>; }
