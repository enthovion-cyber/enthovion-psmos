export function PublishToDocumentControlDialog({ open, onClose, onSubmit }: { open:boolean; onClose:()=>void; onSubmit:(values:any)=>void }) { if(!open) return null; const values:any={}; return <div className="fixed inset-0 z-50 grid place-items-center bg-black/50 p-4"><div className="w-full max-w-md rounded-xl border border-cyan-300/10 bg-white p-5 dark:bg-[#071525]"><h3 className="text-lg font-black text-slate-950 dark:text-white">Publish to Document Control</h3>{['documentTitle','documentNumber','classification','reason'].map(k=><input key={k} className="mt-3 w-full rounded border p-2 text-sm dark:border-cyan-300/10 dark:bg-[#06111f]" placeholder={k} onChange={e=>values[k]=e.target.value}/>) }<div className="mt-4 flex justify-end gap-2"><button className="lopa-button-secondary" onClick={onClose}>Cancel</button><button className="lopa-button-primary" onClick={()=>onSubmit(values)}>Publish</button></div></div></div>; }
