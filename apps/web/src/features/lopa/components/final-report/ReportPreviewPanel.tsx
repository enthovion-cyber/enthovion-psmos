import { FinalReportPanel } from './FinalReportPanel';
export function ReportPreviewPanel({ preview, onGenerate }: { preview:any; onGenerate:()=>void }) {
  return <FinalReportPanel title="Report Preview Panel" subtitle="Backend-generated preview metadata, selected sections, redactions, and draft watermark." action={<button className="lopa-button-secondary" onClick={onGenerate}>Refresh preview</button>}><div className="rounded-lg border border-slate-200 bg-slate-50 p-3 dark:border-cyan-300/10 dark:bg-[#06111f]"><div className="text-lg font-black text-slate-950 dark:text-white">{preview?.title}</div><div className="mt-1 text-xs text-slate-500">{preview?.watermark} · {preview?.generatedAt ? new Date(preview.generatedAt).toLocaleString() : '-'}</div><div className="mt-3 grid gap-2 md:grid-cols-2">{(preview?.pageStructure ?? []).slice(0,8).map((p:any)=><div key={`${p.page}-${p.section}`} className="rounded border border-slate-200 bg-white p-2 text-xs dark:border-cyan-300/10 dark:bg-[#071525]"><b>{p.page}. {p.section}</b><div className="text-slate-500">{p.sourceModule} · {p.status}</div></div>)}</div>{preview?.redaction?.warnings?.length ? <div className="mt-3 rounded border border-amber-400/25 bg-amber-500/10 p-2 text-xs text-amber-700 dark:text-amber-100">{preview.redaction.warnings.join('; ')}</div> : null}</div></FinalReportPanel>;
}
