import { FinalReportPanel } from './FinalReportPanel';
export function RedactionPermissionPreviewPanel({ redaction }: { redaction:any }) { return <FinalReportPanel title="Redaction / Permission Preview" subtitle="Backend-calculated restricted content that will be redacted for the current scope."><div className="grid gap-3 md:grid-cols-3"><Metric label="Restricted attachments" value={redaction?.restrictedAttachments?.length??0}/><Metric label="Restricted history" value={redaction?.restrictedHistory?.length??0}/><Metric label="Restricted linked records" value={redaction?.restrictedLinked?.length??0}/></div>{redaction?.warnings?.length?<div className="mt-3 rounded border border-amber-400/25 bg-amber-500/10 p-2 text-xs text-amber-700 dark:text-amber-100">{redaction.warnings.join('; ')}</div>:null}</FinalReportPanel>; }
function Metric({label,value}:{label:string;value:any}){return <div className="rounded border border-slate-200 p-2 text-sm dark:border-cyan-300/10"><div className="text-slate-500">{label}</div><div className="font-black text-slate-900 dark:text-white">{value}</div></div>}
