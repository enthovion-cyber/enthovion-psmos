import { FinalReportPanel } from './FinalReportPanel';
export function ReportTemplateSelectionPanel({ templates, selectedTemplateId, onSelect }: { templates:any[]; selectedTemplateId?: string; onSelect:(id:string)=>void }) {
  return <FinalReportPanel title="Report Template Selection" subtitle="Configured company/site templates, with system fallback templates when no active template is stored."><div className="grid gap-2">{templates.map((t:any)=><button key={t.id} onClick={()=>onSelect(t.id)} className={`rounded-lg border p-3 text-left text-sm ${selectedTemplateId===t.id?'border-blue-400 bg-blue-500/10':'border-slate-200 bg-slate-50 dark:border-cyan-300/10 dark:bg-[#06111f]'}`}><div className="font-bold text-slate-900 dark:text-white">{t.template_name} <span className="text-xs text-slate-500">v{t.template_version}</span></div><div className="mt-1 text-xs text-slate-500">{t.template_type} · {(t.output_formats_json ?? []).join?.(', ') ?? 'PDF'}</div><div className="mt-1 text-xs text-slate-500">{t.description}</div></button>)}</div></FinalReportPanel>;
}
