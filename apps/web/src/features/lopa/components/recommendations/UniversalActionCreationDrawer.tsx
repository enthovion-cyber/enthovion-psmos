import { inputClass, LibraryDialog, LibraryField, selectClass } from '../libraries/LibraryShared';

export function UniversalActionCreationDrawer({ open, onClose, form, setForm, context, onSave, saving }: any) {
  const update = (key: string, value: any) => setForm((current: any) => ({ ...current, [key]: value }));
  return <LibraryDialog title="Create Universal Action" subtitle="Creates a real Universal Action linked back to this LOPA recommendation or gap." open={open} onClose={onClose}><div className="grid grid-cols-1 gap-4 md:grid-cols-2"><LibraryField label="Action title"><input className={inputClass} value={form.title ?? ''} onChange={(e) => update('title', e.target.value)} /></LibraryField><LibraryField label="Owner"><select className={selectClass} value={form.ownerId ?? ''} onChange={(e) => update('ownerId', e.target.value)}><option value="">Select owner</option>{(context.users ?? []).map((u: any) => <option key={u.id} value={u.id}>{u.displayName ?? u.email}</option>)}</select></LibraryField><LibraryField label="Priority"><select className={selectClass} value={form.priority ?? 'MEDIUM'} onChange={(e) => update('priority', e.target.value)}><option>LOW</option><option>MEDIUM</option><option>HIGH</option><option>SAFETY_CRITICAL</option></select></LibraryField><LibraryField label="Due date"><input className={inputClass} type="date" value={form.dueDate ?? ''} onChange={(e) => update('dueDate', e.target.value)} /></LibraryField><LibraryField label="Description"><textarea className={inputClass} rows={4} value={form.description ?? ''} onChange={(e) => update('description', e.target.value)} /></LibraryField><div className="space-y-2"><label className="flex gap-2 text-sm text-slate-200"><input type="checkbox" checked={!!form.blocking} onChange={(e) => update('blocking', e.target.checked)} />Closure blocker</label><label className="flex gap-2 text-sm text-slate-200"><input type="checkbox" checked={!!form.evidenceRequired} onChange={(e) => update('evidenceRequired', e.target.checked)} />Evidence required</label><label className="flex gap-2 text-sm text-slate-200"><input type="checkbox" checked={!!form.verificationRequired} onChange={(e) => update('verificationRequired', e.target.checked)} />Verification required</label></div></div><div className="mt-5 flex justify-end gap-2"><button className="lopa-button-secondary" onClick={onClose}>Cancel</button><button className="lopa-button-primary" disabled={saving} onClick={onSave}>{saving ? 'Creating...' : 'Create Action'}</button></div></LibraryDialog>;
}
