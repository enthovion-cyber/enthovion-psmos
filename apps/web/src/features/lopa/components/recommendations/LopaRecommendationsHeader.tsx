import { Plus, RefreshCw, Send, Download } from 'lucide-react';
import { TonePill } from '../overview/LopaOverviewShared';

export function LopaRecommendationsHeader({ header, summary, readOnly, onAdd, onAction, onSync, onExport }: any) {
  return <section className="rounded-xl border border-cyan-300/10 bg-[#071525] p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-black text-white">Recommendations / Actions</h2><TonePill>{header.status}</TonePill></div><p className="mt-1 text-sm text-slate-400">Track LOPA recommendations, risk reduction actions, and closure evidence.</p><div className="mt-2 text-xs text-slate-500">{header.lopaNumber} · {header.title} · Open blocking actions {summary.blockingActions ?? 0} · Overdue {summary.overdueActions ?? 0}</div></div><div className="flex flex-wrap gap-2"><button className="lopa-button-primary disabled:opacity-50" disabled={readOnly} onClick={onAdd}><Plus size={15} />Add Recommendation</button><button className="lopa-button-secondary disabled:opacity-50" disabled={readOnly} onClick={onAction}><Send size={15} />Create Action</button><button className="lopa-button-secondary" onClick={onSync}><RefreshCw size={15} />Sync Actions</button><button className="lopa-button-secondary" onClick={onExport}><Download size={15} />Export</button></div></div></section>;
}
