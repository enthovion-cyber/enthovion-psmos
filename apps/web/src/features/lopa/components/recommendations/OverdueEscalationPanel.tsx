import { LopaPanel } from '../overview/LopaOverviewShared';
export function OverdueEscalationPanel({ rows, onReminder, onEscalate }: { rows: any[]; onReminder: () => void; onEscalate: () => void }) { return <LopaPanel title="Overdue / Escalation Panel" action={<div className="flex gap-2"><button className="lopa-button-secondary" onClick={onReminder}>Reminder</button><button className="lopa-button-secondary" onClick={onEscalate}>Escalate</button></div>}><div className="grid gap-2">{rows.length ? rows.map((row) => <div key={row.id} className="rounded-lg border border-red-400/20 bg-red-500/10 p-3 text-sm text-red-100">{row.actionNumber} · {row.title} · Due {row.dueDate}</div>) : <div className="text-sm text-slate-400">No overdue actions.</div>}</div></LopaPanel>; }
