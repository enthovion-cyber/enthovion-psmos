import type { LopaRecommendationFilters } from '../../types/lopa-recommendation.types';
export function RecommendationFilters({ filters, setFilters }: { filters: LopaRecommendationFilters; setFilters: (filters: LopaRecommendationFilters) => void }) { return <div className="grid grid-cols-1 gap-2 rounded-xl border border-cyan-300/10 bg-[#071525] p-3 md:grid-cols-[1fr_160px_160px_160px_auto]"><input className="rounded-lg border border-cyan-300/10 bg-[#03101d] px-3 py-2 text-sm text-slate-100" placeholder="Search recommendation/action/source..." value={filters.q ?? ''} onChange={(e) => setFilters({ ...filters, q: e.target.value })} /><select className="rounded-lg border border-cyan-300/10 bg-[#03101d] px-3 py-2 text-sm text-slate-100" value={filters.status ?? ''} onChange={(e) => setFilters({ ...filters, status: e.target.value || undefined })}><option value="">All statuses</option><option>Draft</option><option>Open</option><option>Accepted</option><option>In Progress</option><option>Verified</option><option>Closed</option></select><select className="rounded-lg border border-cyan-300/10 bg-[#03101d] px-3 py-2 text-sm text-slate-100" value={filters.priority ?? ''} onChange={(e) => setFilters({ ...filters, priority: e.target.value || undefined })}><option value="">All priorities</option><option>Low</option><option>Medium</option><option>High</option><option>Critical</option></select><select className="rounded-lg border border-cyan-300/10 bg-[#03101d] px-3 py-2 text-sm text-slate-100" value={String(filters.blocking ?? '')} onChange={(e) => setFilters({ ...filters, blocking: e.target.value || undefined })}><option value="">All</option><option value="true">Blocking</option><option value="false">Non-blocking</option></select><button className="lopa-button-secondary" onClick={() => setFilters({})}>Reset</button></div>; }
