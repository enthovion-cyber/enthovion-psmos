import { File, FileImage, FileSpreadsheet, FileText } from 'lucide-react'; export function FileTypeIcon({ type }: { type?: string }) { const key=(type??'').toLowerCase(); const Icon=key.includes('pdf')||key.includes('doc')||key.includes('txt')?FileText:key.includes('xls')||key.includes('csv')?FileSpreadsheet:key.includes('png')||key.includes('jpg')||key.includes('jpeg')?FileImage:File; return <Icon className="h-4 w-4 text-cyan-600 dark:text-cyan-300"/>; }
