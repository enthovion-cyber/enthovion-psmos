import type { LopaReviewFilters } from '../../types/lopa-review-signoff.types';
export function ReviewSignoffFilters({ filters, setFilters }: { filters: LopaReviewFilters; setFilters: (filters: LopaReviewFilters) => void }) { const set = (key: keyof LopaReviewFilters, value: string) => setFilters({ ...filters, [key]: value || undefined }); return <div className="grid gap-2 rounded-xl border border-cyan-300/10 bg-[#071525] p-3 md:grid-cols-5"><input className="input" value={filters.q ?? ''} onChange={(event) => set('q', event.target.value)} placeholder="Search reviewer, comment, or signature..." /><select className="input" value={filters.decision ?? ''} onChange={(event) => set('decision', event.target.value)}><option value="">All decisions</option><option>Pending</option><option>Approved</option><option>Changes requested</option><option>Rejected</option></select><select className="input" value={filters.signatureStatus ?? ''} onChange={(event) => set('signatureStatus', event.target.value)}><option value="">All signatures</option><option>Pending</option><option>Signed</option></select><select className="input" value={filters.commentStatus ?? ''} onChange={(event) => set('commentStatus', event.target.value)}><option value="">All comments</option><option>Open</option><option>Resolved</option></select><label className="flex items-center gap-2 rounded-lg border border-cyan-300/10 px-3 text-sm text-slate-300"><input type="checkbox" checked={filters.blocking === 'true'} onChange={(event) => set('blocking', event.target.checked ? 'true' : '')} />Blocking only</label></div>; }
