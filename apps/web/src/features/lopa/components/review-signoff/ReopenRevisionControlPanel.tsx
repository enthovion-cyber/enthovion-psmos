import { RotateCcw } from 'lucide-react';
import { LopaPanel } from '../overview/LopaOverviewShared';
export function ReopenRevisionControlPanel({ header, snapshots, onReopen, busy }: { header: any; snapshots: any[]; onReopen: (reason: string) => void; busy?: boolean }) { return <LopaPanel title="Reopen / Revision Control"><div className="grid gap-3 text-sm md:grid-cols-2"><Info label="Current status" value={header.studyStatus} /><Info label="Current approval version" value={snapshots[0] ? String(snapshots[0].snapshot_version) : 'None'} /><Info label="Locked" value={header.locked ? 'Yes' : 'No'} /><Info label="Previous approvals" value={String(snapshots.length)} /></div>{header.locked ? <button disabled={busy} onClick={() => { const reason = window.prompt('Reason for reopening the approved study'); if (reason) onReopen(reason); }} className="lopa-button-secondary mt-4"><RotateCcw size={14} /> Reopen controlled revision</button> : <p className="mt-4 text-sm text-slate-400">Reopen becomes available after approval/closure. Previous snapshots and signatures are preserved and marked superseded.</p>}</LopaPanel>; }
function Info({ label, value }: { label: string; value: string }) { return <div className="rounded-lg border border-cyan-300/10 bg-[#03101d] p-3"><div className="text-xs uppercase text-slate-500">{label}</div><div className="mt-1 font-bold text-white">{value}</div></div>; }
