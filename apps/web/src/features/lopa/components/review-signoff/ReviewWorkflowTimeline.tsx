import { LopaPanel, TonePill } from '../overview/LopaOverviewShared';
export function ReviewWorkflowTimeline({ workflow }: { workflow: any }) { return <LopaPanel title="Review Workflow Timeline"><div className="space-y-3">{(workflow?.timeline ?? []).map((step: any) => <div key={step.step} className="grid grid-cols-[26px_1fr_auto] gap-3"><div className={`mt-1 h-5 w-5 rounded-full border-4 ${step.status === 'Complete' ? 'border-emerald-400 bg-emerald-400/20' : step.status === 'Current' ? 'border-blue-400 bg-blue-400/20' : 'border-slate-700 bg-transparent'}`} /><div><b className="text-sm text-white">{step.step}</b>{step.assigned ? <div className="mt-1 text-xs text-slate-400">Waiting on: {step.assigned}</div> : null}{step.comments ? <div className="mt-1 text-xs text-amber-200">{step.comments}</div> : null}</div><TonePill tone={step.status === 'Complete' ? 'success' : step.status === 'Current' ? 'info' : 'neutral'}>{step.status}</TonePill></div>)}</div></LopaPanel>; }
