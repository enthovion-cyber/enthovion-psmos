import { ExternalLink } from 'lucide-react';
import { LopaPanel, TonePill } from '../overview/LopaOverviewShared';

export function SectionCompletionChecklist({ checks, onOpen }: { checks: any[]; onOpen: (tab: string) => void }) { return <LopaPanel title="Section Completion Checklist"><div className="max-h-[500px] space-y-2 overflow-auto">{checks.map((check: any) => <div key={check.key} className="flex items-center justify-between gap-3 rounded-lg border border-cyan-300/10 bg-[#03101d] p-3 text-sm"><div><b className="text-slate-100">{check.label}</b><div className="mt-1 text-xs text-slate-500">System requirement: {check.key.replaceAll('_', ' ')}</div></div><div className="flex items-center gap-2"><TonePill tone={check.status === 'Complete' ? 'success' : check.status === 'Warning' ? 'warning' : 'danger'}>{check.status}</TonePill><button onClick={() => onOpen(check.key)} title="Open related section" className="rounded p-1 text-cyan-200 hover:bg-cyan-400/10"><ExternalLink size={14} /></button></div></div>)}</div></LopaPanel>; }
