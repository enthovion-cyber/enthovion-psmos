import { FileCheck2 } from 'lucide-react';
import { LopaPanel, TonePill } from '../overview/LopaOverviewShared';
export function ApprovalSnapshotPanel({ rows, onOpen }: { rows: any[]; onOpen: (row: any) => void }) { return <LopaPanel title="Approval Snapshots"><div className="space-y-2">{rows.map((row) => <button key={row.id} onClick={() => onOpen(row)} className="flex w-full items-center justify-between rounded-lg border border-cyan-300/10 bg-[#03101d] p-3 text-left hover:border-blue-400/30"><div className="flex items-center gap-2"><FileCheck2 size={16} className="text-emerald-300" /><div><b className="text-sm text-white">Approval version {row.snapshot_version}</b><div className="text-xs text-slate-500">{row.approved_at ? new Date(row.approved_at).toLocaleString() : '-'} - {row.approved_by}</div></div></div><TonePill tone={row.snapshot_status === 'Approved' ? 'success' : 'neutral'}>{row.snapshot_status}</TonePill></button>)}{!rows.length ? <div className="text-sm text-slate-400">No approval snapshot exists until the study is approved.</div> : null}</div></LopaPanel>; }
