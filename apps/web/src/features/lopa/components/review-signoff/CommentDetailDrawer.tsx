import { useState } from 'react';
import { X } from 'lucide-react';
export function CommentDetailDrawer({ comment, onClose, onSave, onResolve, saving }: { comment: any; onClose: () => void; onSave: (input: any) => void; onResolve: (reason: string) => void; saving?: boolean }) { const [text, setText] = useState(comment?.comment_text ?? ''); const [title, setTitle] = useState(comment?.title ?? ''); if (!comment) return null; return <aside className="fixed inset-y-0 right-0 z-[70] w-full max-w-xl overflow-auto border-l border-cyan-300/20 bg-[#071525] p-5 shadow-2xl"><div className="flex items-start justify-between"><div><h2 className="text-lg font-black text-white">{comment.comment_number}</h2><p className="text-sm text-slate-400">Structured review comment and change-request record.</p></div><button onClick={onClose} className="rounded p-2 text-slate-300 hover:bg-white/5"><X size={18} /></button></div><div className="mt-5 grid gap-3"><Field label="Title"><input value={title} onChange={(event) => setTitle(event.target.value)} /></Field><Field label="Comment"><textarea rows={6} value={text} onChange={(event) => setText(event.target.value)} /></Field><Info label="Type" value={comment.comment_type} /><Info label="Related section" value={comment.related_tab ?? 'General'} /><Info label="Status" value={comment.status} /><Info label="Owner" value={comment.owner_id ?? 'Unassigned'} /><Info label="Created" value={comment.created_at ? new Date(comment.created_at).toLocaleString() : '-'} /><Info label="Resolution" value={comment.resolution_notes ?? 'Not resolved'} /></div><div className="mt-5 flex flex-wrap gap-2"><button disabled={saving} onClick={() => onSave({ ...comment, title, commentText: text })} className="lopa-button-primary">Save</button>{comment.status !== 'Resolved' ? <button disabled={saving} onClick={() => { const reason = window.prompt('Resolution notes'); if (reason) onResolve(reason); }} className="lopa-button-secondary">Resolve Comment</button> : null}</div></aside>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="grid gap-1 text-sm text-slate-300"><span>{label}</span>{children}</label>; }
function Info({ label, value }: { label: string; value: string }) { return <div className="rounded-lg border border-cyan-300/10 bg-[#03101d] p-3 text-sm"><div className="text-xs uppercase text-slate-500">{label}</div><div className="mt-1 text-slate-100">{value}</div></div>; }
