import { AlertOctagon } from 'lucide-react';
import { LopaPanel, TonePill } from '../overview/LopaOverviewShared';
export function OpenBlockersRequiredActionsPanel({ rows, onAccept }: { rows: any[]; onAccept: (row: any) => void }) { return <LopaPanel title="Open Blockers / Required Actions"><div className="space-y-2">{rows.map((row) => <div key={row.id ?? row.blocker_key} className="flex items-start justify-between gap-3 rounded-lg border border-red-400/15 bg-red-500/5 p-3"><div><div className="flex items-center gap-2"><AlertOctagon size={15} className="text-red-300" /><b className="text-sm text-white">{row.blocker_title}</b></div><p className="mt-1 text-xs text-slate-400">{row.blocker_description}</p><div className="mt-2 text-xs text-slate-500">{row.source_tab ?? 'Review & Sign-Off'} - {row.severity}</div></div><div className="flex flex-col items-end gap-2"><TonePill tone={row.accepted_exception ? 'warning' : 'danger'}>{row.accepted_exception ? 'Exception accepted' : 'Blocking'}</TonePill>{row.id && !row.accepted_exception ? <button onClick={() => onAccept(row)} className="text-xs text-amber-200">Accept exception</button> : null}</div></div>)}{!rows.length ? <div className="rounded-lg border border-emerald-400/20 bg-emerald-500/10 p-3 text-sm text-emerald-100">No active review blockers returned by the backend.</div> : null}</div></LopaPanel>; }
