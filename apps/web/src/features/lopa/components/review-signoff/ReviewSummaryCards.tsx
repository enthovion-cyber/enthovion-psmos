import { CheckCircle2, ClipboardCheck, FileWarning, ShieldAlert, Signature, UsersRound } from 'lucide-react';

const icons = [ClipboardCheck, CheckCircle2, Signature, UsersRound, FileWarning, ShieldAlert];
export function ReviewSummaryCards({ summary }: { summary: any }) { return <div className="grid grid-cols-2 gap-3 md:grid-cols-4 2xl:grid-cols-6">{(summary?.cards ?? []).map((card: any, index: number) => { const Icon: any = icons[index % icons.length]!; const value = String(card.value ?? '-'); const danger = /block|pending|not ready|incomplete|no/i.test(value); const success = /approved|complete|yes|ready/i.test(value); return <div key={card.key} className="rounded-xl border border-cyan-300/10 bg-[#071525] p-3"><div className="flex items-center justify-between text-[11px] uppercase text-slate-500"><span>{card.label}</span><Icon size={15} className={danger ? 'text-red-300' : success ? 'text-emerald-300' : 'text-cyan-300'} /></div><div className={`mt-2 truncate text-lg font-black ${danger ? 'text-red-100' : success ? 'text-emerald-100' : 'text-white'}`}>{value}</div></div>; })}</div>; }
