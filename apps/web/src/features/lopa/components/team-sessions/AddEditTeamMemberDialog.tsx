import { LibraryDialog, LibraryField } from '../libraries/LibraryShared';
import { fieldClass } from './TeamSessionsUi';

export function AddEditTeamMemberDialog({ open, onClose, form, setForm, context, onSave, saving }: any) {
  const update = (key: string, value: any) => setForm((current: any) => ({ ...current, [key]: value }));
  return <LibraryDialog title="Add / Edit Team Member" subtitle="Use IAM/RBAC users or approved external contacts. No duplicate user management is created here." open={open} onClose={onClose}><div className="grid grid-cols-1 gap-4 md:grid-cols-2"><LibraryField label="Full name"><input className={fieldClass} value={form.fullName ?? ''} onChange={(e) => update('fullName', e.target.value)} /></LibraryField><LibraryField label="Email"><input className={fieldClass} value={form.email ?? ''} onChange={(e) => update('email', e.target.value)} /></LibraryField><LibraryField label="Company / organization"><input className={fieldClass} value={form.organization ?? ''} onChange={(e) => update('organization', e.target.value)} /></LibraryField><LibraryField label="Internal / external"><select className={fieldClass} value={form.internalExternal ?? 'Internal'} onChange={(e) => update('internalExternal', e.target.value)}><option>Internal</option><option>External</option></select></LibraryField><LibraryField label="Job title"><input className={fieldClass} value={form.jobTitle ?? ''} onChange={(e) => update('jobTitle', e.target.value)} /></LibraryField><LibraryField label="Department"><input className={fieldClass} value={form.department ?? ''} onChange={(e) => update('department', e.target.value)} /></LibraryField><LibraryField label="Discipline"><select className={fieldClass} value={form.discipline ?? ''} onChange={(e) => update('discipline', e.target.value)}><option value="">Select</option>{(context.disciplines ?? []).map((x: string) => <option key={x}>{x}</option>)}</select></LibraryField><LibraryField label="Study role"><select className={fieldClass} value={form.studyRole ?? ''} onChange={(e) => update('studyRole', e.target.value)}><option value="">Select</option>{(context.studyRoles ?? []).map((x: string) => <option key={x}>{x}</option>)}</select></LibraryField><LibraryField label="Access level"><select className={fieldClass} value={form.accessLevel ?? 'View only'} onChange={(e) => update('accessLevel', e.target.value)}>{(context.accessLevels ?? []).map((x: string) => <option key={x}>{x}</option>)}</select></LibraryField><LibraryField label="Responsibility"><textarea className={fieldClass} rows={3} value={form.responsibilityDescription ?? ''} onChange={(e) => update('responsibilityDescription', e.target.value)} /></LibraryField><label className="flex gap-2 text-sm text-slate-200"><input type="checkbox" checked={!!form.requiredParticipant} onChange={(e) => update('requiredParticipant', e.target.checked)} />Required participant</label><label className="flex gap-2 text-sm text-slate-200"><input type="checkbox" checked={!!form.votingParticipant} onChange={(e) => update('votingParticipant', e.target.checked)} />Voting / decision participant</label><label className="flex gap-2 text-sm text-slate-200"><input type="checkbox" checked={!!form.reviewer} onChange={(e) => update('reviewer', e.target.checked)} />Reviewer</label><label className="flex gap-2 text-sm text-slate-200"><input type="checkbox" checked={!!form.approver} onChange={(e) => update('approver', e.target.checked)} />Approver</label><label className="flex gap-2 text-sm text-slate-200"><input type="checkbox" checked={!!form.facilitator} onChange={(e) => update('facilitator', e.target.checked)} />Facilitator</label><label className="flex gap-2 text-sm text-slate-200"><input type="checkbox" checked={!!form.scribe} onChange={(e) => update('scribe', e.target.checked)} />Scribe</label><LibraryField label="Notes"><textarea className={fieldClass} rows={3} value={form.notes ?? ''} onChange={(e) => update('notes', e.target.value)} /></LibraryField></div><div className="mt-5 flex justify-end gap-2"><button className="lopa-button-secondary" onClick={onClose}>Cancel</button><button className="lopa-button-primary" disabled={saving} onClick={onSave}>{saving ? 'Saving...' : 'Save Member'}</button></div></LibraryDialog>;
}
