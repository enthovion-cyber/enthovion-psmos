import { z } from 'zod'; export const lopaEvidenceMappingSchema=z.object({attachmentId:z.string().optional(),documentLinkId:z.string().optional(),relatedTab:z.string().min(1),evidencePurpose:z.string().min(1),required:z.boolean().optional(),blockingIfMissing:z.boolean().optional(),notes:z.string().optional()}).refine(v=>!!v.attachmentId||!!v.documentLinkId,{message:'Select an attachment or controlled document.'});
