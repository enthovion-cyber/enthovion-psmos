import { useQuery } from '@tanstack/react-query'; import { lopaAttachmentsService } from '../services/lopa-attachments.service'; export function useLopaAttachmentVersions(studyId:string,attachmentId?:string){return useQuery({queryKey:['lopa','attachment-versions',studyId,attachmentId],queryFn:()=>lopaAttachmentsService.versions(studyId,attachmentId!),enabled:!!studyId&&!!attachmentId});}
