import { useQuery } from '@tanstack/react-query'; import { lopaAttachmentsService } from '../services/lopa-attachments.service'; export function useLopaAttachmentComments(studyId:string,attachmentId?:string){return useQuery({queryKey:['lopa','attachment-comments',studyId,attachmentId],queryFn:()=>lopaAttachmentsService.comments(studyId,attachmentId!),enabled:!!studyId&&!!attachmentId});}
