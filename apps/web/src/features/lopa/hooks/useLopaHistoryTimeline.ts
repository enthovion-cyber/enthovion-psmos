import { useQuery } from '@tanstack/react-query'; import { lopaHistoryService } from '../services/lopa-history.service'; import type { LopaAttachmentFilters } from '../types/lopa-attachment.types'; export function useLopaHistoryTimeline(id:string,filters:LopaAttachmentFilters={}){return useQuery({queryKey:['lopa','history-timeline',id,filters],queryFn:()=>lopaHistoryService.timeline(id,filters),enabled:!!id});}
