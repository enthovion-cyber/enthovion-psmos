import { CheckboxField, FormSection, SelectField, TextArea } from '../../sif/sections/section-fields';
export function SafeguardTestEvaluationSection({ value, onChange }: { value: any; onChange: (patch: any) => void }) {
  return <FormSection title="Backend Evaluation Inputs"><SelectField label="Final result" value={value.finalResult ?? value.final_result} options={['Passed', 'Failed', 'Passed With Deficiency', 'Incomplete', 'Not Evaluated']} onChange={(finalResult) => onChange({ finalResult })} /><CheckboxField label="Follow-up required" checked={value.followupRequired ?? value.followup_required} onChange={(followupRequired) => onChange({ followupRequired })} /><CheckboxField label="Startup blocked" checked={value.startupBlocked ?? value.startup_blocked} onChange={(startupBlocked) => onChange({ startupBlocked })} /><TextArea label="Deficiency summary" value={value.deficiencySummary ?? value.deficiency_summary} onChange={(deficiencySummary) => onChange({ deficiencySummary })} /></FormSection>;
}
