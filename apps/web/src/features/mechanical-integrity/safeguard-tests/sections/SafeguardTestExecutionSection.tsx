import { CheckboxField, FormSection, SelectField, TextArea } from '../../sif/sections/section-fields';
export function SafeguardTestExecutionSection({ value, onChange }: { value: any; onChange: (patch: any) => void }) {
  return <FormSection title="Execution / As-Found / As-Left"><SelectField label="As-found condition" value={value.asFoundCondition ?? value.as_found_condition} options={['Normal', 'Degraded', 'Failed', 'Unknown']} onChange={(asFoundCondition) => onChange({ asFoundCondition })} /><SelectField label="As-left condition" value={value.asLeftCondition ?? value.as_left_condition} options={['Normal', 'Degraded', 'Failed', 'Removed From Service']} onChange={(asLeftCondition) => onChange({ asLeftCondition })} /><CheckboxField label="Bypass used during test" checked={value.bypassUsed ?? value.bypass_used} onChange={(bypassUsed) => onChange({ bypassUsed })} /><TextArea label="Test procedure / execution notes" value={value.executionNotes ?? value.execution_notes} onChange={(executionNotes) => onChange({ executionNotes })} /></FormSection>;
}
