import { FormSection, SelectField, TextField } from '../../sif/sections/section-fields';
export function SafeguardTestContextSection({ value, onChange }: { value: any; onChange: (patch: any) => void }) {
  return <FormSection title="Test Context"><TextField label="Test number" value={value.testNumber ?? value.test_number} onChange={(testNumber) => onChange({ testNumber })} /><SelectField label="Safeguard type" value={value.safeguardType ?? value.safeguard_type} options={['SIF', 'Interlock', 'Critical Alarm']} onChange={(safeguardType) => onChange({ safeguardType })} /><TextField label="Safeguard ID" value={value.safeguardId ?? value.safeguard_id} onChange={(safeguardId) => onChange({ safeguardId })} /><TextField label="Equipment ID" value={value.equipmentId ?? value.equipment_id} onChange={(equipmentId) => onChange({ equipmentId })} /><TextField label="Performed by" value={value.performedByUserId ?? value.performed_by_user_id} onChange={(performedByUserId) => onChange({ performedByUserId })} /><TextField label="Test date" type="date" value={value.testDate ?? value.test_date} onChange={(testDate) => onChange({ testDate })} /></FormSection>;
}
