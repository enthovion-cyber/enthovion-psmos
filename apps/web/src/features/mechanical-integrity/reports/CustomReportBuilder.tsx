'use client';

export function CustomReportBuilder() {
  return (
    <section className="rounded-xl border border-[var(--psm-line)] bg-[var(--psm-surface)] p-4">
      <h2 className="font-semibold">Custom Report Builder</h2>
      <p className="mt-2 text-sm text-[var(--psm-muted)]">
        Templates, sections, filters, output formats, scheduling, and official/export controls are stored in the backend report template APIs. Use Generate Report for immediate backend generation.
      </p>
    </section>
  );
}
