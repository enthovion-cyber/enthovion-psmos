import { PsiCard, PsiEmptyState } from '../../shared/PsiUi';
import type { SafeOperatingLimitDetail } from '../../types/safe-operating-limit.types';

export function ControlsSafeguardsTab({ detail }: { detail: SafeOperatingLimitDetail }) {
  return <PsiCard title="Controls / Safeguards" subtitle="Alarm, critical alarm, interlock, SIF/SIS, PSV, SOP, control loop, analyzer, mechanical protection, and training links.">{!detail.controls.length ? <PsiEmptyState title="No controls linked" message="Severe or catastrophic deviations require safeguards or action." /> : <div className="space-y-3">{detail.controls.map((row) => <article key={String(row.id)} className="rounded-lg border border-[var(--psm-line)] bg-[var(--psm-surface-2)] p-3"><div className="flex flex-wrap items-center justify-between gap-3"><h3 className="font-semibold">{String(row.control_description)}</h3><span className="rounded-full border border-info/30 bg-info/10 px-2 py-1 text-xs font-semibold text-info">{String(row.control_type)}</span></div><dl className="mt-3 grid gap-2 text-sm md:grid-cols-3">{['linked_module','linked_record_id','setpoint','safe_state','required_response','reliability_criticality','test_proof_inspection_requirement','owner_user_id','notes'].map((key) => <div key={key}><dt className="text-[var(--psm-muted)]">{key.replaceAll('_', ' ')}</dt><dd>{String(row[key] ?? '-')}</dd></div>)}</dl></article>)}</div>}</PsiCard>;
}
